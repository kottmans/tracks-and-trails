# Re-review handoff — `T189-R1` and `T186-R1`, 2026-08-08

**From:** Implementer
**To:** Reviewer (Codex)
**Review base:** `6a0d97f` — the head your reviews read
**Review head:** `365182c` — committed, clean tree
**Prior reviews:** *2026-08-08 — `T-189` and `T-186`*, both **Changes requested**, one High each

```
git diff 6a0d97f..365182c
```

Both findings are corrected. **Neither is claimed closed.**

| Finding | Severity | Disposition |
|---|---|---|
| `T189-R1` | High | **Corrected.** The recording step's comment now states the gate instead of denying it |
| `T186-R1` | High | **Corrected.** Ten further claims fixed; the method changed, not just the count |

**Both were mine and both were avoidable**, in the same way: I changed a thing and did not re-read
what else asserted something about it. Worth saying once rather than in each section.

Four commits are in the boundary. Two are the corrections; the others are
`d9e9398` (the previous handoff document) and `f007182` (a `T-188` survey, no fixture — **not a
correction to either finding**, and it does not need a verdict here).

---

## `T189-R1` — a workflow that stated both halves

**What you found.** `ci.yml`'s *Record the environment* step said the default suite does not need
ffmpeg and that recording is *"rather than a gate"*, two steps above a *Full suite* step I had just
made require it.

**The correction, and the part worth reading.** The claim was **not wrong when written**. It rested
on a real measurement — *"measured on Linux with it removed from PATH, the counts are identical"* —
taken before `T108-R1` put the merged-file proof into the default suite. Nothing re-took it. So the
sentence outlived its evidence rather than being false at the time, and that is recorded in place,
because it is the same shape as the stale premises `T-186` is sweeping up.

What has **not** changed is stated as plainly as what has: this step still installs nothing, and
restoring ffmpeg on `STARBASE` stays a deliberate maintainer act (`OPS-005`). Recording says *which*
configuration ran; the step below refuses the wrong one.

Verified the comment still sits at the step's property level, a sibling of `run:` — the same
structural check as before, since **`ci.yml` is still unexecuted here** (no `pyyaml`, and I will not
install one into the maintainer's venv to lint a workflow). The first CI run at this head remains
the requirement's first genuine exercise.

## `T186-R1` — a search, not a sweep

**Ten more, in seven files.** The three you named in `queue_view.py` were four; the file you did not
name, `file_actions.py`, held the flattest one — *"it would act on 'the selection', and there are
two"*.

| Where | What it claimed |
|---|---|
| `queue_view.py` | *"Shared with History since `T-145`"* |
| `queue_view.py` | *"History names the same download with the same function"* |
| `queue_view.py` | *"the queue draws its state as a chip; History does not … every history row is finished"* |
| `queue_view.py` | *"because History **now** flattens the same way"* |
| `row_delegate.py` | *"which is the history row's shape exactly"* |
| `file_actions.py` | *"it would act on 'the selection', and **there are two**"* |
| `format_text.py` | *"a record **is** not going to be downloaded as anything"* |
| `tests/ui/test_reveal.py` | *"the ordinary case for a history record"* |
| `tests/ui/test_file_actions.py` | *"a `history` row **outlives** the setting"*, and *"not only on history"* |
| `tests/ui/test_theme_metrics.py` | *"on the history rows"* |

**The method changed, and that is the actual correction.** The first pass grepped for present-tense
verbs adjacent to *History* and stopped at what that returned — eight of eighteen. **A pattern that
finds eight of eighteen looks exactly like a finished sweep.** This pass enumerated all **129**
mentions across `src/` and `tests/`, classified every one, and then re-audited with a check that
reads the *surrounding* lines, so a correction note above or below counts as the marker it is.

**Four mentions survive the re-audit and are correct** — please check these specifically, since they
are where an over-eager sweep would do damage:

- **Two in `tests/unit/test_persistence.py`** are about frozen schema fixtures that genuinely
  **contain** `history` rows. The migration test asserts the table is *dropped*; the present tense
  there describes fixture data and must stay.
- **`tests/ui/test_queue_view.py`** — *"grows with queue history"* is the queue's length over time,
  nothing to do with the feature.
- **`tests/ui/test_format_text.py`** — historical narrative about a deleted file.

**Same keep-rule as the first pass.** Where History merely *justified* a live invariant, the reason
is restated rather than removed. `file_actions.py` is the clearest: the context menu it argued for
is still the arrangement in place, and a second table is what `REQ-021` would need again. So the
paragraph now says the ambiguity **was** real and no longer arises, rather than losing the argument
with the sentence that carried it.

Migrations, frozen fixtures and `ai/archive/` are untouched. Production behaviour is unchanged —
comments, docstrings and assertion messages only.

## `f007182` — `T-188` survey, for awareness

No fixture, no verdict wanted, task still `Proposed`. Two results:

- **`media.ccc.de` is closed rather than unresolved.** The entry invited *"if a conference is found
  whose API states a licence, this task is nearly done"*; the original probe sampled one conference
  (0 of 222). The whole archive is now swept — **452 conferences, 16,828 events, zero stated
  licences.**
- **One candidate**, put to the maintainer rather than committed: the DASH-IF / Akamai Big Buck
  Bunny manifest, which yields ten video-only formats with `acodec: 'none'` beside one audio-only
  with `vcodec: 'none'`. CC BY 3.0, unsigned, stable. Its two weaknesses are in the entry — a
  `generic` extractor rather than a site one, and a licence known from content identity rather than
  stated by the source.

## `365182c` — your own reviews, committed

`ai/REVIEWS.md` had been uncommitted since you wrote it, as it is every round. Landed unmodified
and append-only (63 lines added, none removed), **before** the corrections in history order, so the
findings precede their fixes. Flagged because it is your file and I changed nothing in it.

## Checks, with their actual results

Run at `365182c`, Linux:

| Check | Result |
|---|---|
| `git diff --check` | clean |
| `ruff check` | All checks passed |
| `ruff format --check` | 217 files already formatted |
| `mypy` (src + tests) | Success: no issues found in 125 source files |
| `mypy --platform win32` | Success: no issues found in 125 source files |
| `pytest tests/unit/test_task_placement.py` | 14 passed |
| Suites whose prose changed | 305 passed |
| `QT_QPA_PLATFORM=offscreen pytest` | **2787 passed, 14 skipped, 2 deselected** in 365 s |

**2787 is unchanged from the reviewed head, and that is the expected result** — both corrections are
comments and docstrings, so a moved count would have meant something went wrong.

## What is not claimed

- **No verdict claimed** for `T-189` or `T-186`.
- **`ci.yml` still has not run.** Structural verification only, as before.
- **Windows runtime and real sites remain unverified.**
- **`T-188` is not a correction** and is outside both findings.
