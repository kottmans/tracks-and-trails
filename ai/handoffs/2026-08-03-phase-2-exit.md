# Phase 2 exit review — handoff

You are the Reviewer (`AGENTS.md` §3). **This is a phase exit review, not a task review.** Phase 0
and Phase 1 each required one and neither could self-certify; this is the same thing for Phase 2.

**Repository:** `/mnt/storage/software_projects/tracks-and-trails/tracks-and-trails`, branch `main`
**Head:** `5eb2611`. `origin/main` is at `53b07ec`; everything after it is local and docs-only
except `471d424`, `b07de03` and `5eb2611`, which touch tests.

**What is being asked:** criterion 6 — *"reviewed and signed off"*. Every deliverable is approved,
`T-118` is approved with follow-ups, and nothing is red. The question is whether Phase 2 has
actually met its exit criteria, not whether its tasks were done.

---

## Read this first: the table will be wrong somewhere

Phase 1's exit review found **two wrong rows** — a headless criterion evidenced by a static import
guard and by Qt's offscreen platform plugin, neither of which detaches a display. Both had been
read past repeatedly. Building the table against the repository is what exposed them.

`ai/IMPLEMENTATION_PLAN.md` carries Phase 2's seven criteria with evidence per row. **Treat that
table as a claim, not a record.** I have not rebuilt it, deliberately: I have been inside this
work all day, and the failure mode here is precisely reading a claim you already believe. Four
review rounds on `T-118` all turned on the same thing — a test written by the person who wrote the
fix, agreeing with it rather than checking it.

The rows most worth attacking, on the evidence of how this phase went:

- **Criterion 5** (no worker outlives exit, both platforms) — "both platforms" is doing a lot of
  work in a phase where the Windows story changed twice today.
- **Criterion 1** (three concurrent downloads, independent progress, UI interactive) — three
  distinct claims. The row already lost one review round for asserting all three while observing
  the first.
- **Criterion 7** (a user can actually start a queue) — added *by* the phase proof after every
  deliverable was approved and no user could start a queue. Worth asking what else the criteria
  never named.

## What changed today, because it bears on the evidence

**The gate itself was rebuilt**, and that changes what "CI is green" means:

- The Windows leg of `check` **no longer runs** while `WINDOWS_RUNNER` points at `STARBASE`; the
  desktop job ran the identical suite on the same machine (1972/21/32 in both, four seconds apart).
- The **full Windows suite is off the push path** — nightly, on demand, on a pull request, or a
  push whose message contains `[win]`.
- What is genuinely given up is the **offscreen platform plugin on Windows**. The real plugin on
  Windows and offscreen on Linux both remain.

`ai/TESTING.md` §10 is canonical. If you think a phase exit needs a full matrix run on the exact
head, say so — it is one `workflow_dispatch` away and I have not spent it.

**The most recent full Windows evidence** is run `30859578131` at `53b07ec`: `STARBASE` green on
the full suite, hosted `windows-latest` 1 failed / 1971 passed, the failure being `T-118`'s own
scaling oracle (now `T-122`, corrected). Runs since then cover `dca3bfa` and later but **not**
`5eb2611`.

## Open items, and why none of them blocks the exit

| Item | State | Bearing on the exit |
|---|---|---|
| `T-121` | Proposed, narrowed | Test fixture. Two parts fixed; the abort itself is unreproduced. **Latent twice over** — it did not recur *and* hosted Windows no longer runs |
| `T-123` | Proposed, for evaluation | Parallel suite. Deliberately not started before an exit review: intermittent failures are worse than a slow suite |
| `T-074` | Ready | Undiagnosed Windows segfault, downgraded by `OPS-007` maintainer decision |
| `T-066` | **Ready as of today** | Was blocked on frozen-artifact evidence that now exists |
| `T-092`, `T-056`, `T-068`, `T-033`, `T-039` | Blocked | Re-triaged today — see below |

**I re-triaged the Blocked list because its premise changed**, and doing so corrected my own
summary rather than confirming it. `STARBASE` returned on 2026-08-03 and now runs every Windows
job, so I expected several to fall out. Only one did:

- **`T-066` is genuinely unblocked** — its blocker was "both `frozen` jobs are hosted and have not
  started since the quota ran out", and `frozen windows` now runs on `STARBASE`.
- **`T-092` was never blocked on availability** — it needs a person at the machine to arm dumps and
  crash something. "Blocked on `STARBASE`" said the wrong thing.
- **`T-056` already had its `STARBASE` evidence, and that is the finding**: reverting the fix
  passes 20/20 there. More runs of the same machine cannot close it.
- **`T-068`'s remaining half got harder today** — it asks why hosted runners never showed the
  fault, and hosted Windows no longer runs. Answering it now needs `WINDOWS_RUNNER` unset for a run.
- **`T-033`** waits on a maintainer decision; **`T-039`** on a Phase 5 installer.

## Two things I corrected today that you should sanity-check

Both were gates that could not fire, found while doing something else:

- **`AGENTS.md` §9** told agents to run `-m process_tree` tests "one worktree at a time" — and
  **no test carries that marker**; it was retired when `T-019` landed. `ai/TESTING.md`'s type table
  also still said those tests were *excluded* from the default run. Both now name the files.
- **`T083-R2`** — the backoff test measured from a `spin` poll that *noticed* the failure, so poll
  lag was subtracted from the interval being bounded. Both instants now come from the manager's own
  signals. Verified it still fails with a zero backoff (0.05 s reported).

## Evidence at this head

| Gate | Result |
|---|---|
| `ruff check .` / `ruff format --check .` | clean, 143 files |
| `mypy src` / `mypy` / `mypy --platform win32` | clean — 43, 101, 101 files |
| Full suite (Linux) | **1986 passed, 11 skipped, 2 deselected** in 4m52s |
| Task placement | 14 passed |

## What is not verified

- **This head on Windows.** `5eb2611` has not run there; the last full Windows evidence is
  `53b07ec`. The changes since are tests and documents, but that is my reading, not a run.
- **A real extractor.** Every probe in the suite replays a recorded fixture or a localhost server.
- **The dark theme has never been on a screen.** `T-120`'s contrast is asserted as arithmetic.
- **`OPS-004`'s subjective half** — whether Windows rendering *looks* right, whether Narrator
  *sounds* coherent. Blocks first release, not this phase, and stated so since Phase 0.

## If you want the application in front of you

```bash
cd /mnt/storage/software_projects/tracks-and-trails/tracks-and-trails && .venv/bin/python -m tracks_and_trails
```
