# Re-review handoff — `T188-R1`, third pass, 2026-08-08

**From:** Implementer, and the maintainer for the two amendments
**To:** Reviewer (Codex)
**Review base:** `226366d` — the head your re-review read
**Review head:** `3ce0b8b` — committed, clean tree
**Prior review:** *2026-08-08 — `T188-R1` focused correction re-review*. Criterion 3 **Resolved**,
criterion 4 **Open — High**

```
git diff 226366d..3ce0b8b
```

**One verdict wanted: `T-188`.** All five criteria are met, two of them by maintainer amendment.

---

## The short version

**You were right that rewording is not removing.** I implemented the removal you asked for — commit
`1ba5f73` — and it **fails five `tests/ui/test_format_table.py` cases**, including `T-075`'s refusal
path. That is the substance of this pass: the criterion could not be met as written, and the
alternative reading would have forced the fixture's provenance to lie.

Both endings are maintainer amendments, and both bind a **property** where the original bound a
**mechanism**. Neither is a concession on the finding.

## §1 · Criterion 4 — what the removal measured

`derived_format_columns` is the only fixture that is **pairable *and* holds complete formats**:

| Fixture | pairable | COMPLETE formats |
|---|---|---|
| `derived_format_columns` | yes | **4** |
| `dash_akamai_big_buck_bunny` | yes | **0** |

`tests/ui/test_format_table.py` needs both **in one source**: pairability is what makes the mode
control exist, and a complete format inside the same source is what `T-075`'s refusal has to refuse.

**A recorded source cannot be both, and the reason is structural rather than incidental.** DASH
separates the streams by construction — which is the very property that makes it the merge-pair
evidence. The thing that qualifies it for criterion 1 disqualifies it here.

### Why the wording was amended rather than satisfied

Two readings, and both were tried:

- **Drop `formats[3]`/`formats[4]` from `what_is_synthetic`, keep the formats.** That field declares
  which entries are **synthetic**. Those two are synthetic and still present, so removing them from
  the declaration makes a truthful field **false**. The criterion would have been met by making the
  provenance lie.
- **Remove the formats.** Implemented at `1ba5f73`, costs five cases, reverted at `71a9dfa`.

So criterion 4 now binds what it was protecting — **the derived fixture stops being `REQ-008`'s
evidence, and its provenance names the fixture that is** — and keeps the pair for the one
combination no recorded source can supply. The amendment is in the criterion with the measurement
attached, so a later reader sees why rather than a softened sentence.

**`why_this_source` states the division** in the fixture itself: these two are still synthetic and
still declared, they are *not* the pair's evidence, and what they remain for is the
pairable-with-a-complete-format path — with the zero-complete-formats measurement and the commit
that established it.

## §2 · Criterion 2 — amended earlier in this boundary

Also a maintainer ruling (`967e07a`), and worth flagging because you accepted the *source* under a
criterion that still said the licence must be **stated by the source**.

Your review treated the maintainer's ruling as authority for the source, which it was — **but a
ruling to accept a source is not an amendment to a criterion**, and leaving that line standing would
have put `T-188` exactly where `T143-R1` blocked `T-143`: everything delivered, one criterion unmet,
the gap closed by narrative.

The criterion was aimed at **belief about a publisher's usual practice** — media.ccc.de's talks are
widely thought to be CC BY and its API states nothing for any of 16,828 events. It now requires the
licence to be **verifiable for the specific work**, with the fixture recording how it was
established. **The teeth are unchanged**: media.ccc.de still fails, because the line that does the
work is *specific work* versus *publisher practice*. Precedent is `OPS-013`, which bound exit
criterion 1 to where a source reports a column rather than listing exceptions. `ai/TESTING.md` §5 is
untouched — it never required the statement.

## §3 · A mistake of mine, and what it cost

**I committed the removal having run only the fixture and selection modules.** Criterion 5 names the
*dialog* tests and I did not run them; the full suite found the five failures afterwards. The
removal reaching history is the one thing I would defend — it turned "I think this is redundant"
into a measurement — but it should have been measured **before** the commit, not after.

*(Separately: the 23-failure run reported last round was stale bytecode, not a regression. Your
isolated `PYTHONPYCACHEPREFIX` run is the right answer and every figure here is measured with
bytecode writing disabled.)*

## §4 · Criteria, as they now stand

| # | Criterion | State |
|---|---|---|
| 1 | A source is found and captured, or the task closes with the finding that none exists | **Met** — `dash_akamai_big_buck_bunny` |
| 2 | The licence is verifiable for the specific work, and the fixture records how *(amended)* | **Met** |
| 3 | The routing assertions read the recorded fixture, and say which | **Met** — your last pass resolved it |
| 4 | The derived fixture stops being the pair's evidence and says so; keeps its place for shapes no source has, and for pairable-with-complete *(amended)* | **Met** |
| 5 | `ruff`, `ruff format`, both `mypy` gates, and the fixture, selection and dialog tests are clean | **Met** |

---

## Checks, with their actual results

Run at `3ce0b8b`, Linux, bytecode caching disabled:

| Check | Result |
|---|---|
| `git diff --check` | clean |
| `ruff check` | All checks passed |
| `ruff format --check` | 220 files already formatted |
| `mypy` (src + tests) | Success: no issues found in 125 source files |
| `mypy --platform win32` | Success: no issues found in 125 source files |
| `pytest tests/unit/test_task_placement.py` | 14 passed |
| Fixture + selection + **format table** + dialog suites | **358 passed, 11 skipped** |
| `QT_QPA_PLATFORM=offscreen pytest` | **2801 passed, 17 skipped, 2 deselected** in 369 s |

The format-table module is listed explicitly because its absence from my last run is what let the
regression through. **2801 is unchanged from the last clean measurement**, which is the expected
result: the pair is back where it was and this boundary changes two lines of provenance and two
criteria.

## What is not claimed

- **No verdict claimed** for `T-188`.
- **`ci.yml` has still not executed.** Untouched in this boundary.
- **`ai/REVIEWS.md` carries no edit of mine** — `bc19b4d` commits your record unmodified, on its own.
- **Windows runtime and real sites remain unverified.**
