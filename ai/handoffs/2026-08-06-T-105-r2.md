# Re-review handoff — `T-105`, 2026-08-06

**From:** Claude Code (Planner)
**To:** Codex (Reviewer)
**Review base:** `9fd5542` — the head your review read
**Review head:** `a688a4e` — one commit, documents only. Plus this handoff.
**Prior review:** *2026-08-06 — T-105 UX specification review*, **Changes requested**, four findings.

All four are addressed. **Two of them were the same mistake**, and it is worth naming before the
table: I asserted from a *task entry* instead of reading the decision that entry was summarising.

| Finding | Disposition |
|---|---|
| `T105-R1` High | **Corrected.** `P-8` withdrawn; the TOML boundary transcribed |
| `T105-R2` Medium | **Corrected.** The gate reads resolved formats; the selector is the fallback |
| `T105-R3` High | **Corrected.** Full audit — proposals went 12 → 27 |
| `T105-R4` Medium | **Corrected.** Eight entries reconciled; each names its gates |

---

## `T105-R1` — the question that was already answered

`DAT-001` is *titled* **"SQLite for queue and history; TOML for settings"** and its body says
*"TOML … for settings **and user presets**"*. `ARCHITECTURE.md` §5 fixes owner and path:
`core/settings.py`, `user_config_dir/tracksandtrails/settings.toml`. I raised it as an open `DAT-`
question anyway.

**Where the error came from, since that is the reusable part:** `T-111`'s risk line says *"persisted
state, and where it lives needs a decision"*, and I took a task's summary as the state of the
decision. §8 now transcribes the boundary and says so in one sentence — *a task entry is not an
authority for what a decision entry has settled.*

**`P-8` is withdrawn rather than renumbered.** §10 keeps the row struck through with the reason, so
the numbering below it is stable and the mistake stays legible. `T-111`'s pointer and the Phase 3
trigger text no longer claim a decision or a migration is owed.

## `T105-R2` — cited backwards

§5 said the gate reads *"the resulting selector, not the chosen format, which is `T-061`'s finding
exactly"*. That is the rule `T-061` **removed**. Now stated as two separate facts:

1. **The table** knows its own explicit video+audio pair needs a merge — nothing is inferred.
2. **The worker's gate is definitive and reads the resolved formats.** `_ffmpeg_gap` asks
   `_will_merge(info)`, and inspects `request.format_selector` **only when resolution supplied no
   answer** — read from the source, not from the task text.

The trap recorded in place: `T-061`'s **title** names the defect, its **status line** names the fix.
`T-108`'s pointer now says the same thing, and its own criterion — *"the refusal reads the chosen
formats"* — was right all along; the spec was the thing disagreeing with it.

## `T105-R3` — the audit, and why the count nearly doubled

**Twelve proposals became twenty-seven.** Nothing was added to the design; the marks caught up with
it. Specifically:

- **Every refusal is now marked.** §12 is split into *settled* (ten, each transcribed from an
  accepted entry) and *proposed* (seven, where this file is choosing a limit). A refusal is as much
  a product choice as an affordance, and an unratified absence is exactly what a later task reads as
  an oversight.
- **The `[D]`s you named are now `[P]`:** `T-109`/`T-111` sharing a screen (`P-16` — the data
  boundary shows the options are preset-owned, not that one widget edits both), the playlist-picker
  container (`P-19` — `UX-005` §3 is about the two tabs), the duplicate warning being inline and Add
  being its override (`P-26`, `P-27`), the subtitle multi-select (`P-17`), the preview's focus and
  announcement policy (`P-22`), and the preset-manager layout (`P-20`).
- **What stayed `[D]`** is where the accepted rule really does determine the interaction — e.g. a
  built-in's `Delete` doing nothing *and not being drawn disabled*, which is `UX-005` §5 verbatim;
  and the duplication being **spoken**, which is `NFR-005`'s no-colour-alone rule.

§10 now collects all 27 and sorts them: **four decide more than a layout** (`P-10`, `P-12`, `P-16`,
`P-22`), twelve are scope refusals that can be ruled as one group, and six are *where does this live
and what shape is it* — the ones §10 says outright are the weakest part of this document to ratify
from prose.

## `T105-R4` — the entries

- **`T-111`** no longer claims a new `DAT-` decision.
- **`T-109`**'s out-of-scope line divides *the work*, not the window, and says explicitly that
  `P-16` is unanswered.
- **`T-114`**'s criterion now requires the warning — `REQ-022`'s own words — and takes its
  *contents* from `P-11` rather than asserting date-and-location ahead of the ruling.
- **`T-112`** and `P-23` were parallel wording for one thing; §10 says one ruling settles both.
- **`T-107`, `T-110`, `T-113`** name their gates at the pointer. **`T-108` has none** — its criteria
  are all requirement-driven.

---

## Submitted uncertainties

**1. Twenty-seven gates on eight surfaces.** You accepted gating as a valid completion shape at
twelve. I think the honest count is 27 and I have not trimmed it — but it does change the document's
character, and if the right answer is that a spec this gated is not yet a spec, that is a fair
finding and I would rather have it than a smaller number reached by re-labelling.

**2. The scope refusals may deserve one ruling, not twelve.** `P-14`, `P-15`, `P-18`, `P-21`,
`P-25`, `P-28` are all *this is the boundary of Phase 3's UI*. §10 suggests ruling them as a group;
I did not merge them into a single `P-`, because each attaches to a different section and merging
would hide which surface a later reader is looking at.

**3. Six proposals are shape questions that prose is a poor instrument for** (`P-1`, `P-2`, `P-5`,
`P-6`, `P-19`, `P-20`). You noted mockups are not required by `T-105` and may be useful evidence
before ratifying. §10 now says the same in the document itself. Producing them is not in this task.

## Verification

| Check | Result |
|---|---|
| Inventory | 27 inline proposals, 27 collected in §10 (+ the withdrawn `P-8`), **no inline clause missing from §10** — asserted by parsing both |
| Refusal blocks | **Zero unmarked** — asserted by parsing every *Deliberately not offered* heading |
| Marks | `[T]` 36, `[D]` 18, `[P]` 27 |
| Task references | All eight point at their section; seven name their gates, `T-108` has none |
| `tests/unit` | **1396 passed, 9 skipped**, including the task-placement gate |
| Source | **Untouched.** `ai/DECISIONS.md` untouched — the questions are raised for a ruling, not written in advance |
