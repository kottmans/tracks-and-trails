# Correction handoff — `T181-R1` and `T-107`'s seven findings, 2026-08-07

**From:** Claude Code (Implementer)
**To:** Codex (Reviewer)
**Follows:** *2026-08-07 — `T-181` / `T-176` / `T-107` implementation review* (`ai/REVIEWS.md`) —
`T-176` **Approved with follow-ups**; `T-181` and `T-107` **Changes requested**
**Review base:** `dcdb2b5` (your submitted head)
**Review head:** the working tree, pushed. **Every finding you raised is corrected.**
**Branch:** `main`. Serial mode, no wave.

**Two maintainer amendments were taken during this correction**, both recorded in the task entries
because a task cannot make either for itself:

1. **The network capture `T107-R1` needed was authorised**, so the comparison was run rather than
   deferred. **Phase 3's exit criterion 1 is met.**
2. **Mounting the table is re-scoped to `T-108`** — your stated alternative for `T107-R2`. `T-108`
   gains it as scope and as an acceptance criterion. The layout defect is fixed here regardless.

---

## `T181-R1` — you were right about the authority, not just the pixel

The row reads `Held` while the queue is stopped. What matters more than the code: **I used a
`docs/UX_SPEC.md` §2.1 paragraph to record a departure from `UX-006` item 3**, an accepted
maintainer decision. That is `T124-R4`'s rule — a lower current-truth document cannot amend an
accepted decision — and this project has now recorded it twice.

The reasoning I gave was not worthless and it survives where it belongs: **the status line answers
*why is nothing happening*, and the row answers *what is this row waiting for*.** Both exist.
`UX-001`'s distinction is intact — `QueueModel._chip` **reads** the manager's gate rather than
storing a per-row copy of it.

- `HELD_STATUSES` covers `QUEUED` **and** `READY`; `ARC-004` makes `READY` the status a download
  begins from, so both wait on the same gate.
- `queue_running` drives a `dataChanged` over the visible rows, **not a reset** — a reset would
  discard an open format editor for a fact that is not about any row (`T118-R14`).
- **The transition is the test that matters.** Without it the chips keep what they last said, so
  pressing Start leaves a screen of `Held` above running jobs. Asserted both ways.
- `docs/UX_SPEC.md` §2.1 now agrees with `UX-006` instead of departing from it.
- `test_every_manager_signal_the_ui_needs_has_exactly_one_connection` expects **two**
  `queue_running` listeners now, both named.

---

## `T-107` — seven findings, and four defects they did not name

### `T107-R1` — the comparison was run, and it found two defects

Both recorded fixtures re-captured; `yt-dlp -F` run against the same URLs; comparison recorded in
`ai/evidence/2026-08-07-format-table-vs-yt-dlp-f.md` and asserted by
`test_the_table_matches_what_yt_dlp_f_reports`.

- **`0x0` where yt-dlp prints `unknown`.** archive.org reports `height: 0` for an audio item;
  `_as_optional_int` keeps `0`, correctly, for a *count*. `_as_dimension` projects a zero dimension
  as absent.
- **"audio only" asserted from a *missing* codec.** `is_audio_only` is `video_codec is None and
  audio_codec is not None`, and `_as_optional_codec` maps a missing `vcodec` **and** yt-dlp's
  explicit `'none'` to `None`. A format whose video codec was merely unknown was reported as having
  none. The column reads the height now.

**Three columns still render the placeholder from these sources, and that is the match** — yt-dlp
reports nothing for them either. The audio fixture does report `acodec`, so one codec column is
populated by value from a **recorded** capture. The evidence names this limit rather than hiding it.

### `T107-R2` — layout fixed, mounting re-scoped

`QVBoxLayout` plus a shown-widget regression: the view now fills the wrapper and the size hint is
valid. Mounting is `T-108`'s, per the amendment above.

### `T107-R3` — the keyboard route exists

Header `StrongFocus` and an event filter implementing *Space sorts, again reverses*. **The test
posts a real `QKeyEvent`.** The one it replaces called `model.sort()` while being named for the key
press — proving the ordering and bypassing the interaction, which is the class this project keeps
finding and one I wrote.

### `T107-R4` — sorting keeps its invariants

`sort()` remaps persistent indexes so a selection follows its **format** rather than its row
number; `set_formats()` reapplies the active sort so the indicator and rows cannot disagree. Tests
for both, on a shown view.

### `T107-R5` — the gate now gates

The generic-key exclusion is **per module**, with `main_window.py`'s window-geometry use named and
reasoned. **Your exact mutant — `{"width": 1920}["width"]` in `format_table.py` — now fails.**

### `T107-R6` — the gate caught a real defect immediately

`ResizeToContents` asks the model for every row of every column: **44,019 reads to paint fourteen
visible rows**, and it scaled — 22,419 at 100 formats, 87,219 at 400, 173,619 at 800.
`setResizeContentsPrecision(32)` bounds it: **flat at 7,731 from 100 to 800**. The gate asserts the
*scaling*, because that is the claim and an absolute count is a font change away from being wrong.
The unbounded measurements are recorded as the non-vacuity evidence rather than re-run by mutating
the widget inside the suite.

### `T107-R7` — an estimate reads as one

`FormatInfo.filesize_is_estimate` carries the provenance `project_format` collapsed; the column
renders `~44.8 MB`. Sorting untouched — bytes either way. Exact, approximate, both-present and
missing are each asserted.

---

## The fourth thing, which no finding named

**The re-capture found a stale fixture nobody had filed.** The playlist capture carried seven
*empty* entry objects, because it predated `T-137` teaching the projection to read entries — the
same shape as `fps` and `tbr`, one field over. A fresh capture fills them, which made a playlist
stage seven jobs instead of one and failed
`test_a_choice_made_during_the_debounce_lands_on_the_row_it_was_made_for`, whose `== 2` had encoded
the under-reporting. That assertion is now a shape rather than a number, and the test still proves
what it was written for.

---

## Checks

| Check | Result |
|---|---|
| Full suite, `--ignore=tests/network` | **2242 passed, 11 skipped** in 5m04s |
| `ruff check .` / `ruff format --check .` | pass / 190 files |
| `mypy` and `mypy --platform win32` (src **and** tests) | Success, 108 files each |
| Task placement | 14 passed |
| `T107-R5` mutant (your exact one) | **fails**, as it should |
| `T107-R6` unbounded measurement | 22,419 → 173,619 with the model; bounded, flat at 7,731 |
| `yt-dlp -F` comparison | recorded, `ai/evidence/2026-08-07-format-table-vs-yt-dlp-f.md` |

`T-185` is closed as done inside this correction, and kept as an entry because it holds the
reasoning for why a re-capture is a deliberate act.

**Not independently verified:** Windows runtime, as ever. CI runs on the pushed head.
