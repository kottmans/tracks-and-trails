# Python
__pycache__/
*.py[cod]
*.so
.Python
build/
dist/
*.egg-info/
.eggs/

# Environments
.venv/
.venv-prose/
venv/
env/
.python-version

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# Frozen build output (T-020). Produced by packaging/tracks-and-trails.spec.
/dist/
/build/
*.spec.bak

# CI evidence (T-006). Written by the workflow and uploaded as an artifact; also appears
# locally when the CI steps are run by hand.
reports/

# Qt / PySide
*.qmlc
*.jsc
ui_*.py
qrc_*.py

# Packaging
*.spec.bak
Output/

# Editors / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Local overrides
.env
.env.local
*.local.toml

# NEVER COMMIT — user data, credentials, and downloaded media (AGENTS.md §7)
cookies.txt
*.cookies
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm
downloads/
*.mp4
*.mkv
*.webm
*.m4a
*.mp3
*.opus
*.flac
*.part
*.ytdl

# Handoffs are messages to a reviewer, not records (AGENTS.md §6). They may exist locally so the
# reviewer can read one; they are never committed. What is durable lives in docs/project/REVIEWS.md and the
# task entry.
docs/project/handoffs/

# T-319: 174 MB of third-party Windows binary, fetched by packaging/fetch_ffmpeg.py with a
# pinned digest. Reproducible from that script, so it does not belong in the history.
packaging/vendor/
