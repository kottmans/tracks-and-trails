# T-326 / T-332 review

**Purpose:** Independent review of the partial release-candidate evidence and yt-dlp baseline bump.
**Owner:** Reviewer
**Update when:** Candidate evidence, the bump, or its release-gate disposition is reviewed.

## 2026-09-13 — Initial partial-evidence review

**Reviewer:** Codex, independent of the implementer.
**Tasks:** T-326, T-332; remaining T-327 release verification is tracked, not closed.
**Base:** `2e2507b5cfe4e262fcfda9b954dbbf68c390636c`.
**Head:** `8e67f6ff5b5804a8da907c56aeecac8e9fa5a0a7`.
**Platforms verified:** Linux source/tests, retained records and CI evidence.
**Verdict:** Blocked on unfinished candidate gates. These tasks were submitted
as In Progress, not as completed release evidence.

| ID | Severity | Blocks approval | Finding and evidence | Disposition/status |
|---|---|---|---|---|
| T326-R1 | High | Yes — mandatory release check | The submitted enumeration finds no behavioral test for changing settings during a running download without changing that job's request. Frozen dataclasses do not establish the composed mid-flight behavior. The required §7 check remains absent. | **Open — route through T-326's release-gate completion**, with the implementer/maintainer assigning the test work. No automatic new task is created. |
| T326-R2 | Medium | Yes — release-gate instructions | The corrected dated task paragraph says canary item 10a is now owed, but the current T-326 Scope still says it is not, and its linked evidence says 0.1.0 ships 2026.07.04 with no canary owed. That evidence also assigns the entire frozen smoke item 8 to T-324's probe steps, which do not perform managed-download cancellation/normal exit. | **Open — T-326 completion sync.** Correct current routing and append a dated superseding note to retained evidence. Keep item 8's manual remainder and item 10a's actual run/result explicit; do not rewrite captured historical results. |

The pin in pyproject and `BASELINE_YTDLP_VERSION` agrees at 2026.8.19. The local
environment uses that version and its licence text matches the committed text.
The option-audit update records the changed baseline. The network test's removed
second `manager.start()` corrects the duplicate admission: the composed queue
already starts the admitted job.

The recorded full-video comparison is stronger than the initial 10-KB sample;
neither is treated as an independently rerun YouTube transfer here. No further
baseline move is requested.

T-332 still owes the canary run, REL-002 negative collection build and rebuilt
clean-Sandbox 1080p MP4 transfer. The reviewed-code CI run has passing Linux and
both frozen smoke jobs, but Windows integration was cancelled. Its passing
desktop and unit/UI slices do not supply full Windows success. Candidate runs
must cover the final corrected implementation and actual release artifacts.

The first-release migration item may be N/A, but a usable 0.1.0 fixture must be
retained for the next release. RELEASE currently says “from 0.2 onward” while
the task calls for keeping the fixture at the 0.1.0 tag; reconcile that in the
same completion sync. The manual T-327 session and remaining T-319/T-322 checks
are still open. [Shared checks](phase-5-second-batch.md).

## 2026-09-13 — Focused correction review and baseline-bump evidence

**Reviewer:** Codex. **Base/head:** `8e67f6f` → `acdc57071026fac0dfb15743fe24651cb1fcf013`.
**Verdicts:** T-326 remains **Blocked** on the candidate and manual operations.
T-332's baseline-bump scope is **Approved**; that is not candidate approval.

| ID | Severity | Blocks approval | Disposition and independent evidence |
|---|---|---|---|
| T326-R1 | High | No | **Resolved.** The new composed-application test starts a real worker, changes network settings through the screen while it runs, verifies a subsequently built request carries the new proxy and the stored running request stays equal to its original. The child records its handed request independently. The test passes in the serial integration suite. |
| T326-R2 | Medium | No | **Resolved for the original omissions.** Current scope says the canary is owed; the dated evidence has a superseding note; cancellation and normal exit remain an explicit sitting. RELEASE starts retention of historical database fixtures at 0.1.0. The remaining incorrect download routing is recorded below as a completion correction, not an approval of that clause. |
| T326-R3 | Low | No | **Open — T-326 completion sync before candidate evidence.** The new Scope and evidence-table wording credits T-324's probe steps with the real download. Those steps run the five offline probes, not `--download-probe`; cite the clean-machine transfer evidence for that operation. The corrected manual cancellation/normal-exit requirement stays. Preserve dated captured results when updating the routing. |

For T-332, the new retained Sandbox record identifies bundled 2026.08.19 and a
completed 134,886,020-byte 1080p MP4 preset download. CI `34742480032` is green at
`40b1dd8`. Independently read canary `34743729647`: the resolved version is
2026.8.19 and its suite reports **4,350 passed, 42 skipped, 14 deselected**,
23 warnings, 1,102.99 seconds. The canary's expected-stale exclusion remains
explicit; default CI separately runs the pin-dependent tests.

REL-002's dated re-evaluation records the discarded user-managed first attempt,
then isolated bundled-baseline results with 1,751 extractors and 971 extractor
modules in both builds. It retains the explicit list; no new build-policy change
is being approved. This review inspected that record, not a fresh negative build.
T-326 still needs its candidate-specific run IDs and remaining operations.

T-327 items 2–6 and T333-R1 remain unverified by the maintainer. The supplied
T-327 draft stays a draft: its missing observations and the maintainer's own
words cannot be filled in from CI or the automated transfer. No tag was created.
[Shared validation](phase-5-second-batch.md).

## 2026-09-13 — Completion routing verification at ddc8f3d

**Reviewer:** Codex. **Head:** `ddc8f3d18854991f89d5b8656bf1c7b6be65e15f`.
**T326-R3 — Resolved (Low, nonblocking).** Current Scope and the evidence routing
table now attribute the real download to the candidate's clean-machine transfer,
not T-324's offline probes. Cancellation and normal exit remain a separate
sitting. This closes the wording correction, not the candidate-evidence task.

## 2026-09-14 — Clarification of the existing T-332 approval

**Reviewer:** Codex. **Round:** Disposition clarification, not a new initial review.
**Current head:** `03c67454e4f6e9cae9596fb145af7f393ff81a5d`.
**Verdict:** T-332's baseline-bump scope remains **Approved** at its recorded `acdc570` boundary.
The September 13 focused review above already approved it after inspecting the completed
Sandbox transfer, canary and REL-002 negative-build evidence. T-326 remains a separate
candidate-evidence task; its unfinished gates do not revoke the baseline-bump approval.

There is no diff from `acdc570` to this head in `pyproject.toml`, the baseline environment
module, the licence README or the option audit. The current Linux suite also passes at the
pin, as recorded in [the focused session checks](T-337.md#2026-09-14--focused-correction-re-review-at-03c6745).
This clarification does not claim fresh canary, negative-build or Windows executions.

| ID | Severity | Blocks approval | Disposition |
|---|---|---|---|
| T326-R4 | Low | No | **Open — T-332 ordinary completion sync.** Its active status and STATUS still say it awaits review. Reconcile them with the explicit existing approval and perform the normal full-record move when closing it. The index's zero is a count of **migrated entries**, not missing review rounds. No further baseline-bump review or new task is required. |

## 2026-09-14 — T-332 completion sync verified at 66b674c

**Reviewer:** Codex. **Base/head:** `4e10935` → `66b674c2f3b7a6c1d1c940e44560106790d30a23`.
**T326-R4 — Resolved (Low, nonblocking).** T-332's completed record cites the existing
September 13 approval at `acdc570`, its full task body is preserved, and neither TASKS nor
STATUS still presents it as awaiting review. Unit placement and task-reading checks pass.
This is completion-record verification, not a new baseline-bump or candidate approval.
Shared checks are in [the T322-R5 review](T-322.md#2026-09-14--authorized-third-pass-on-t322-r5).

## 2026-09-14 — Candidate evidence review at 00c422c

**Reviewer:** Codex, independent of the implementer.
**Base/head:** `d0cfcf2` → `00c422c`; actual artifact/test candidate `3c011b8`.
**Verdict:** **Changes requested.** The supplied runs and artifact provenance are accepted within
the limits below, but the claim that DRM retries are unreachable from the application is false.
[T328-R3](T-328.md#2026-09-14--010-candidate-and-release-gate-review) records the blocking
counterexample and correction; it is one finding shared with the release gate, not a duplicate.

The [candidate evidence](../evidence/2026-09-14-T326-release-candidate-0.1.0.md) identifies the
actual draft bytes, Linux and Windows default/network results, frozen probes, real clean-machine
transfers, maintainer cancellation/quit answers and bumped-baseline canary. The downloaded logs,
API asset digests and independent local checks agree. The Linux executable-bit failure and
subsequent corrected run are disclosed; only the latter is credited. Network outputs are retained
for both platforms. Item 5 is N/A for the first release, with the v12 fixture retained for the next
release. No network suite or clean VM was newly run by the reviewer.

All 38 named mandatory-test references in the enumeration resolve, and the complete default
suite passes. Passing membership does not establish the missing ordinary-row stale-action DRM
case: the existing stale **group** action check covers a route with its own guard. Correct the
candidate evidence's safety claim and current STATUS deferral alongside T328-R3. A corrected
candidate needs its own applicable gate evidence; these hashes/runs remain historical evidence
for `3c011b8`.

| ID | Severity | Blocks approval | Finding and evidence | Disposition/status |
|---|---|---|---|---|
| T326-R5 | Low | No | `tests/unit/test_persistence.py:857`, `test_a_retry_uses_the_stored_request_not_the_current_defaults`, constructs unused alternate defaults and only rereads a stored job. Its name/docstring claim a retry/settings-freeze exercise that never occurs. | **Open — T-326 ordinary completion correction.** Rename/document the actual persistence property and remove the misleading unused control, or strengthen the test to exercise the claimed behavior. No standalone task or additional release blocker is needed. |

**Settings-freeze ruling:** one effective proving test is sufficient. The composed test at
`tests/integration/test_composition.py:4091` starts an actual spawned worker, changes settings
through the screen, positively verifies a new request uses the new settings, and checks both the
running stored request and the request independently recorded by the child. It passes in the
full local and candidate CI suites. T326-R1's earlier mutation-backed resolution stands; that
mutation was not freshly rerun here. The weak unit test does not invalidate the stronger test.

The update probe exercises frozen production installation/extraction/resolution/reversion with
synthetic wheel/index/transport data. It supplies the already-established offline artifact gate;
it is not evidence of a live upstream wheel download or a human clicking Update. First published
application-update discovery is the separate post-publication T-328 criterion. Item 6's missing
manual observations belong to T328-R4; no machine result is promoted into those observations.
Shared checks, all release-gate dispositions and remaining work are in the T-328 review.

## 2026-09-14 — Refreshed evidence for candidate 93c7357

**Reviewer:** Codex, independent of the implementer.
**Base/head:** `2fb9c16` → `4f7c7a5`; candidate `93c7357d22fcdf9066e28161c3d6380896caa9c3`.
**Verdict:** **Changes requested.** Actual machine results and asset provenance agree, but the
mandatory DRM requirement still fails through two sibling retry paths. The prior stale queue-menu
case is corrected; the claim that the DRM weakness is wholly corrected is not established.
[T328-R3](T-328.md#2026-09-14--focused-release-review-of-candidate-93c7357) records both
independent counterexamples and the remaining correction.

| ID | Severity | Blocks approval | Disposition and independent evidence |
|---|---|---|---|
| T326-R5 | Low | No | **Resolved.** The persistence test is renamed `test_a_stored_request_reads_back_exactly_as_it_was_queued`; the unused changed-defaults object is removed, the same request equality is asserted, and the docstring correctly delegates settings-freeze proof to the composed test. |
| T326-R6 | Low | No | **Open — T-326 ordinary completion sync.** The new heading says everything below was taken on `93c7357`, while the cancellation/quit row correctly says `3c011b8`. Qualify the heading and carry the maintainer's newly supplied host, **spock**, into that earlier-candidate attribution. Keep its results separate from the outstanding current-candidate sitting. |

Independently inspected candidate CI `34905128347`, release `34908379562` and canary
`34908424424`: each resolves to the exact candidate and succeeds. Linux orphans is skipped under
existing policy. Retained network outputs pass on both platforms. The Ubuntu and added Fedora
clean-machine reports, Windows Sandbox report, and stated Fedora host transfer all identify the
new draft. Both downloaded artifact hashes and the checksums file match the authenticated GitHub
asset metadata; the actual draft body matches the corrected changelog section. All four artifact
gates pass on both payloads in the build log and independently on the extracted downloaded AppImage.

The enumeration's original 38 named tests remain present; new manager/queue regressions cover the
original finding. They do not cover a pre-existing automatic deadline outliving a manual retry,
or the Add-dialog retry actions discarding a staged DRM failure and creating another probe.
Correct the current evidence's overall DRM disposition with T328-R3. Preserve this candidate's
runs as evidence at their actual revision when recording any replacement candidate.

**Item 8 is incomplete for `93c7357`:** probes and real downloads pass, but the maintainer's
cancel/companion-completion/File → Quit answer belongs to `3c011b8`. In this review the maintainer
answered **“spock”** to the Linux-host question. Codex did not perform that earlier sitting, and
this answer identifies its host without establishing the exact Linux version, TLS environment,
or a new run. The current acceptance sitting must include a companion transfer completing,
cancellation, normal quit and no remaining processes; the sheet's cancellation step alone does
not record all of those operations.

Shared exact counts, limitations and release-gate dispositions are in the T-328 review. No
network suite, clean VM, manual desktop sitting or Fedora host download was newly performed by
Codex; their supplied execution provenance remains explicit.

## 2026-09-15 — Evidence attribution correction

**Reviewer:** Codex, independent of the implementer. **Round:** Focused correction.
**Base:** `9065d78`. **Correction:** `8d70e01`. **Verified through:** `ea49516`.
**Verdict:** **T326-R6 Resolved.** Replacement-candidate release evidence remains pending.

The evidence heading now assigns the machine checks to `93c7357` and the human sitting
to `3c011b8`. The sitting identifies the maintainer's Linux host as **spock** without
inventing its distribution or trust store, and explicitly leaves cancellation,
companion completion and normal quit owed on the approved candidate. Item 4 now records
the DRM failure at `93c7357` and distinguishes its two subsequently corrected paths.

The current source correction resolves T328-R3 as recorded in the
[September 15 review](T-328.md#2026-09-15--four-handoffs-reviewed-through-ea49516).
That finding's resolution does not retroactively make `93c7357` safe or establish a
new artifact run. The committed evidence still identifies that older candidate;
the user reports that replacement evidence and hashes will follow. No new release
run, network suite, clean machine or Sandbox evidence was inspected in this pass.

## 2026-09-16 — Published artifact attribution

**Reviewer:** Codex. **Scope:** Publication disposition and metadata verification
alongside the [public-page review](T-328.md#2026-09-16--published-readme-and-release-notes)
at `1aa85e9`. This is not a fresh execution of the candidate gates.

The replacement evidence committed in `f37ccf7` identifies
`5b1bf97e63cb6f6b3ede2f8e75d0feccda3404a3`, CI `35021004959`, release build
`35021277805` and canary `35021322867`. Its summary records successful machine
checks and still correctly attributes the sole human cancellation sitting to
`3c011b8`. Those run results are retained implementer evidence in this pass;
their remote job logs and transfers were not independently rerun or re-audited.

Independently read the live GitHub release API: release `389487493` is published,
not a draft or prerelease, and carries the named three assets. Its installer size
**93,482,037** and digest
`12dab2d3ae168bab9539ffb75fb886ef406f47fe38711df68004c727fc4111f7`, and AppImage
size **68,311,544** and digest
`37a3a7b0c4c7aef82e80edb13404fbe0a95ebc2d8535e321169c370224656992`, agree with the
committed candidate table. `SHA256SUMS` is present, **206 bytes**. These are API
metadata comparisons, not newly computed hashes of downloaded payloads.

**Disposition:** Published by the maintainer's explicit September 16 waiver;
unperformed human acceptance remains unperformed. T328-R4 is still Open, High,
waived for this publication. Earlier source-finding resolutions stand at their
recorded revisions. This closes the stale “replacement evidence pending” snapshot
without claiming an independently approved release-gate completion. The page
corrections and outstanding first published-update check belong to T-328.
