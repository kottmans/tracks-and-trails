# T-230 — Review record

**Purpose:** Dated review evidence and disposition history for this task or shared scope.
**Owner:** Assigned Reviewer · **Update when:** This scope is reviewed or rechecked.
**Covered tasks:** T-220, T-229, T-230, T-239

[Review index](../REVIEWS.md) · [Review policy](../TESTING.md#14-review-policy)

Moved from `85422bc0b086de9b18d2f809abb4d6bcebb180e4:docs/project/REVIEWS.md` on 2026-09-08.
The entries below retain their exact original bytes and relative order. Historical
path spellings, line citations and references to “above” describe that source;
the [migration manifest](../evidence/2026-09-08-review-migration.json) records the
original order and byte ranges. Shared entries are stored once; covered tasks
link to this same record. Navigation grants no approval or new review provenance.

## Recorded rounds

- [2026-08-12 — T-230, T-220, T-229, and T-239 unattended-run review](#migrated-review-0243)

<a id="migrated-review-0243"></a>
<!-- review-migration:0243:start -->

## 2026-08-12 — T-230, T-220, T-229, and T-239 unattended-run review

**Reviewer:** Codex (Reviewer)
**Task(s):** `T-230`, `T-220`, `T-229`, `T-239`, and the disclosed format-check incident
**Review boundary:** `c63eec4` (T-230), `febc82b` (T-220 reading), `93a6f95` (T-229), and
`5ed1b26..4dc358f` (T-239 filing, measurement, and correction). `5fbe74f` corrects the Markdown
formatting; later commits through current head `bf6081d` are records only and were read for current
truth.
**Platforms verified:** Linux focused and integration verification; GitHub Actions
workflow-dispatch run `31657727760` at `fba1ee5`, successful on all five jobs, including the full
Windows suite.
**Verdict:** **T-230 Approved; T-220 Approved; T-229 Approved; T-239 Approved.** One Low,
non-blocking STATUS wording correction remains (`T239-R1`).

### Task dispositions

| Task | Verdict | Reason |
|---|---|---|
| **T-230** | **Approved** | `redirect()` exports the literal POSIX and Windows platformdirs variable families from the same per-test root it uses for in-process patching, restores the previous environment, and records the explicit-`env`, Windows-layout, and downloads-directory limits. The child test crosses a real subprocess boundary, and the mapping guard does not derive its required keys from itself. Independent full integration measurement: **404 passed, 0 files** under sentinel `XDG_*` roots. |
| **T-220** | **Approved** | The records-only reading is correct. Direct source/spec inspection gives `+ Add URLs`, spacer, `Start`/`Stop`, `Clear finished`; `docs/UX_SPEC.md` §2.1 requires those three verbs and run states. The exhaustive toolbar test accounts for every action and the spacer, while Windows UI Automation observes both run states. No amendment is needed. |
| **T-229** | **Approved** | The ordered subprocess pair exercises restoration between tests and fails by field if palette or `_applied` restoration is removed. The two product-state tests apply a shipped theme; the panel checks the painted stylesheet surface rather than the bare-widget flag, and the keyboard test computes and validates its off-row probe under themed metrics. The focused slice and exact-head Windows full suite pass. |
| **T-239** | **Approved** | The failed test synchronized on file existence between `partial.replace()` and `_note_publication()`, while the product gate reads `cache_generation`. Waiting for that generation to advance aligns the test with the product's publication point; the following file assertion preserves ordering evidence. The timeout is unchanged, and the submitted membership-only mutation still kills the original `T179-R1` regression. |

### Finding

| ID | Severity | Blocks approval | Finding | Required correction |
|---|---:|---:|---|---|
| **T239-R1** | **Low** | **No** | `ai/STATUS.md` says format runs before everything and that run `31657367781` produced no Windows evidence. The run shows **Types under the Windows platform** and the dedicated **Windows desktop suite** both succeeded before `ruff format --check .` failed. What was skipped was the Qt baseline and full Windows suite, so the T-239 regression itself received no Windows execution. | Narrow the STATUS statement to the evidence actually lost. The consequence remains material: T-239 lacked full Linux/Windows test execution until dispatched run `31657727760`. |

### The format incident

The incident does not block T-239 because its validation debt was paid explicitly. Run
`31657367781` at `4dc358f` failed formatting on Linux and Windows. Linux skipped both type steps,
the Qt baseline, and tests; Windows had already passed Win32 types and its dedicated desktop slice,
then skipped the Qt baseline and full suite. Both frozen jobs passed. The records-only formatting
commit `5fbe74f` triggered only the task-placement workflow, so workflow-dispatch run `31657727760`
was necessary and is the first full five-job run of the corrected T-239 test.

The underlying process miss is also confirmed: the implementer ran `ruff format --check src tests`
while repository policy and CI require `ruff format --check .`. The current tree passes the latter.
No separate task is needed because the command is already canonical in `ai/TESTING.md` §4, the
failure and its cost are recorded, and the exact required command now passes.

### Independent verification

| Check | Result |
|---|---|
| Repository state before reviewer records | **Clean tracked tree; `HEAD == origin/main == bf6081d`.** The handoff is ignored/untracked as required. |
| Boundary whitespace | **Passed** independently for `c63eec4`, `93a6f95`, and `4dc358f`. |
| Focused review slice | **10 passed:** T-230 directory guards, T-229 ordered restoration and both dressed behaviors, T-239 publication synchronization, and T-220's exhaustive toolbar test. |
| T-230 full measurement | **404 integration tests passed; 0 files** left under fresh sentinel `XDG_CACHE_HOME`, `XDG_CONFIG_HOME`, and `XDG_DATA_HOME` roots. The first sandboxed attempt was discarded after localhost socket creation was denied; the valid run used the suite's required local-socket capability. |
| Repository static gates | **Passed:** `ruff check .` and `ruff format --check .` (`169 files already formatted`). |
| T-239 original evidence | Run `31655610375`, attempt 1, failed only the named queue-view regression on Linux at `93a6f95`; its rerun at the same SHA passed. |
| Format-failure evidence | Run `31657367781` failed `Format check` on Linux and Windows at `4dc358f`. The later steps skipped are exactly as recorded above. |
| Final CI | Run `31657727760` succeeded on all five jobs at `fba1ee5`. Its Linux and Windows logs directly show the new T-230, T-229, and T-239 tests passing; Windows finished **3117 passed, 29 skipped, 34 deselected**, Linux **2726 passed, 18 skipped**. |

### Records synchronization

T-230, T-220, T-229, and T-239 move to Complete. During disposition, their current-truth task
records were also trued: T-220 no longer says its label half or final reading is still open;
T-239 names the test as its affected surface and no longer recasts T-238's local xdist crash as a
runner-red/local-green event.

`ai/STATUS.md` remains Implementer-owned and was not edited. It needs to move all four tasks to
Complete and apply `T239-R1`'s narrower account of what the format failure prevented. The Reviewer
changed only `ai/REVIEWS.md` and approved task/follow-up records in `ai/TASKS.md`; no reviewed
source, test, workflow, dependency, commit, push, roadmap, or other remote state was changed.

<!-- review-migration:0243:end -->
