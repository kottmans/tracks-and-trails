# T-329 review

**Purpose:** Independent review of the header focus-floor correction.
**Owner:** Reviewer
**Update when:** T-329 is reviewed or a finding receives a disposition.

## 2026-09-11 — Initial review with Phase 4 exit evidence

**Reviewer:** Codex. **Assessed head:**
`59598de4fd7af2c9fe921bb99002761d7f01c766`; the gate change landed in `88fc6b1`.
The current task record at `0761605` explicitly requests a mutation on both
platforms. Its later documentation does not change the assessed implementation.

**Verdict: Blocked pending the Windows mutation result.** The normal Windows job
is verified green, and the correction works for the current audit inventory.
No inaccessible product control or changed global threshold was found. The two
findings below distinguish required evidence from a nonblocking helper correction.

| ID | Severity | Blocks approval | Finding and disposition |
|---|---|---|---|
| T329-R1 | Low | No | `_focused_section` reads `header.currentIndex()`, but SortableHeader paints its own `current_section()`. They coincide at zero in the current audit. After sorting column 1, the painted section is 64–77 pixels wide while the helper still reports 84. **Open — coupled correction in T-329's completion:** use the actual painted-section state and cover a nonzero section; clarify that SortableHeader draws the extra ring itself. This does not invalidate the observed default-state pass or require a separate task. |
| T329-R2 | Medium | Yes — explicit acceptance check | The task requires the no-geometry-focus mutation to fail **on both platforms**. The supplied CI artifacts contain the successful normal tests; no Windows mutation command/result was supplied or located. A green unmodified suite does not establish this negative control. **Open — provide the existing Windows mutation record, or execute the focused mutation with a normal positive control when the desktop is available.** The result must identify the implementation, platform and palette outcomes. This is missing evidence, not a claimed Windows failure. |

### Measurements

The reviewed tree was extracted with `git archive 59598de` into a temporary
directory; the working checkout and its later Phase 5 product changes were not
used as the test subject. Linux uses Python 3.14.7 / PySide6 6.11.1, offscreen.

- **133 passed in 17.09 s** over the contract, accessibility and colour suites.
- The unmodified two-palette full-surface focus sweep passes: **2 passed in 3.42 s**.
  For the six SortableHeaders, the actual painted section and current model index
  are both zero. Section geometry is **84×30**, changed pixels **430**, and the
  new floor **134.4**. Overall header widths range from 198 to 312 pixels; the
  new floor stays fixed. MINIMUM_FOCUS_RING_SHARE remains **0.6**, and the
  non-header perimeter calculation is unchanged.
- Replacing **SortableHeader.paintSection with QHeaderView.paintSection** in a
  temporary pytest plugin removes the application's extra painted ring. Both
  palette cases fail on all six headers: **0 changed pixels against 134.4**,
  **2 expected failures in 2.00 s**. This is an actual rendered-control mutation,
  not a replacement of the numeric measurement. It was executed on Linux only.
- A separate probe sets sortIndicatorSection to 1 before the existing focus
  measurement. SortableHeader then paints section 1, but currentIndex.column()
  stays zero. Actual section widths are **64, 65 or 77**, while `_focused_section`
  still returns **(84, 30)**. The two palette sweeps still pass, so R1 is Low:
  the current product drawing is correct and the default audited state agrees;
  the helper's claimed relation to the painted section needs correction.

### Windows evidence and its limit

[Run 34653977245](https://github.com/kottmans/tracks-and-trails/actions/runs/34653977245)
at the assessed head has all required jobs green. Its full Windows report is
**4,152 passed / 36 skipped**, and both full-surface focus palette cases pass
(3.743 s and 3.736 s in JUnit). That suite runs **offscreen on Windows**, as the
workflow deliberately specifies; the separate 37-case desktop suite uses the
native Windows plugin. The environment artifact records PySide6/Qt **6.11.2**.
Linux and Windows CI lint, formatting and the applicable mypy gates pass.

These artifacts establish the positive Windows case. They do not contain a
mutated run, and no such result is inferred from it. The implementer was asked
for the Windows mutation's command/output or log path during this review. At the
time of the record it remains unverified. STARBASE is online but busy on another
CI run; no competing measurement or runner change was started.

The remaining evidence can be supplied without changing code. If R1 is corrected
first, identify the new test tree in the subsequent verification. The ordinary
focused correction pass for this task remains available under TESTING §14.
Task/status ownership remains with the Implementer; this review does not close
or move the task record.

## 2026-09-12 — Focused correction re-review (UTC)

**Reviewer:** Codex, independent of the implementer.
**Task:** T-329. **Round:** Focused re-review.
**Base:** `59598de4fd7af2c9fe921bb99002761d7f01c766`.
**Head:** `2e2507b5cfe4e262fcfda9b954dbbf68c390636c`.
**Platforms verified:** Linux, Python 3.14.7 / PySide6 6.11.1, offscreen.
**Verdict:** Blocked pending inspection of the reported Windows mutation evidence.

| ID | Severity | Blocks approval | Recheck and disposition |
|---|---|---|---|
| T329-R1 | Low | No | **Resolved.** `_focused_section` reads `current_section()` when supplied by the painted header; the new inventory-driven test changes to a differently sized section. Restoring the old currentIndex helper in a temporary pytest plugin produces **1 failed / 95 passed**, failing only the new regression. The normal colour suite passes. |
| T329-R2 | Medium | Yes — explicit acceptance check | **Open; correction reported, direct evidence not yet verified.** TASKS records a clean a4db6f8 STARBASE baseline (**2 passed / 3.36 s**) and mutation (**2 failed / 3.80 s**), both palettes, six headers, zero changed pixels against a floor of 121. That supplies a claimed tree and outcome, but the original stdout/report was not supplied or located in this environment. Its location was requested during review. Inspect the retained `reports/windows-mutations.txt` or equivalent baseline/mutation output before resolution; a new run is unnecessary if the original evidence is available. |

### Independent checks

- Shared unmodified selection: **133 passed in 11.94 s** across the colour,
  artifact-gate, version-checker, AppDir metadata and skeleton suites.
- The committed `mut_header_no_extra_ring` plugin, selected only against
  `test_focus_is_visible_on_every_control_the_application_shows`, produces
  **2 failed in 1.97 s** on Linux. Both palettes report the six headers changing
  zero pixels against their section-derived floor. This is the rendered mutation,
  not a substituted numeric threshold.
- The restored-currentIndex helper produces **1 failed / 95 passed in 9.15 s**.
  MINIMUM_FOCUS_RING_SHARE remains **0.6**, and the correction does not change
  non-header perimeter measurements.
- Ruff lint and formatting passed; bare mypy and its Windows-platform run each
  passed over **171 source files**, before unrelated work appeared locally.
- The focus helper and header-removal plugin are unchanged between the claimed
  Windows measurement head a4db6f8 and the submitted head. The driver has a later
  summary-regex correction, which is not evidence that the earlier Windows
  assertions executed.

### Environment evidence and scope

The bundle-backed remote, stale source and stale venv explain why the three
earlier tables cannot establish the corrected behavior. They are explicitly
withdrawn evidence, not passing or failing measurements of this implementation.
The new checkout checks and recorded dirty-tree handling improve provenance;
they cannot retroactively validate a stale run. A result table must be read with
its tree identity, baseline, actual assertions and platform configuration.

STARBASE_HOST is not configured here, and no retained interactive-session report
was present in the supplied checkout. This review did not launch a competing
Windows session or treat source-level Linux checks as Windows results. The
previous independently checked CI positive case remains preserved above;
the latest CI run's Windows jobs are queued and cannot provide the missing
negative control.

T-331's broader driver validation and T-330's job-duration work are not submitted
for approval in this pass. The remaining request is retrieval of the claimed
Windows evidence, not another broad code audit. Phase 4 sign-off and task/status
closure are not granted by this record.
