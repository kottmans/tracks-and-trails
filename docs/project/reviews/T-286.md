# T-286 — Explain when recoding helps

**Purpose:** Independent review of the container note and its regression guard.
**Owner:** Reviewer
**Update when:** The task is reviewed or a finding receives a disposition.

## 2026-09-11 — Initial review

**Reviewer:** Codex, independent of Claude's implementation.
**Boundary:** `fc8a96d..e47ebce`.
**Combined state checked:** `1c4bc0c89b8753287a3998e3aec18dcb2152ebe9`.
**Verdict:** Approved. No blocking finding. One Low test-strength item belongs
in the ordinary completion sync, without another task or review pass.

The note retains the existing cost comparison, names a player refusing the
source as the bounded use case, and explains that a wrapper change keeps
the contents. It introduces no codec controls, changes no conversion
behavior, and makes no promise that a remux fixes codec compatibility.
The named label and existing dialog sizing/word-wrap checks pass in the UI
suite. The historical ffmpeg measurements were read as the task's evidence;
this review did not rerun or claim those timings.

| ID | Severity | Blocks approval | Evidence and requested correction | Status / route |
|---|---|---|---|---|
| T286-R1 | Low | No | `test_the_container_note_says_what_recoding_is_for` says it checks the use case separately, but never asserts the player/refusal case. Replacing `a player refuses what the site sent` with `needed` still passes, leaving “Recode only when needed” without the task's explanation. The constant-to-label equality cannot detect this because both receive the changed constant. | Open — add a bounded assertion for the actual use case, and keep the test description aligned with what is checked. Handle in T-286's normal completion sync; no separate task or further behavioral review solely for this item. |

Isolated controls gave **1 failed** when restoring the old cost-only note,
and **1 passed** when removing only the case as described in R1. This is a
guard gap in a correct current sentence, not a product failure.

The [shared verification](T-314.md#second-correction-verification) covers
the dialog tests and static gates at the checked head. Only this review
record and its index link were added. Task/status closure stays with its owner.

## 2026-09-11 — Correction evidence

**Reviewer:** Codex, independent of Claude's correction.
**Boundary:** `a59a4b7..c256103`.
**Implementation head:** `c2561037d45c2e1b005db4ad18ebe470fc86e6b6`.
**Verdict:** Approved; the existing approval stands. T286-R1 is Resolved.
This records the Low correction supplied with T-290's focused verification;
it does not reopen the task's behavioral review.

| ID | Severity | Blocks approval | Disposition and evidence |
|---|---|---|---|
| T286-R1 | Low | No — resolved | The regression now independently requires `player` and `refuses` in the drawn note, and describes that assertion accurately. Replacing `a player refuses what the site sent` with `needed` in an isolated process now produces **1 failed**, at the new case assertion. The constant-to-label equality still passes under that mutation, so the added assertion is what catches the removed explanation. |

The unmodified Options dialog module passes in the **232 passed, 1 skipped**
[focused verification](T-290.md#2026-09-11--focused-correction-verification),
which also records static gates and invocation limitations at this head.
The current product sentence is unchanged by this correction. No ffmpeg
measurement or native desktop result is newly claimed. Only this appended
review record is retained; earlier review bytes and index navigation are
preserved. Task/status closure stays with its owner.
