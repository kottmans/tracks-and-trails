# T-316 — State chips in the staging list

**Purpose:** Independent review of staged-row state presentation.
**Owner:** Reviewer
**Update when:** The task is reviewed or a finding receives a disposition.

## 2026-09-11 — Initial review

**Reviewer:** Codex, independent of Claude's implementation.
**Boundary:** `34d7e0d..285f3b6`.
**Combined state checked:** `1c4bc0c89b8753287a3998e3aec18dcb2152ebe9`.
**Verdict:** Approved. No blocking or non-blocking finding established.

StagingModel now supplies STATE_CHIP_ROLE from the same exhaustive state
mapping its existing state text uses. RowDelegate already paints that role
as a bordered, named chip on the headline line. The new role therefore
exposes `Read` versus `Couldn't read` in the shared state position without
changing the selection indicator or adding a colour-only channel.

The existing detail-line comparison removes state only when the chip says
the same thing. Failure details remain, and a duplicate's additional clause
is retained because its full state text differs from the bare chip. The
staging accessible text continues to include state through its existing
STATE_ROLE path. No second state vocabulary or new painting rule was added.

The recorded 2026-09-11 maintainer ruling accepts shipping the chip while
leaving a possible tint open. Approval is for that scope. It is neither an
approval of a future red palette nor a claim that colour was forbidden.

The new ready/failed regression passes. Restoring StagingModel.data from
`8472795`, before the chip answer, produces **1 failed** on the ordinary-row
positive control: None instead of `Read`. The shared delegate's rendering,
state-text and selection checks pass in the full UI suite.

See [shared batch verification](T-314.md#second-correction-verification)
for the independent checks and offscreen/Windows limits. Only the review
record and index link were added; task/status closure remains with its owner.
