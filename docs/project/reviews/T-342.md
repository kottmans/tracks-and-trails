# T-342 review

**Purpose:** Independent review of dialog placement and size bounds.
**Owner:** Reviewer
**Update when:** The implementation or the required Windows observation changes.

## 2026-09-15 — Dialog bounds implementation review

**Reviewer:** Codex, independent of the implementer.
**Round:** Initial. **Base:** `8d70e01`. **Implementation head:** `a988847`.
**Verification checkout:** `ea495166b59d3d76bd52524da7473df4af7bc513`.
**Scope:** T-342; the accompanying T-343 task filing at `682f71d`.
**Platforms verified:** Linux, offscreen Qt. No new Windows execution.
**Verdict:** **Blocked on the required real Windows display at 125%.** No blocking
implementation defect established in this pass.

### Findings

| ID | Severity | Blocks approval | Finding and evidence | Disposition/status |
|---|---|---|---|---|
| T342-N1 | Note | No | Frame-aware shrinking, top/left clamping, the bounded Add URLs hint and its explicit initial resize are consistent with the measured defect. The filter is owned by the main window; queued callbacks have a dialog context. | No implementation change requested. |
| T342-N2 | Note | No | Fitting makes the maximum size smaller and does not restore the previous maximum on a larger monitor. The four-pass limit also bounds all subsequent fitting in that showing. Movement alone is deliberately not watched. | Implementation limits recorded; this review does not promise unrestricted resizing after a monitor move. |

### Checks and limits

Inspected the complete change, geometry and lifetime tests, composition installation,
and the task's Windows measurements. The independent full Linux suite at `ea49516`
passed **4,721 tests, 22 skipped**; Ruff and both mypy platforms passed. Shared execution
details are in [T-328](T-328.md).

The retained Windows measurements distinguish native 100% from `QT_SCALE_FACTOR`
experiments; the latter still reported geometry refusals and cannot establish the
real 125% criterion. A person must check Add URLs and Preferences with the main window
normal and maximized on a Windows display actually set to 125%, using the draft build.

T-343 is a documentation-only filing for the taskbar-close behavior the maintainer
explicitly deferred. This review grants no implementation approval for that unbuilt task.
The task/status owner should route T-342 to the outstanding observation rather than
describe offscreen tests as its completion. Release acceptance remains [T328-R4](T-328.md).
