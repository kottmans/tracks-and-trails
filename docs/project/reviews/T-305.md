# T-305 — Absent and unknown format fields

**Purpose:** Canonical review rounds and dispositions for T-305.
**Owner:** Reviewer
**Update when:** This task is reviewed or a finding receives a disposition.

## 2026-09-09 — Initial review

**Reviewer:** Codex, independent of Claude's implementation.
**Task:** T-305
**Round:** Initial
**Base:** `2595314da36318dee81d7628737edc6abfbe406b`
**Head:** `2181ee13b559dd582a0df6ef662524310200a638`
**Platforms verified:** Linux; Python 3.14.7, PySide6/Qt 6.11.1, Qt offscreen plugin.
**Verdict:** Approved

### Findings

| ID | Severity | Blocks approval | Finding and evidence | Disposition/status |
|---|---|---|---|---|
| T305-R1 | Low | No | The current missing-field rule in `docs/UX_SPEC.md` §4, “What it shows” (lines 290–291 at this head), still prescribes `UNKNOWN_TEXT` universally. The corresponding opening rule in `ui/format_table.py` also predates this distinction. Both now contradict the intended `None` cells. Describe absent streams and absent notes separately from unknown values. | Open — Documentation Maintainer/Implementer to synchronize current prose during T-305's completion update; no new task or review pass required solely for this cleanup. Preserve captured historical evidence. |

### Behavior and evidence

Reviewed `ceae45a`'s implementation and test changes, including the existing
assertions it changed, against REQ-003, the task's tri-state criteria, the adapter
and `FormatInfo`. The explicit false stream flag takes precedence; an unknown
codec remains `Unknown`; a missing free-text note becomes `None`. Size handling
and numeric sort keys retain their behavior. No runtime blocker found in T-305.
The later codec-label and tooltip defects are assessed under T-306,
not attributed to this absence correction.

For the task's by-eye comparison against a real probe, independently rendered the
current `FormatTable` at **1300 × 600**, in both application themes, from the
existing **recorded** captures `dash_akamai_big_buck_bunny.json` (2026-08-08) and
`wikimedia_caminandes.json` (2026-08-07), under `tests/fixtures/infodicts/`.
Passed each capture's `info_dict` through `project_media` and compared the visible
cells against its raw format rows by ID:

- All **11 DASH rows** were visible: ten video-only rows displayed audio `None`,
  the audio-only row displayed video `None`, and all unknown sizes stayed `Unknown`.
- All **five Wikimedia rows** were visible: missing notes displayed `None`; the
  source row's unspecified codecs stayed `Unknown`; reported size estimates kept
  their estimate marker.

This is a visual check of the current widget against recorded real probe output.
It is not a fresh network probe, a site-extractor claim about the generic DASH
capture, or a desktop session. The synthetic fixture separately covers missing
and fractional fields; its success is not represented as recorded-source evidence.

### Checks and readiness

The [shared batch checks](T-303.md#checks-run) at this exact head passed, including
all **48 format-table tests** in the 169-test targeted run, the **1159-pass UI
suite**, Ruff, and both source/test type-check runs. Those records preserve the
three UI skips, seventeen warnings and pending CI conclusion.

T-305 is approved at the stated implementation head. Route T305-R1 through the
existing completion update and transfer the task under AGENTS §6; the Low finding
does not keep it In Review. No source, tests or task/status state was changed by
this review. T-306 retains its own approval boundary.

## 2026-09-09 — T305-R1 disposition update

**Reviewer:** Codex, independent of the correction.
**Task:** T-305
**Round:** Disposition update; no reopening of the approved implementation.
**Correction reviewed:** `1e31d91`, present at
`b76595d8130da61125dcb9565318e2321565d9b3`.

**T305-R1 — Resolved. Severity: Low; Blocks approval: No.** The current
missing-field rule in UX_SPEC §4 now names the absent-stream/absent-note
exception, and the opening rule in `ui/format_table.py` distinguishes
`ABSENT_TEXT` from `UNKNOWN_TEXT`. Inspected those exact prose hunks and their
consistency with the existing behavior. No historical capture was rewritten by
these two prose changes.

T-305's approval at `2181ee1` stands. This entry closes its documentation finding;
it does not approve T-306 or transfer task/status ownership. The task/status
owner still performs the ordinary completion update under AGENTS §6.
