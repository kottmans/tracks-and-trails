# T-345 review

**Purpose:** Independent review of startup retry presentation and progress-chip wording.
**Owner:** Reviewer
**Update when:** These changes or their acceptance evidence change.

## 2026-09-15 — Startup offer and chip wording review

**Reviewer:** Codex, independent of the implementer.
**Round:** Initial. **Base:** `55dcf05`. **Implementation head:** `81d7bdd`.
**Verification checkout:** `ea495166b59d3d76bd52524da7473df4af7bc513`.
**Platforms verified:** Linux, offscreen Qt; no new Windows runtime or desktop observation.
**Verdict:** **Approved for the implementation.** Release acceptance remains T328-R4.

### Findings

| ID | Severity | Blocks approval | Finding and evidence | Disposition/status |
|---|---|---|---|---|
| T345-N1 | Note | No | `compose()` carries recovered IDs without opening the retry offer; `present()` shows the window before offering them, and the phase-exit startup script follows the same order. The regression observes visibility at the actual offer call through `present()`. | No change requested. An offscreen visibility check is not a compositor-stacking observation. |
| T345-N2 | Note | No | The amended UX-005 vocabulary identifies the operation beside its percentage, with matching step/stage fallbacks. The longest requested example fits at ordinary narrow widths in the independent Linux probe. | No change requested; Windows fonts and display scaling still belong to draft acceptance. |

### Checks and limits

At the probe's default Linux font, `Adding thumbnail 42%` occupies **138 px**. In the
actual queue delegate, main-window widths of **960 / 600 / 420 px** leave approximately
**691 / 331 / 151 px** for the title. This measures the delegate's geometry, not a human
judgment of a Windows screenshot. At an extreme 99-pixel window the existing layout
has no text space; the chip change does not establish that as a supported useful width.
Local capture: `/tmp/tt-review-ea49516-ui-probe.log`.

The full independent Linux suite passed **4,721 tests, 22 skipped**, including composition
and chip regressions; Ruff and both mypy platforms passed. See [T-328](T-328.md) for commands.

The maintainer's reported “everything looks good” belongs to the source trial at
`ea49516`. It supports the reported correction but is not a two-platform walk of files
downloaded from the draft. Checks C and D still need that candidate-specific evidence.
The task/status owner may record this implementation approval without closing the
release gate or turning the source trial into artifact acceptance.
