# T-323 review

**Purpose:** Independent review of the four executable artifact gates.
**Owner:** Reviewer
**Update when:** T-323 is reviewed or a finding receives a disposition.

## 2026-09-12 — Initial review (UTC)

**Reviewer:** Codex, independent of the implementer.
**Task:** T-323. **Round:** Initial.
**Base:** `59598de4fd7af2c9fe921bb99002761d7f01c766`.
**Head:** `2e2507b5cfe4e262fcfda9b954dbbf68c390636c`.
**Platforms verified:** Fedora, Python 3.14.7, PySide6 6.11.1,
PyInstaller 6.21.0; retained Linux CI reports. No Windows execution here.
**Verdict:** Changes requested.

### Findings

| ID | Severity | Blocks approval | Finding and evidence | Disposition/status |
|---|---|---|---|---|
| T323-R1 | Medium | Yes — the linkage gate accepts its failed property | `artifact_gates.py:108` counts dangling symlinks as present, and line 144 inspects only the first four sorted bindings. In a real build of this head, QtWidgets is fifth. Deleting `_internal/PySide6/Qt/lib/libQt6Widgets.so.6` leaves no usable bundled Widgets library, yet **all four gates pass, exit 0**. Its second path is a symlink, not an independent copy. Direct `ldd` on the omitted binding resolves Widgets to `/lib64/libQt6Widgets.so.6` and reports incompatible private Qt symbols. Additionally, missing bindings/loader, loader errors, and bindings with no Qt dependency all return success; the Windows branch establishes presence alone despite the accepted scope requiring dependency verification. | **Open.** Inspect the required bindings/dependencies without an arbitrary four-file limit; require usable in-artifact library targets and positive linkage evidence; reject inspection failures instead of returning success. Cover dangling links, absent dynamic dependencies and the supported Windows check. Revise the recorded duplicate-copy explanation to the measured artifact topology. A narrower Windows guarantee needs explicit disposition, not an implied pass of item 11. |
| T323-R2 | Medium | Yes — the privacy gate accepts planted cookie material | `artifact_gates.py:266` skips cookie scanning by suffix and line length, and scans contents without checking artifact filenames. A synthetic `Cookie:` header planted in `.bin`, and a populated Netscape `cookies.txt`, both survive the scan on a real build. In isolated fixtures the same header is missed in an extensionless file or a long text line; URL token material and a registered literal that `core.logging.redact` removes also pass. Importing three cookie patterns does not implement the claimed complete redaction vocabulary or an every-file scan. | **Open.** Cover cookie filenames and content across artifact files with bounded scanning that does not silently skip the property; account for the other declared secret classes. Keep the passing ordinary-data control and demonstrate representative secret mutations fail. No actual credential leak is claimed from these synthetic counterexamples. |
| T323-R3 | Medium | Yes — required CI baseline fails | The actual `frozen linux` job at **2e2507b**, run **34671191193**, fails the new gate on bundled Python libraries. `artifact-gates.txt` reports **110** home-directory/user-path hits, beginning with `libpython3.14.so.1.0` and `lib-dynload` extensions. The earlier run **34670254907** at 450cecd fails the same way. Local green gates do not establish that the check runs successfully in its required CI environment; Windows jobs at the submitted head remain queued. | **Open.** Diagnose and correct the CI artifact/scan interaction without broadly exempting binaries, then retain green baseline and required mutation evidence on both platforms. The log alone does not decide whether these are forbidden build paths or upstream provenance colliding with the runner identity. T-324 still owns future release-workflow integration. |
| T323-R4 | Low | No | T-323 asks to narrow the manual release prompt after automation, but `docs/RELEASE.md` still repeats the dynamic-link, licence-text and secret checks in the unchanged moved prompt. TESTING correctly leaves the repository half of item 13 manual. | **Open — T-323 completion sync.** Point the artifact checks at their actual evidence and clearly retain the manual remainder; do not narrow beyond what the corrected gates prove. No separate task or pass is needed for this wording. |

These are defects in the checks and their evidence. The review does not infer an
actual licensing violation from a synthetic mutant or treat a missing DLL as proof
of static linkage.

### Reproducible artifact measurements

The pinned subject was extracted with `git archive 2e2507b`. After a separate
release-version test, its version file was restored byte-for-byte from that
commit. It was frozen from the archived repository root using the existing venv:

```bash
PYTHONPATH=/tmp/tt-phase5-release-tree/src \
  /mnt/storage/software_projects/tracks-and-trails/tracks-and-trails/.venv/bin/python \
  -m PyInstaller --noconfirm --clean \
  --distpath /tmp/tt-phase5-pinned-freeze/dist \
  --workpath /tmp/tt-phase5-pinned-freeze/build packaging/tracks-and-trails.spec
```

The build completed in approximately **57 s**. This is a Fedora smoke payload,
not the submitted AppImage or clean-machine evidence. The working checkout gained
unrelated implementation edits during review; the archived build prevents those
edits becoming the test subject.

The bindings, in sorted order, are Core, DBus, Gui, Network, **Widgets**. The
Widgets paths are:

```text
_internal/libQt6Widgets.so.6 -> PySide6/Qt/lib/libQt6Widgets.so.6
_internal/PySide6/Qt/lib/libQt6Widgets.so.6 (regular file)
```

| Probe over that build | Actual result |
|---|---|
| Unmodified full gate | All four pass, exit 0 |
| Delete the regular Widgets file, retaining the now-dangling symlink | No existing Widgets library inside artifact; direct loader inspection borrows host Qt and reports version errors; **full gate still exits 0** |
| Replace every PySide Qt binding with a compiled ELF stand-in having no Qt dependency, leaving the shared-library filenames | Linkage gate returns no problems |
| Plant the same synthetic Cookie header in `.txt` / `.bin` | `.txt` rejected; `.bin` passes |
| Plant a populated Netscape `cookies.txt` | Passes |
| Insert a `.so`, then a `.pyd`, inside `yt_dlp` | Each rejected |
| Delete each of the four required licence files independently | Each rejected |
| Restore all mutations | All four pass, exit 0 |

The earlier isolated probe also confirmed that invalid ELF input, no bindings and
no `ldd` return success. These are not extrapolated Windows results.

Local diagnostic logs are `/tmp/tt-phase5-pinned-build.log`,
`/tmp/tt-phase5-pinned-mutations.log` and `/tmp/tt-phase5-gate-probe.log`.
They are convenience copies; the commands, topology and results above retain the
substantive evidence without depending on a transient handoff.

### Other checks and retained CI

- **11 artifact-gate unit tests pass**, within the shared **133 passed in
  11.94 s** run. Their fixture carries no PySide binding, so its positive case
  never exercises the loader check. Passing these tests does not refute R1.
- Ruff lint/format and bare mypy on Linux and with `--platform win32` passed
  before concurrent implementation edits appeared; each mypy run covered
  **171 source files**. No Windows runtime coverage is inferred from typing.
- Both GNU texts exactly match the URLs in their provenance: LGPL **7,652 bytes**,
  GPL **35,149 bytes**. The **1,211-byte** yt-dlp text matches the installed
  **2026.7.4** wheel's licence. NOTICE is locally authored, as recorded.
- The [submitted-head CI run](https://github.com/kottmans/tracks-and-trails/actions/runs/34671191193)
  supplied `frozen-evidence-linux/reports/artifact-gates.txt`; the older
  [450cecd run](https://github.com/kottmans/tracks-and-trails/actions/runs/34670254907)
  supplies the same failure. Reports were independently downloaded, not inferred
  from the aggregate run status. The latest Windows jobs were queued when checked.

The conditional ffmpeg licence requirement and deferred integration in T-324
are sensible task boundaries. Neither removes the current gate defects. Task/status
updates and any implementation correction remain with their respective owners.
