# T-331 review

**Purpose:** Independent review of the Windows mutation driver's verdicts and retained run.
**Owner:** Reviewer
**Update when:** T-331 is reviewed or a finding receives a disposition.

## 2026-09-13 — Initial review

**Reviewer:** Codex, independent of the implementer.
**Boundary:** The submitted T-331 driver and its final mutation corrections at
`acdc57071026fac0dfb15743fe24651cb1fcf013`; correction-batch base `8e67f6f`.
**Platforms verified:** Linux driver counterexamples; source and supplied Windows
records. No fresh Windows mutation run by this reviewer.
**Verdict:** Changes requested.

| ID | Severity | Blocks approval | Finding and disposition |
|---|---|---|---|
| T331-R1 | Medium | Yes — the task's verdict-correctness criterion | `run_mutations.py:173–188` uses exit 1 alone to award a kill. An error-only pytest run, or exit 1 with no result summary, is reported **KILLED** and can leave the whole driver at exit 0. A baseline exiting 2–5 is also not entered into `broken`, so its mutations receive kill verdicts despite no valid baseline. **Open — correct within T-331:** require usable outcome evidence and a successful baseline for each selection; classify setup/collection/incomplete runs as inconclusive. Preserve real assertion failures accompanied by teardown errors, as in the supplied Windows run. |
| T331-R2 | Low | No | The comment above the title control still says a surviving control makes every later verdict meaningless, contrary to the task's explicit correction of that claim and the driver's actual behavior. **Open — T-331 completion sync:** state what that control proves and how its result affects the table without invalidating independently evidenced mutations. |

### Independent counterexamples

A temporary script executes the **whole unchanged driver** in a temporary
repository-shaped directory, replacing only `subprocess.run` results for git
and pytest. It exercises the actual classification, report writing and final
exit. The normal control has passing baselines and real-failure summaries.

| Substituted result | Driver result |
|---|---|
| Normal successful baselines and `1 failed, 38 passed` mutations | Exit 0, expected verdicts |
| Desktop baseline exit 2, `Interrupted: 1 error during collection` | Baseline `NO RESULT`; following desktop mutations nevertheless `KILLED`; overall exit 1 |
| Title-control exit 1, `1 error in 1.0s`, no assertion executed | **KILLED; overall exit 0** |
| Title-control exit 1, only progress dots | **KILLED; overall exit 0**, despite `NO SUMMARY LINE` |

The error-only case is a real pytest possibility: an independent one-test file
with a fixture raising before the assertion returns **exit 1, 1 error**. The
driver has no information establishing a detected product mutation in that case.
Reproduction script: `/tmp/tt-phase5-corrections-review/mutation_driver_probe.py`;
the sequences and results above are retained here so the temporary file is not
required to understand or reproduce the finding.

### What the Windows evidence does establish

The task retains a clean `40b1dd8` rerun table, with **three baselines and seven
mutation cases** (ten rows total). The earlier full report at `4320859` was also
located and inspected: the title control has two actual test failures, and the
other killed mutations have actual failed assertions alongside teardown errors.
Those observed failures are not invalidated by R1. The final table records the
two formerly stale cases as killed after their corrections.

The name-based dialog swap changes two currently reachable controls; the view
reversal is observable in the failed state with three controls. The driver names
the tree, refuses dirty work by default, preserves its full output, and suppresses
verdicts after a baseline **test-failure** exit. These are useful corrections;
the unhandled baseline and error-only cases above prevent approval of the
broader verdict claim. [Shared checks](phase-5-second-batch.md).

## 2026-09-13 — Focused correction verification at ddc8f3d

**Reviewer:** Codex. **Base/head:** `acdc570` → `ddc8f3d18854991f89d5b8656bf1c7b6be65e15f`.
**Verdict:** Approved. No open findings remain in this review.

| ID | Severity | Blocks approval | Disposition and evidence |
|---|---|---|---|
| T331-R1 | Medium | No | **Resolved.** The pure verdict function requires failed-test evidence for a kill, and the loop invalidates a selection after any non-OK baseline. The original whole-driver counterexamples now return NO RESULT and exit 1; the normal control retains its expected table and exit 0. The new verdict suite passes, including real failures with teardown errors. |
| T331-R2 | Low | No | **Resolved.** The control comment now distinguishes evidence that the plugin works from independently evidenced later kills. |

The original temporary reproduction executes the unchanged current driver,
including subprocess-result handling, report writing and final exit. With the
correction, an interrupted baseline suppresses all its dependent mutation
verdicts; error-only and missing-summary mutations are inconclusive and make
the driver fail. This verifies the loop as well as the new pure function.

The original `4320859` Windows summary lines were independently replayed through
`verdict`, retaining that run's historical expectations: all ten verdicts remain
unchanged, including its two unexpected outcomes. The later clean `40b1dd8`
table likewise supplies passing baselines and failed-test counts for its kills.
No Windows rerun is needed to establish this change in classification; this
review does not claim to have rerun the desktop mutations.
[Shared validation](phase-5-second-batch.md).
