# T-325 review

**Purpose:** Independent review of startup measurement and REL-008's implementation.
**Owner:** Reviewer
**Update when:** T-325 is reviewed or measurement evidence is supplied.

## 2026-09-13 — Initial review

**Reviewer:** Codex, independent of the implementer.
**Base:** `2e2507b5cfe4e262fcfda9b954dbbf68c390636c`.
**Head:** `8e67f6ff5b5804a8da907c56aeecac8e9fa5a0a7`.
**Platforms verified:** Linux source/tests and retained measurement record.
**Verdict:** Changes requested.

| ID | Severity | Blocks approval | Finding and evidence | Disposition/status |
|---|---|---|---|---|
| T325-R1 | Medium | Yes | `tools/startup_time.py:80,116–129` defaults to five consecutive launches even with `--cold`, labels their median cold, and gates on that median. Only the first launch after a reboot is cold. Feeding measured durations `[6, 1, 1, 1, 1]` through `main(["fake-artifact", "--cold", "--bound", "5"])` prints **cold median 1.000s** and exits **0**, accepting a cold launch above the bound. TESTING §8 advertises this command as the executable cold gate. | **Open — correct in T-325.** Gate the actual cold sample independently from warm launches; multiple cold samples require independent cold conditions. Add a counterexample where cold exceeds the bound and warm passes. |
| T325-R2 | High | Yes | The submitted evidence explicitly lacks Linux cold measurement, yet the current T-325 status says NFR-002 is met. The Windows cold snippet contains one duration without an artifact path, digest or source commit, and the record calls it a release build rather than establishing the installed artifact required by NFR-010. The ten-number/two-median criterion and item 14's build-identification criterion are not met. REL-008 changes the Windows bound; it does not waive Linux cold measurement or establish the identity of an unrecorded build. | **Open — T-325 measurement/coordination.** Supply the missing cold evidence and exact artifact identities, or obtain an explicit amendment of the evidence criteria. Preserve existing measurements as historical evidence and remove unsupported current compliance claims. |

The deterministic R1 probe substitutes only `one_launch`'s returned measurements;
it executes the real CLI parsing, loop, aggregation and verdict. It does not
pretend those synthetic durations were measured on a physical machine.

The platform distinction itself is correct: NFR-002 remains Linux-only, and
REL-008 explicitly approves a separate 5-second Windows bound. The observation
that a 4-second bound has little margin is supported by the recorded values.
Warm and first-touch results cannot establish the untouched cold criterion.

Timestamp tests pass and the hook uses the existing exposure watch. No reference
machine was rebooted, no cache was dropped, and no new physical startup timing is
claimed here. [Shared checks](phase-5-second-batch.md).
