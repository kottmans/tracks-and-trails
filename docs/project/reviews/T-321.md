# T-321 review

**Purpose:** Independent review of the Linux AppImage build and its evidence.
**Owner:** Reviewer
**Update when:** T-321 is reviewed or a finding receives a disposition.

## 2026-09-12 — Initial review (UTC)

**Reviewer:** Codex, independent of the implementer.
**Task:** T-321. **Round:** Initial.
**Base:** `59598de4fd7af2c9fe921bb99002761d7f01c766`.
**Head:** `2e2507b5cfe4e262fcfda9b954dbbf68c390636c`.
**Platforms verified:** Linux source/metadata and a locally frozen payload;
the submitted AppImage and its clean-container session were not available locally.
**Verdict:** Changes requested.

### Findings

| ID | Severity | Blocks approval | Finding and evidence | Disposition/status |
|---|---|---|---|---|
| T321-R1 | High | Yes | The second acceptance criterion requires a completed download on both the clean supported distribution and Fedora. The submitted clean-container observation is twenty seconds offscreen, with probes; its only completed download is on Fedora. The task explicitly acknowledges this gap. Neither a launch nor the desktop transfer establishes the clean-machine session. | **Open.** Retain the clean-machine preflight, artifact identity, launch, completed transfer and exit under T-318's evidence route, or obtain an explicit criterion amendment. The existing container ruling changes the environment, not the required operation. |
| T321-R2 | High | Yes | The documented command starts stock `python:3.14-slim-bookworm`, but the script neither installs nor mounts `appimagetool`. At `packaging/build_appimage.sh:102`, packaging occurs only if `/usr/local/bin/appimagetool` already exists; otherwise the command succeeds with an AppDir. No second packing command is documented. Thus the advertised command does not produce the acceptance criterion's AppImage. The README also names neither this build image nor the asserted distribution/glibc floor, despite the task saying it does. | **Open.** Document and supply the complete tool/setup/packing route, with a successful command producing the named AppImage and recording the supported floor. An intentional AppDir-only mode must not be reported as a completed AppImage build. T-324's future automation does not supply today's missing command. |

### Checks and evidence limits

- Seven AppDir metadata tests passed within the shared **133 passed in 11.94 s**
  run (artifact gates, version checker, skeleton and colour suites).
- `bash -n packaging/build_appimage.sh` and `desktop-file-validate` passed.
  Ordinary `appstreamcli validate` failed its two online URL checks because the
  sandbox could not resolve GitHub. `appstreamcli validate --no-net` succeeded
  with two pedantic messages. This validates source metadata, not metadata
  extracted from the unavailable submitted AppImage.
- The submitted source freezes successfully on this Fedora host. That payload
  is useful for adversarial gate checks, not evidence of compatibility with
  Ubuntu or Debian. See [T-323](T-323.md) for the actual Qt dependency failure
  the gate still accepts.
- The written Fedora launch/download/launcher and AppImage update/isolation
  results remain implementer-reported evidence. No original build log, final
  AppImage hash, clean-container log or session record was located in the
  supplied checkout. Their location was requested during this review.
- A completed download alone is also less than TESTING §8 item 8: that item
  additionally asks for another download's cancellation, exit, and no recursive
  launch or orphan. This review does not mark the entire release smoke item
  passed from the task's download statement.

The choice of an older-glibc container is consistent with the accepted direction;
no return to a developer-host release build is requested. The launcher integration
question remains disclosed work, and the missing AppImage CI workflow already
belongs to T-324. These observations grant neither release approval nor approval
of T-318/T-324. Task/status changes remain with their owner.
