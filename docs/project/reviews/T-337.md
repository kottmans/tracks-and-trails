# T-337 / T-338 / T-335 session review

**Purpose:** Independent review of the Phase 5 session changes since the last approval.
**Owner:** Reviewer
**Update when:** This implementation range or its corrections are reviewed.

## 2026-09-14 — Initial review of the session changes

**Reviewer:** Codex, independent of the implementer.
**Tasks:** T-337, T-338, T-335; accompanying T-327 UI fixes and record consolidation.
T-322's new findings and T-039/T-325's disposition updates remain in their existing records.
**Base:** `08251df`. **Head:** `39dab46599661d204fffe1bc59283fe7cbd6f3ff`.
**Scope:** All 37 intervening commits, including documentation and deleted tools.
**Round:** Initial comprehensive review of this new range. Prior approvals remain intact.
**Platforms verified:** Linux, Python 3.14.7 / PySide6 and Qt 6.11.1; Windows branches type-checked.
**Verdict:** **Changes requested.** T-335's implementation is **Approved** at this head.
T-337 and T-338 need the corrections below; T-322 has two additional blockers in its record.
No release or completion of the remaining Windows manual session is approved.

The maintainer's recorded rulings were treated as settled. In particular, this review does not
reopen naming as a preference, literal per-item renaming, removal of preset naming, bounded
automatic read retries, one uninstall confirmation, notify-only updates, the candidate-only
installer gates, or the startup-measurement exception for 0.1.0.

### Findings

| ID | Severity | Blocks approval | Finding and evidence | Disposition/status |
|---|---|---|---|---|
| T337-R1 | Medium | Yes | The durable `Probed` handler in `downloader/manager.py:2947` copies title, thumbnail, uploader and duration but omits the four new naming fields. A real spawned probe reporting upload date `20260913`, ID `site-id`, channel `Channel` and site `Youtube` reaches READY with **all four stored as None**. Playlist entries and recovered unprobed jobs therefore preview known naming fields as `NA`; accepting an edited name can freeze that incorrect preview into a literal name. The migrations and Add URLs projection do not repair this path. | **Open — T-337 correction.** Carry the fields in the same durable revision as the other probe metadata, including replacement of stale values with None when a new probe reports none. Verify a real background probe and subsequent Rename preview, rather than only constructing a Job with the fields already populated. |
| T337-R2 | Medium | Yes | `ui/main_window.py:1128` chooses today's Settings template whenever the existing filename is literal. This applies to every subsequent rename, although its stated exception is for clearing a rename. With an existing `%(uploader)s/My clip.%(ext)s` and today's default Title pattern, opening Rename and accepting its unchanged prefill writes **`My clip.%(ext)s`**, dropping the folder. Typing `Second name` likewise drops it. | **Open — T-337 correction.** Preserve the current request's folders for a nonempty rename and preserve an unchanged acceptance without writing. Resolve today's setting only for the explicit reset operation. Cover subsequent renames, changed settings, and playlist folders/position padding. |
| T337-R3 | Medium | Yes | `core/output_template.py:111` no longer supports the previously offered `duration_string`. Loading `output_template = "%(title)s (%(duration_string)s).%(ext)s"` now returns an empty naming preference and a warning claiming the field would render NA. Saving any other setting then replaces the old preference with the default. UX-014 expressly keeps stored templates that the new field editor cannot display; removing the old field from the offered menu does not authorize dropping those settings. | **Open — T-337 correction.** Preserve valid legacy naming through load, unrelated saves, preview and request construction, or migrate it without losing its behavior. Keep the approved new Duration presentation. The positive control using `%(title).30s.%(ext)s` already survives an unrelated update-preference save. |
| T337-R4 | Medium | Yes | `resolve_queue_fields` (`core/output_template.py:273`) replaces raw substrings, including escaped literals and text inserted by a previous replacement. Readable `100%(duration)s {Title}` becomes a filename **`100%(title)s.mp4`** for title `Clip`, instead of the literal text followed by the title. With `{Playlist}/{Title}`, playlist name `%(duration)s Mix` and duration 507, the folder becomes **`%8m27s Mix`**. The inserted metadata is being interpreted again. | **Open — T-337 correction.** Resolve only actual unescaped field tokens, once, and preserve inserted literal metadata. Audit the sibling field/refusal readers for escaped percent handling and all three queue-time fields. Retain the separator-removal behavior and the final containment guard. |
| T338-R1 | Medium | Yes | The automatic check's only schedule is a startup `singleShot` (`app.py:364–373`). It captures the initial preference; the preference writer at `app.py:1041` updates the saved setting but cannot cancel the request. A composed-window probe switches the actual checkbox off before the timer fires, verifies false on disk, then still observes an automatic request. The timer also has no daily rearm: an application left open across days never checks again, and a launch inside the 24-hour window schedules nothing for when that window expires. | **Open — T-338 correction.** Give the daily schedule a lifetime owner, consult the current preference and due time at dispatch, cancel pending work on opt-out, and schedule the next due check while the application stays open. Test preference changes during the delay, a manual answer before the delay expires, and the next daily deadline without another launch. An already-started request need not be retroactively undone. |
| T338-R2 | Medium | Yes — decision pending | `docs/UX_SPEC.md:153` labels the status-bar placement **[T]** while explicitly calling it the implementer's choice. The supplied review request confirms that only non-interruption was ruled, despite `REL-009` item 2 currently attributing the placement to the accepted decision. Under UX_SPEC §1, the placement is **[P]**, not a transcription or a necessary derivation. | **Open — T-338 placement disposition.** The built status-bar notice is a reasonable quiet treatment, but review cannot attribute that choice to the maintainer. Ratify the concrete placement or implement the chosen alternative, then reconcile the two canonical descriptions. This asks about placement only; notify-only updates remain settled. |
| T337-R5 | Low | No | Current documentation still contains superseded statements: REQ-011 calls naming a set of named choices; UX-014's closing paragraph says playlist fields are refused; STATUS still lists T039-R2 as awaiting a ruling and omits the newly active review tasks. These conflict with the same range's later rulings and implementation. | **Open — ordinary completion sync for T-337/T-338/T-327.** Refresh current descriptions and append any necessary decision amendment without rewriting dated evidence. No separate task is requested. |

T337-R1 through R4 concern observable naming behavior. No path escaping the download root was
established, and the findings do not claim an outside-directory write or overwritten media.

### Checks and counterexamples

The committed suite and the additional review probes answer different questions. The review
probes deliberately assert the required behavior and fail on the submitted implementation; they
were temporary test files, removed from the checkout after execution. No product source was edited.

| Check | Actual result |
|---|---|
| Ruff lint and whole-tree formatting at head | Passed; 411 files already formatted |
| Bare mypy, and bare mypy with `--platform win32` | Both passed; 190 source files per run |
| First default Linux suite, `python -m pytest -n auto` | 4,523 passed, 57 failed, 21 skipped, 17 warnings; 115.53 s. Local socket creation was denied for 55 tests; two toolchain tests lacked the activated venv on PATH. |
| Those 57 failures, activated venv and permitted local sockets, `python -m pytest -n auto --lf` | **57 passed**, 35.56 s. Together the runs verify **4,580 distinct committed tests passed, 21 skipped**. This is not described as one clean full run. |
| Additional review probes | **7 expected-behavior assertions failed, 2 controls passed**, 3.35 s. Failures: lost probe metadata; unchanged and changed subsequent renames; opt-out before dispatch; legacy duration template; literal queue-field text; re-expansion of playlist text. |
| Containment control | **336 cases: 140 contained, 196 refused.** Rendered templates included traversal, absent fields adjacent to dots, leading slash, Windows drive/UNC forms, reserved names, percent text, and a symlink to a sibling directory. Every accepted resolved path remained beneath the chosen root. No media was written. |
| Unshown-template control | A real composed Preferences screen saves another preference without changing `%(title).30s.%(ext)s`. |
| Database upgrade coverage | The committed persistence suite passed. Its parameterized historical-fixture test loads pre-migration SQL, preserves every prior job column/value, applies forward migrations, and reads the result through today's JobRepository. This includes v10 → v12 and v11 → v12, not merely a fresh schema. |
| Live release query through production `latest_app_release()` | **`AppReleaseError: No version has been released yet.`** The fixed GitHub endpoint was contacted without credentials or user identifiers. This verifies the live 404 path, not a published release. |
| Eleven task moves in `19788c0` | Mechanical comparison found the task bodies retained; removed text consists of superseded status paragraphs, separators and an enclosing section heading. The README-install, changelog and AppImage-menu obligations are carried by T-328. |
| Deleted-path references | No live executable or current procedural reference to the removed tools/checklists was found. Retained dated references remain history, including DECISIONS' mention of `tools/failed_row_screenshot.py`. |

The local logs are `/tmp/tt-review-39dab46-pytest.log`,
`/tmp/tt-review-39dab46-pytest-retry.log` and `/tmp/tt-review-39dab46-probes.log`.
The temporary probe source is retained locally at `/tmp/tt-review-39dab46-probe.py`.
The concrete inputs and outputs above are the durable reproduction record; approval does not
depend on those temporary files surviving.

### Disposition of the other flagged areas

- **Queue Again:** Approved at the reviewed head. Cancellation remains terminal to late worker
  outcomes; the write waits for session release, partial cleanup precedes readmission, and the new
  admission is a probe. The committed manager, state, row-action and shutdown tests pass.
- **Automatic read retries:** The classification is a heuristic, not proof of transience.
  In the installed pinned yt-dlp, `ExtractorError.expected` describes an ordinary explained error
  versus a likely bug; YouTube's known unavailable/sign-in reasons use `expected=True`, while
  georestriction keeps its separate error kind. No counterexample was established in those paths
  that retries a known removed/private video. A permanent 403 or an extractor bug can consume the
  three-retry budget; 14 seconds is backoff alone, excluding request duration and worker overhead.
  This is a bounded cost of the implemented heuristic, not a new blocker. The download path stays
  network-only. The underlying semantics are in the pinned
  [yt-dlp error class](https://github.com/yt-dlp/yt-dlp/blob/2026.08.19/yt_dlp/utils/_utils.py#L981)
  and [YouTube extraction](https://github.com/yt-dlp/yt-dlp/blob/2026.08.19/yt_dlp/extractor/youtube/_video.py#L4047).
- **Theme and hand-painted row verbs:** No regression was established in the changed faces,
  press tracking, indicator assets, packaging, keyboard/menu routes or accessible naming. The
  buttons were delegate drawings before this range too; their keyboard route remains the row menu.
  Passing Qt/UIA inventories do not establish Narrator speech or Windows high-contrast behavior.
  Those remain explicitly unverified in T-327's existing manual session, not a newly invented task.
- **Show in folder:** The changed argv separates `/select,` from the path and keeps argument-list
  execution. Tests cover spaces and other filename characters. Windows behavior remains supported
  by the maintainer's reported desktop/Sandbox observation, not a Windows execution by this reviewer.
- **Uninstaller:** See [T-322's new findings](T-322.md#2026-09-14--review-of-the-one-dialog-uninstaller).
  The claimed shared-temp-directory race was not established: the new invocation starts the
  installed executable and obtains its own temporary directory. The exclusive log-file race is
  concrete source evidence and is recorded separately. The new code has not received a fresh
  scripted Sandbox run. The accepted candidate-only cadence remains in force; a new per-push
  requirement is not imposed.
- **Published update response:** Sample-response, URL-construction, ASCII-version, service-pool,
  explicit/automatic and preference tests pass. The live 404 now has independent evidence. The first
  real published tag still needs the release comparison and download-page link checked in T-328.
- **Intermediate red CI:** The disclosed test-import type error was corrected in the next commit;
  both type checks pass at this head. This review does not erase or recast the intermediate failure.

The Windows result of 131 passed at `f29a542` is supplied implementer evidence in T-338. It is not
a fresh Windows run by this reviewer, a complete Narrator session, or candidate release approval.
The T-336 crash disposition and cancellation of the diagnostic tasks remain as ruled.

### Correction and readiness boundary

Correct the blocking implementation findings within their existing tasks and return one mapped
correction batch with regression and negative-control evidence under TESTING §14. Keep the
one-dialog and notify-only product rulings. T-335 can leave review in the normal owner completion
sync. [T-039](T-318.md#2026-09-14--disposition-of-the-candidate-only-gate-ruling) and
[T-325](T-325.md#2026-09-14--disposition-of-the-010-startup-exception) now have their prior blockers
dispositioned against the accepted amendments. Task/status closure remains the serial owner's work.

No branch, tag, release, Windows installation, source correction or push was performed. The
review-document commit identifies this record; the reviewed implementation remains `39dab46`.

Review-document checks passed: Ruff lint and formatting (412 files), `git diff --check`,
155 local file links and the new inter-review anchors. All three appended review records retain
their earlier bytes as exact prefixes. The historical migration verifier also passes: 381 entries
in 105 files, with all 2,403,546 original bytes preserved.

## 2026-09-14 — Focused correction re-review at 03c6745

**Reviewer:** Codex, independent of the implementer.
**Tasks:** T-337 and T-338; shared verification for T-322 and the completion-record moves.
**Round:** Focused correction re-review, following the initial session review above.
**Base:** `6933cea`. **Head:** `03c67454e4f6e9cae9596fb145af7f393ff81a5d`.
**Verdicts:** **T-337 Approved; T-338 Approved** for the submitted implementation.
T-322's original corrections are verified, but its new confirmation finding leaves that scope
**Blocked** pending the TESTING §14 disposition in [its record](T-322.md#2026-09-14--focused-correction-re-review-at-03c6745).
The remaining installed-build observations and release gates retain their existing owners.

The two submitted commits are `bde090d` and `03c6745`. The four substantive review records
rebased from `c19b712` to `6933cea` are byte-identical. Comparing `39dab46` with `b6b7e56`
finds no change under `src/` or `packaging/`; the intervening GitHub-page work does not change
this implementation boundary. The review index additionally acquired the other scope's link,
which is preserved. The accepted product rulings were not reopened.

| ID | Severity | Blocks approval | Disposition and independent evidence |
|---|---|---|---|
| T337-R1 | Medium | No | **Resolved.** The durable `Probed` write now carries all four naming fields and overwrites stale values with `None`. Both committed spawned-worker tests pass. The independent original metadata probe passes, and an additional worker-to-Rename probe shows `2026-09-13 site-id Channel Youtube Clip` in the input and preview from the stored job. |
| T337-R2 | Medium | No | **Resolved.** Nonempty renames use the job's current folders, unchanged acceptance writes nothing, and an empty name resets a previously renamed job to today's setting. Both original counterexamples pass, along with changed-preference and playlist-padding regressions. An independent probe verifies that typing the default stem `A clip` produces literal `%(uploader)s/A clip.%(ext)s`, while clearing produces today's `%(title)s.%(ext)s`. This is consistent with the requested explicit reset: matching the default text is no longer a second reset command. |
| T337-R3 | Medium | No | **Resolved.** The legacy `duration_string` remains supported without being offered. The original settings-load counterexample passes; the committed unrelated-save and manager-preview checks preserve the preference and render its length. The unshown `%(title).30s.%(ext)s` control still survives a real Preferences change. |
| T337-R4 | Medium | No | **Resolved.** Queue-time resolution consumes escaped percents and actual tokens in one pass; inserted metadata is not revisited. The original literal and playlist-name counterexamples pass. The committed reader/refusal and separator cases cover the sibling parsing changes. The repeated 336-case containment control reports 140 contained and 196 refused, with no accepted path outside the root. |
| T337-R5 | Low | No | **Resolved.** REQ-011 now describes named fields, UX-014 explicitly supersedes the old playlist-field refusal, and STATUS reflects the submitted correction state. Future approval/status updates belong to the ordinary completion sync. |
| T338-R1 | Medium | No | **Resolved.** Composition owns `DailyUpdateCheck`; dispatch reads the current preference and answer time, preference changes cancel or arm its timer, answers and failures rearm it, and shutdown stops it. The original real-checkbox opt-out probe passes. Committed tests also cover a launch before the deadline, a second day without relaunch, a manual answer during the delay, no-answer retry, off/on transitions and stopping. The strengthened negative assertions wait for the worker thread rather than inspecting an empty request list immediately. |
| T338-R2 | Medium | No | **Resolved.** REL-009's September 14 amendment explicitly records maintainer ratification of the status-bar button, and UX_SPEC no longer attributes it merely to the implementation. No further placement choice is needed. |

### Independent checks

| Check | Actual result |
|---|---|
| Full committed Linux suite, activated venv with local socket permissions, `python -m pytest -n auto` | **4,609 passed, 22 skipped, 17 warnings; 211.92 s**, exit 0. This is one complete passing run at the submitted head. |
| Ruff lint and formatting | Passed; 420 files already formatted before temporary probes. |
| Bare mypy and `mypy --platform win32` | Both passed; 192 source files each. Windows runtime is not inferred from these checks. |
| Independent review probes, including the original 336-case containment control | **11 passed**, 5.29 s. The original nine checks pass, with the opt-out probe adapted only to the new timer owner. Two additional probes cover background metadata reaching Rename and literal-default versus empty-reset behavior. |
| First additional-probe attempt | **10 passed, 1 failed**, 4.82 s. The reviewer initially expected compact `20260913` in the visible name; the offered Upload date field correctly formats `2026-09-13`. Correcting that mistaken expectation produced the passing run above. No source change was needed. |
| T-039 and T-325 task moves | Both complete task bodies from `**Owner:**` onward match the prior active records exactly; only the status introductions changed. Neither task remains in the active queue. Their accepted review dispositions are preserved. |
| T-332 approval boundary | The pin, baseline constant, licence README and option audit are unchanged from the already-approved `acdc570` baseline-bump scope. See [the existing record and disposition clarification](T-326.md#2026-09-14--clarification-of-the-existing-t-332-approval). |

Logs are retained locally as `/tmp/tt-review-03c6745-pytest.log`,
`/tmp/tt-review-03c6745-probes-first.log` and `/tmp/tt-review-03c6745-probes.log`.
Temporary probe source is `/tmp/tt-review-03c6745-probe.py`; it was removed from the checkout
after execution. The exact examples, results and limitations in this record remain usable if
those temporary files are discarded. The implementer's mutation results were inspected through
the changed tests and their named failure conditions; this pass does not claim a fresh execution
of every reported mutation.

The containment control uses the same 12 templates, seven titles and four playlist values as
the first review, including traversal, drives/UNC syntax, escaped field text and a symlink to
a sibling directory. It renders candidate paths and exercises the final containment guard;
it does not write media or prove immunity to concurrent filesystem changes.

The supplied STARBASE result is **271 passed, 1 skipped** under the Windows platform plugin.
It remains implementer evidence, not a new Windows run by this reviewer. The retained Sandbox
artifact and the interactive-uninstall limits are assessed in T-322's record. Narrator, visible
uninstall messages and the other remaining installed-build observations stay with T-327.
The first real published-release response remains T-328's check; no new release was published.

T-335's existing approval is undisturbed. T-337/T-338 may leave correction review in the serial
owner's normal completion sync, preserving their remaining manual criteria. The approval above
identifies implementation head `03c6745`, not the later review-document commit.

Review-document validation passed: Ruff lint and formatting (420 files), `git diff --check`,
155 local file links and all new cross-review headings. Each of the three appended records
retains its prior bytes as an exact prefix. The migration verifier preserves all 381 original
entries in 105 files, totaling 2,403,546 historical bytes. The commit-message check also passes.
