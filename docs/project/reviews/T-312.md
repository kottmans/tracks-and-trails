# T-312 — Panels as pages of the add dialog

**Purpose:** Independent review of the shared format, playlist and template page mechanism.
**Owner:** Reviewer
**Update when:** T-312 is reviewed or a finding receives a disposition.

## 2026-09-10 — Initial comprehensive review

**Reviewer:** Codex, independent of Claude's implementation.
**Base:** `fe9787c5a3520ff3bbcb177bc4207ceba5306a3e`.
**Implementation head:** `e03b63afe960306bfa0d6b4fc175e887cc703e53`.
**Combined state tested:** `fec0d3e510c5835ce9d0a0d579b77bc8a38b42ee`.
**Verdict:** Approved at the combined tested head, for T-312's page mechanism.

Inspected the complete five-file implementation diff, the earlier ruling and
discarded-attempt record, the UX amendments, and every retired/repointed test.
The common `RowPanel` mechanism now mounts on a page of the same dialog.
Deferred reparenting and the `setIndexWidget(index, None)` editor cleanup both
remain. Closing returns to the row by identity; a removed row closes its page.
The screen-bounded widening rule remains and no longer measures list chrome.

### Finding

| ID | Severity | Blocks approval | Evidence and requested correction | Status / route |
|---|---|---|---|---|
| T312-R1 | Low | No | Removed machinery retains current-tense explanations in `StagingModel.refresh` (relayout/remount), `_build_staging` (resize connection), and `_open_panel` (withdrawing uniform row sizing). The corresponding calls have been deleted. The new page implementation is correct, but these comments describe actions it no longer performs. | Open — remove or update those current explanations during T-312's ordinary completion sync. Preserve the dated first-attempt and review records. No new task or extra review pass solely for this cleanup. |

### Retired guards and surviving concerns

| Retired concern | Current evidence / disposition |
|---|---|
| Only the expanded row grows | Rows now report panel height zero; expansion is no longer row geometry. The page-size test verifies the replacement arrangement. |
| Initial widening when the format panel opens | The stronger `test_the_mounted_format_panel_fits_the_dialog_it_asked_for` remains, repointed to the page: three captures at three font sizes with a roomy substituted screen. |
| Panel bounded by the staging viewport | `test_an_open_panel_fills_the_page_and_keeps_its_way_out` checks actual page size and visible exit controls at four sizes, including 640×480. |
| Playlist and format panels left behind by value refresh | The widget is owned by the page layout. Surviving page/summary checks explicitly refresh while it is open and measure its realized geometry; URL retype/reset and reopening checks also remain. |
| Scroll during mounting overwrites panel geometry (T-296) | There is no mounted index widget to receive that scroll geometry. Page ownership and the surviving resize/reopen checks cover the replacement. |

The replacement does not rely on a page-size test alone: the mounted-fit,
summary reachability, narrowing, URL removal/reset, editor reopening, Esc,
playlist selection and template tests also passed. Tab traversal stays in the
visible page in the committed keyboard test. The tooltip finding is addressed
by hiding the staging view while retaining its row description; both sides are
asserted together. No lost behavioral guard was established in this review.

The 320px narrowing assertion now inspects meaningful child constraints instead
of treating the new stack wrapper as the cause. The diagnostic tool's matching
change preserves containers that impose a width beyond that of any child.
Its reported floor is implementer evidence; this review verified the committed
narrowing tests, not a new native desktop measurement.

### Checks, limits and coordination

All relevant tests passed within the independent **4,097 passed / 21 skipped**
suite, including the environment retry, and lint/format/types passed. Exact
commands and results are in [shared verification](T-314.md#shared-independent-verification).
The Linux offscreen result does not establish native Windows layout or a
release gate. T-313's separate preset/Notes findings do not invalidate the page
mechanism approval.

Only this record and its index link were added by the reviewer. The task owner
may perform completion sync, including R1; no source fix, task closure, branch
change or push was performed.
