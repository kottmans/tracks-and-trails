# T-184 review

**Purpose:** Independent review of the additional-options parser, measurements and implementation.
**Owner:** Reviewer
**Update when:** The submitted scope, evidence or finding disposition changes.

## 2026-09-18 — Initial stage A measurement and design review

**Reviewer:** Codex, independent of the implementer.
**Round:** Initial, stage A only; no hatch implementation is submitted.
**Base:** `c57b457aad721e0e16cc3457c60b9ea6c6b4ff83`.
**Implementation/evidence head:** `10ad47bdadac533289883bc618b43d6af7e87310`.
**Checked through:** `531f4b8a1a099afda0f3285d6fbe1458230c7868`; its additional change is T-348/status prose.
**Scope:** The two option-audit tests, `tools/ytdlp_option_keys.py`, the reservation integration
tests and the stage A conclusions in TASKS. Earlier T-183/T-256 rulings remain authoritative.
**Verdict:** **Changes requested** on the design conclusions. The inventory and reservation
measurements themselves are supported. Default-deny remains a maintainer decision.

### Findings

| ID | Severity | Blocks approval | Finding and evidence | Disposition/status |
|---|---|---|---|---|
| T184-R1 | Medium | Yes | `TASKS.md:730–732` concludes that merging the diff against CLI defaults preserves intent. It loses explicit values equal to those defaults when the library/application defaults differ. At the pin, `parse_options(['--fragment-retries', '10'])` yields 10, equal to `parse_options([])`, so the proposed diff is empty. Merging it into `build_options` leaves `fragment_retries` absent; the actual fragment downloader supplies that absent value to `RetryManager`, which gives **zero retries**. This is a hatch-class option, not a conflicting typed control. The requested ten retries silently become none. | **Open — correct stage A's merge conclusion in T-184.** Preserve explicit option effects even when equal to CLI defaults, including coupled outputs and precedence. Add the explicit-default counterexample to the eventual merge tests; a raw baseline diff is insufficient. |
| T184-R2 | Medium | Yes | `TASKS.md:784–795` treats classification as proof that the archive-stop reason is visible. `QueueModel._detail` sends only FAILED rows to `_failure_detail`; cancelled rows omit `error_message`. An independent probe with the actual `ExistingVideoReached` message produced `''` for the cancelled row's detail. The same row switched to FAILED produced the message. The adapter retains the reason, but the queue does not show it. The recorded `_match_entry` probe also does not establish the complete worker route or separately measure `--break-per-input`. | **Open — retract the claim that the visibility criterion is met.** Retaining CANCELLED is a possible design, but T-184 must measure both named options through the worker/manager/UI boundary and make their stopping reason visible before declaring them eligible on this basis. No new state is required by this finding. |
| T184-R3 | Medium | Yes | `TASKS.md:736–742` overstates the parser's isolation: unknown options/bad values raise `OptParseError`, but `parse_options(['--help'])` and `parse_options(['--version'])` raise `SystemExit`. Passing argv suppresses automatic config discovery, not explicit includes: a temporary config containing `--fragment-retries 37`, supplied via `--config-locations`, was loaded and yielded 37. At the pin `parseOpts` still invokes `root.configs[0].load_configs()`. A refusal performed only after calling the unrestricted parser is too late to prevent its exits and file reads. | **Open — narrow the recorded guarantees and specify admission before parser side effects**, plus exception containment. Carry tests for exits, automatic discovery and explicit config inclusion in T-184's parser stage. Those refused options need to be stopped before their actions occur. This is a design finding, not a claim that an unbuilt hatch already exposes the application. |

### Verified measurements

- Both inventory tests pass against the installed pin. The first derives the uncovered
  suppressed surface from the parser and pins its spellings; the second checks documented
  parser options against audit rows. The constant holds **39 spellings for 36 option objects**,
  consistent with the task's count. These are inventory guards, not yet the eventual complete
  admission-policy test.
- Re-running the survey reproduced **292 options; 175 changing keys, 72 with no dictionary
  difference, 45 unsuccessful sample parses, 27 changing multiple keys**. These describe the
  tool's selected sample values, not every possible value or combination. The geo-bypass
  counterexample is real; admission cannot rely solely on the diff.
- All **8 reservation tests passed**. They exercise a real loopback HTTP download onto an
  exclusive-create zero-byte target. A temporary pytest plugin removing `overwrites` from
  the options returned by `build_options` produced **4 failures / 4 passes**, with no setup
  errors: baseline, `--continue`, `--part`, and `--post-overwrites` failed on zero-byte output.
  Repository source was not edited for the mutation. The baseline passing run precedes it.
- This supports Finding 7's bounded question: each of the five individual settings preserves
  a complete direct download with `overwrites=True`. It does not establish all combinations,
  postprocessor behavior, resume from an existing partial, or a future parser/merge path.

### Checks and limits

On Linux at the checked head, Ruff lint/format passed (447 files); `mypy src` passed (67 files);
bare mypy and `mypy --platform win32` each passed (201 files). The combined focused selection
of audit, placement, toolchain, reservation, taskbar and composition tests passed **188 tests**.
An initial invocation had two PATH/toolchain failures and eight sandbox socket setup errors;
activating the project environment and enabling its loopback sockets resolved those environment
failures. They are not recorded as product defects.

The broader `pytest -q -n 2 tests/unit tests/ui` run gave **1 failed, 4,279 passed, 22 skipped**;
the same taskbar test fails in current-head Linux CI. Its disposition is
[T343-R2](T-343.md#2026-09-18--ownership-correction-and-current-head-verification).

[CI run 35310343208](https://github.com/kottmans/tracks-and-trails/actions/runs/35310343208)
succeeded at `10ad47b`, including Windows desktop and the full-suite jobs. The handoff's statement
that only `9c5ff66` carries Windows evidence is superseded: that run was cancelled.
[Run 35333950948](https://github.com/kottmans/tracks-and-trails/actions/runs/35333950948), at the
checked head, passed Windows desktop (46), Windows unit/UI (4,258 passed / 42 skipped), and
Windows integration (495 passed / 5 skipped), but failed Linux on T343-R2. These are inspected
remote results, not Windows tests executed by this reviewer.

### Readiness

The evidence supports continuing T-184 once these conclusions are corrected and its policy
choice is ruled. It does not complete T-184, T-048, a migration, or a release gate. The
default-deny recommendation is consistent with keeping new parser surface closed until
dispositioned, provided normalized-value handling still permits the safe no-bypass form.
Only this review record and its index entry are changed; task/status synchronization remains
with their owner.

## 2026-09-18 — Stage A corrections and cancelled-row implementation

**Reviewer:** Codex, independent of the implementer.
**Round:** Focused stage A correction review; initial review of the ruled cancelled-row change.
**Base:** `2b4b6c4352a6eb6df8db1081b78361c3bec618f2`.
**Cancelled-row implementation:** `271b5bebb5fb2445fb19108958c191fdab59e63c`.
**Checked head:** `24a5450de92b4e980dfd4ce58c65020b66ba1467`.
**Scope:** Stage A's corrected conclusions, the accepted default-deny/UX-005 rulings,
shared cancellation sentence and row rendering, and the 95-to-93 count correction/guard.
The parser, persisted value shape and migration 0013 are not submitted in this range.
**Verdict:** **Changes requested** on accessible cancellation text. The stage A factual
corrections are verified; this is not approval or completion of T-184 as a whole.

### Findings and disposition

| ID | Severity | Blocks approval | Finding and evidence | Disposition/status |
|---|---|---|---|---|
| T184-R1 | Medium | No | The task now specifies merging destinations affected by named options, including explicit values equal to CLI defaults, and retains the fragment-retries counterexample with RetryManager's zero-retry consequence. It no longer proposes merging only the raw baseline diff. | **Resolved for the stage A conclusion.** The eventual implementation must still prove coupled outputs, explicit defaults and precedence; none of those unbuilt paths is approved here. |
| T184-R2 | Medium | No | The task explicitly withdraws the earlier assertion that classification proved queue visibility. UX-005 now rules the display condition, and the submitted row code exposes the foreign reason visually while suppressing the shared application sentence. It preserves the ordinary cancellation's byte-detail fallback. | **Resolved for the stage A factual correction and visual branch.** The new accessibility defect is T184-R4. Full worker/manager/UI measurements of both break options remain T-184 acceptance work before eligibility or task completion is claimed. |
| T184-R3 | Medium | No | The task now distinguishes OptParseError from help/version SystemExit, and automatic config discovery from explicit config includes. It requires token admission before unrestricted parsing plus exception containment. | **Resolved for the stage A conclusion.** The future parser still needs tests of these side effects, including token variants; this documentation disposition does not certify an unbuilt admission layer. |
| T184-R4 | High | Yes | `QueueModel._detail` now draws the cancellation reason (`ui/queue_view.py:999–1009`), but `_whole_row` includes reason text only for FAILED rows (`ui/queue_view.py:1190–1197`). A CANCELLED row with the archive-stop reason exposes that text via DETAIL_ROLE and omits it from AccessibleTextRole. A screen-reader user still receives no explanation for the same stopped job, contrary to NFR-005 and the model's established whole-row contract. | **Open — current T-184 implementation.** Use the same cancellation-detail rule for accessible row text and verify foreign, default, empty and multiline reasons through the actual model roles. Keep the ordinary cancelled-row size behavior. |

### Independent evidence

An offscreen QApplication and QueueModel with one CANCELLED job, ErrorKind.CANCELLED and the
real ExistingVideoReached sentence gave these results through `model.data(model.index(0, 0), role)`:

```text
DETAIL_ROLE: Encountered a video that is already in the archive, stopping due to --break-on-existing
AccessibleTextRole: Job: https://example.invalid/item. Status: Cancelled. Total size unknown; progress cannot be measured. Size: 0 B of Unknown. Speed: Unknown. ETA: Unknown. Download as: best
reason included: True False
```

The shared constant is imported by the manager, job detail and queue, eliminating the former
independent literals. The ordinary cancellation retains its previous drawn detail; the foreign
reason is normalized to a single line. The new tests cover the visual branch but not its accessible
counterpart. No source or test file was changed for the independent reproduction.

The audit's counts are **65 typed + 93 hatch + 92 refused = 250**. The new consumer test checks
the phase-plan table against the audit, and passed with the existing audit guards. This supports
the count correction without treating that inventory as the future executable allow-list.

Fresh pin-level probes also reproduced an empty explicit-10 diff, RetryManager(None, callback)
reporting zero retries, help/version raising SystemExit, and an explicit temporary config
producing 37 fragment retries. The first RetryManager probe omitted its required callback; after
correcting that probe invocation, all four observations above completed successfully.

The live fragment-retry default is recorded in T-252 as requested; no retry-policy change is
silently included here. Default-deny is now the recorded maintainer ruling, with provenance in
the supplied request and `93651db`, not a reviewer-made product decision.

### Checks and limits

- Focused reveal/raise/taskbar/queue/audit/placement selection: **279 passed**, 11.11 seconds.
- Ruff lint and format checks passed (452 files); `mypy src` passed (67 files); bare mypy and
  `mypy --platform win32` passed (202 files each).
- `pytest -q -n 2 tests/unit tests/ui`: **4,299 passed, 22 skipped**, 253.69 seconds.
- Submitted-head CI: Linux unit/UI **4,299 passed / 22 skipped**, Linux integration
  **500 passed**, and the real Windows desktop suite **46 passed**. The Windows unit/UI
  partition failed the new host-dependent reveal test: **1 failed, 4,276 passed, 42 skipped**;
  disposition is [T347-R5](T-347.md#ci-completion-during-this-review--additional-gate-finding).
- An additional local serial `pytest -q` run was stopped after the completed CI gates made
  it redundant. Following SIGINT it exited **139 (segmentation fault)** during Qt item-view
  layout, without a pytest summary. It is **not** counted as a passing full run. The log has
  an invalid Python frame and does not establish a changed-source cause; no spontaneous
  application-crash or clean-interruption claim is made. Completed gates above remain the
  basis for the stated test results.

No Windows runtime tests or real external downloads were executed locally by this reviewer. Linux's type
check of Windows branches is not a desktop acceptance result. The build-head CI status and
broader modal-state verification are recorded in the contemporaneous T-343 review.

Keep T-184 In Progress. This review does not approve the parser, the value shape, migration
0013, option eligibility, the release or phase completion. The cancellation change remains part
of T-184; no separate bookkeeping task is created. Only this review and its index navigation
are edited, leaving task/status synchronization with their owner.

## 2026-09-18 — Accessible cancellation correction and negative assertions

**Reviewer:** Codex, independent of the implementer.
**Round:** Focused verification of T184-R4 and its test correction.
**Base:** `72a0979bc56a4f3cd91104080d07757f28f97807`.
**Source/test corrections:** `6e9ffb901ce10a9ee486cfada9dfcb82018f8eb1` and
`0cec26579532ec6dfa040a3ce91a9d4795cdb5be`.
**Checked source head:** `0cec26579532ec6dfa040a3ce91a9d4795cdb5be`, initially clean.
**Scope:** Cancellation explanation in the drawn and accessible queue row, and meaningful
assertions for the two cases that should remain quiet. No parser or migration is submitted.
**Verdict:** **Approved** for this cancellation-row correction. T-184 as a whole remains
In Progress; this is neither parser approval nor a completed full-suite/release gate.

| ID | Severity | Blocks this correction | Finding and evidence | Disposition/status |
|---|---|---|---|---|
| T184-R4 | High | No | `QueueModel._whole_row` now uses the same `_cancellation_detail` as the drawn detail. Tests read DETAIL_ROLE and AccessibleTextRole through the real model for a foreign reason, the application sentence, an empty reason and multiline text. | **Resolved.** Foreign reasons reach both roles; multiline reasons use the same normalization; default and empty reasons do not acquire the application's cancellation sentence. |

The interim negative assertion was vacuous because its empty-string branch always passed.
`0cec265` now directly asserts that `CANCELLED_BY_THE_APPLICATION` appears in neither role for
both silent cases. Independently appending that sentence to the spoken row in memory produces
**2 failed, 2 passed** in the four-case test; both failures are the intended negative assertions.
The unmodified cases pass within the **264-test** combined queue/reveal/shutdown/placement run.
Repository source and tests were not modified to perform the mutation.

At the checked head, Ruff lint and formatting passed, as did `mypy src`, bare mypy and
`mypy --platform win32`. These are Linux execution/type-check results. No new Narrator,
Windows-runtime, worker-option eligibility or end-to-end break-option observation is claimed.

The implementer's full-suite result remains **4,807 passed, 22 skipped, 6 failed**. The independent
format-fixture investigation and its limits are recorded in
[T-350](../TASKS.md#t-350--a-staged-format-fixture-races-a-real-probe-and-loses-its-format-control)
and the [contemporaneous T-347 review](T-347.md#2026-09-18--sealed-worker-correction-and-daemon-discovery-regression).
Those failures are not silently waived by this bounded cancellation approval.

Stage A's previously resolved factual findings retain their existing scope. Explicit-default
merging, token admission before parsing, parser side-effect containment, persisted shape,
option eligibility and migration 0013 still require their own implementation and review.
Only this record and its index entry change for this review; task/status synchronization stays
with its owner. Nothing was pushed.

## 2026-09-22 — Parser, request and preset implementation review

**Reviewer:** Codex, independent of the implementer.
**Task:** T-184.
**Round:** Initial comprehensive review of the newly built parser/request/preset stages.
The earlier measurement and cancellation reviews retain their narrower verdicts.
**Base:** `a073f1df856246f5e75bd1e2b0911fb1b2c2b187`.
**Head:** `51f90ab4982f85ce88b90e34de8235b8c3d9863e`, initially clean on `main`.
**Submitted commits:** `9a42c35`, `0644f17`, `fda0481`, `51f90ab`.
**Dependency inspection:** Admission, audit and destination-table work in
`f7d8ff9d08bd2eefba98c05ca54374b067f82a93..a073f1d`, plus the settings, queue persistence,
logging and worker consumers of the new fields. T-347 is outside this review.
**Platforms verified:** Linux, Python 3.14.7, pinned yt-dlp 2026.08.19; Windows branches
were type-checked only.
**Verdict:** **Changes requested.** Eight blocking findings remain in T-184. The green
unit/integration gate does not establish the new field's persistence, privacy or output contract.
The UI is explicitly unbuilt; neither this review nor seam tests establish dialog behavior.

### Findings

All source locations below refer to `51f90ab`; all findings are **Open**, owned by the
Implementer in T-184's correction batch. No separate tasks are needed for these defects.

| ID | Severity | Blocks approval | Finding and evidence | Required correction |
|---|---|---|---|---|
| T184-R5 | Critical | Yes | Persisted parsed values bypass admission and the application's safety settings. `core/models.py:423–458` checks only JSON-compatible shape; `ytdlp_adapter.py:800–801` then applies every supplied key except `writethumbnail`. A hand-edited preset with visible text `--continue` and parsed `geo_bypass=True` loads without a problem, survives queue JSON serialization/deserialization, and overrides the adapter's mandatory false value. An arbitrary `outtmpl` overrides the worker-supplied staging template by the same route. | Establish the admitted, semantically validated contract at persisted/request boundaries, including text/value agreement, excluded normalized values and application-owned destinations. Do not trust arbitrary stored pairs because the dialog would normally have parsed them. Cover settings load, queue restore and the options actually passed to yt-dlp. Preserve containment and `REQ-EXCL-002`/`SEC-003`. |
| T184-R6 | Critical | Yes | Hatch credentials reach logs unredacted. An admitted `--add-headers "Authorization: Bearer T184_REVIEW_CANARY"` reaches `FormatChoice`'s default repr (`core/presets.py:437–461`). Separately, saving and loading that preset produces a validation exception quoting its value (`core/models.py:444`); settings incorporates it into the reason that startup logs (`core/settings.py:1344–1347`, `app.py:1367–1370`). The actual `RedactingFormatter` leaves the canary intact after registering every secret returned by settings load. | Enforce `REQ-031` redaction through projections, validation failures, settings diagnostics and worker logging. Correct the false claim that every credential-bearing option is refused. `repr=False` on two models is insufficient; fixing the TOML writer alone must not leave malformed-input diagnostics or `FormatChoice` exposing values. Test the real formatter and caller arguments with synthetic secrets. |
| T184-R7 | High | Yes | A valid preset with any nonempty parsed pairs does not survive the real settings writer/reader. The newly preset-owned fields automatically enter `_PRESET_FIELDS`, but `_toml_value` (`core/settings.py:1463–1479`) writes each pair as a string. Saving `--continue` succeeds; reload rejects the string `"('continuedl', True)"` as a pair and omits the entire preset. | Add a deliberate codec for the complete supported value shape. Verify `settings.save` → `settings.load`, retained preset/default selection and unrelated settings, alongside queue JSON compatibility. In-memory `Preset` → `DownloadRequest` tests do not cover persistence. |
| T184-R8 | High | Yes | Invalid option values are accepted and silently dropped at the advertised edit-time seam. `hatch_options` catches parser failures and returns `{}` (`ytdlp_adapter.py:870–880`); `options_for` returns the original usable admission unchanged (`extra_options.py:250`). Both `--fragment-retries nope` and `--concurrent-fragments 0` produce `usable=True`, no refusals and no values. Adding valid `--fragment-retries 7` before an invalid concurrency value silently drops that intent too. | Return a value-validation refusal for the whole field, preserving safe error handling and a useful reason. The caller must distinguish empty valid input from failed parsing and prevent submission of the latter. Replace the current test expectation that merely returning no keys is success. |
| T184-R9 | High | Yes | Several admitted valid options cannot enter either model. The parser produces a `DateRange` for `--date`, callable objects for `--match-filters` and `--download-sections`, nested functions for `--retry-sleep`, and a set inside SponsorBlock postprocessors. `options_for` reports these usable, but `_as_option_values` rejects them as non-JSON values. | Define a declared, transport-safe parsed representation and its worker conversion for every admitted value family. Verify the entire route from `options_for` through models, preset/queue persistence and pickle to worker behavior. Do not pass upstream callables through the process contract or silently narrow the ruled option surface to make the model pass. |
| T184-R10 | High | Yes | Download options erase hatch postprocessors. `--split-chapters` and `--convert-thumbnails png` both parse to postprocessor specs and enter a request, but `ytdlp_adapter.py:837` overwrites them with `build_postprocessors(request)`, yielding `[]` for a plain request. `requires_ffmpeg` reads that same application-only list (`:925`), so it returns false for both. | Compose the admitted postprocessors with the application's chain under defined ordering and ownership, and derive the ffmpeg preflight from the resulting work. Assert effects on output files as well as dictionary contents, including the worker's handling of additional outputs. |
| T184-R11 | High | Yes | Requested retained files disappear at two stages. With embedding plus `--write-thumbnail`, the `EmbedThumbnail` spec omits `already_have_thumbnail=True` (`ytdlp_adapter.py:1009`), and the real postprocessor deletes the cover. Without embedding, the real worker downloads a JPG and info JSON, then discards both: `requested_sidecars` only recognizes subtitles (`worker.py:1318`), and successful cleanup deletes the unclaimed files (`:689–705`). It reports success with only the MP4. | Preserve the user's keep intent through postprocessing and through final output claiming. Extend the contained, collision-safe output-family contract to files the admitted hatch asks to retain; do not simply keep every staging intermediate. Cover keep/embed combinations, additional sidecar writers, collisions, missing outputs and failure cleanup through the real worker. |
| T184-R12 | High | Yes | The supposedly frozen parsed field shares mutable nested dictionaries and lists. `_as_option_values` retains `parsed` directly (`core/models.py:457`); requests and presets consequently share the parser result and each other's nested values. Mutating the original `player_client` list from `web` to `android` changes an already-created request. This violates the settings freeze in ARCHITECTURE §8. | Make the model's parsed representation deeply immutable and detach caller-owned state. Reconstruct mutable yt-dlp structures only at the adapter boundary. Cover mutation through the original parse, preset/request projections and restored values; an outer tuple or a shallow copy does not establish the invariant. |

### Independent reproductions

All probes used synthetic values and temporary directories. No real credentials, external
service downloads, desktop manipulation or excluded download behavior were exercised.
Repository source and tests were unchanged. Temporary diagnostic scripts are not durable
repository artifacts; the inputs, routes and decisive results are retained here.

**R5 — public settings load, then the queue codec and adapter.** Save an ordinary user preset
with `extra_options = "--continue"`, then set its TOML field to the following valid nested array:

```text
extra_option_values = [["geo_bypass", true], ["outtmpl", "/tmp/t184-not-written/outside.%(ext)s"]]
```

Call `settings.load`, `presets.to_request`, `_serialize_request`, `_deserialize_request`,
and `build_options(restored, <temporary downloads>/expected.%(ext)s)`, in that order.
The first two values below are from the public settings loader; the last values are from
that restored request's actual options:

```text
PUBLIC_LOAD problem None presets 1
PERSISTED_WORKER_OPTIONS text --continue geo_bypass True outtmpl /tmp/t184-not-written/outside.%(ext)s
```

The probe stops before downloading. It establishes a defeated application setting and a
replacement of the supplied output template, not a measured outside-directory write or an
executed command. The worker's `_extract` passes these built options to `YoutubeDL`.

**R6/R7 — settings persistence and the startup logging arguments.** Use `options_for(text)`
and `dataclasses.replace(BEST_VIDEO, name="review-preset", built_in=False, ...)` to construct
valid presets, put each in `Settings.presets`, then use `settings.save` and `settings.load`.
For `--continue`, save returns `None` and emits:

```text
extra_option_values = ["('continuedl', True)"]
```

Reload has zero user presets and a problem saying that entry is not a `(key, value)` pair.
Repeat with `--add-headers "Authorization: Bearer T184_REVIEW_CANARY"`. Register every
literal in `loaded.secrets` using `remember_a_path`, as startup does, and format a real
`logging.LogRecord` using startup's message and arguments:
`"settings: %s (%s)", (loaded.problem.reason, loaded.problem.path)`.
The retained output, with only the temporary pathname shortened, is:

```text
REAL_LOG_ARGUMENTS admitted True registered_secrets 0 canary_survives_formatter True
settings: 1 saved preset(s) could not be read and were left out:
preset 'review-preset': Preset.extra_option_values[0] must be a (key, value) pair, not "('http_headers', {'authorization': ' Bearer T184_REVIEW_CANARY'})" (<temporary settings.toml>)
```

Independently, both `repr(format_choice_of(request))` and its passage through `redact`
contain the same canary. This is a present failure of the credential-free argument.
No history persistence is claimed: History was withdrawn, and `FormatChoice` is currently
a rendering projection. Nor is storing user-supplied text in the queue itself the privacy
finding; this finding is about the explicit hatch log-redaction contract.

**R8/R9/R10 — the public seam followed immediately by model construction and options building.**
Each request uses the text and pairs returned together by `options_for`, a synthetic URL,
an absolute temporary output directory, selector `best` and template `%(title)s.%(ext)s`.

| Input | Observed result at the reviewed head |
|---|---|
| `--fragment-retries nope` | Usable, no refusals, empty pairs; request constructs and the option is absent. |
| `--concurrent-fragments 0` | Same silent acceptance and omission. |
| `--fragment-retries 7 --concurrent-fragments nope` | Usable, no refusals, all pairs empty, including the valid seven retries. |
| `--date 20260901` | Usable; `daterange` is `DateRange`; request raises `TypeError`. |
| `--match-filters duration>0` | Usable; `match_filter` is `function_with_repr`; request raises `TypeError`. |
| `--download-sections "*1-2"` | Usable; `download_ranges` is `download_range_func`; request raises `TypeError`. |
| `--retry-sleep exp=1:20` | Usable; `retry_sleep_functions` contains functions; request raises `TypeError`. |
| `--sponsorblock-mark sponsor` | Usable; postprocessor categories contain a set; request raises `TypeError`. |
| `--split-chapters` | Request constructs with postprocessors; built download postprocessors are `[]`; `requires_ffmpeg` is false. |
| `--convert-thumbnails png` | Same accepted-then-erased postprocessor and false ffmpeg result. |

`test_an_option_the_parser_refuses_adds_nothing_rather_than_raising` currently protects
R8's empty-result behavior; its passing mutation evidence does not demonstrate edit-time
refusal. The checks must assert the user's promised outcome as well as exception containment.

**R11 — real postprocessor, then a separate real worker download.** Generate a 0.2-second
MP3 and a 32×32 JPG with ffmpeg in a temporary directory. Take `EmbedThumbnail`'s spec
from `build_options` for `embed_thumbnail=True` plus parsed `--write-thumbnail`, and run
the pinned `EmbedThumbnailPP` with that spec on those files. Repeat on fresh files with
only the control `already_have_thumbnail=True` added:

```text
control_keep_flag False thumbnail_survived False media_survived True
control_keep_flag True thumbnail_survived True media_survived True
```

For the worker route, generate a tiny MP4 and cover in a separate temporary source directory,
serve them from a localhost HTML page with `og:image`, and call the production
`run_session(SessionKind.DOWNLOAD, ...)` with parsed
`--write-thumbnail --write-description --write-info-json`. All settings/cache roots and
output paths are isolated under the temporary directory. A wrapper around `_discard_staging`
only lists the directory before calling the unchanged cleanup function. No download,
postprocessor, output-claiming or result behavior is mocked. The result was:

```text
BEFORE_REAL_CLEANUP ['T184 local fixture (1).info.json', 'T184 local fixture (1).jpg']
REAL_WORKER admitted True exit 0 outcomes [Succeeded(job_id='review', output_path='<temporary downloads>/T184 local fixture (1).mp4', total_bytes=None)]
FINAL_OUTPUTS ['T184 local fixture (1).mp4']
```

The generic page fixture supplied no extracted description, so this real-worker capture
proves deletion of the JPG and info JSON only. A separate synthetic `claim_outputs`/cleanup
probe also discarded a present description file, consistent with the subtitle-only consumer.
This distinction matters: the real worker's successful outcome is evidence of missing
requested files, not merely an inference from a missing dictionary key.

**R12 — mutation after request creation.** Parse
`--extractor-args youtube:player_client=web`, construct a request from its returned pairs,
then assign `"android"` to
`dict(pairs)["extractor_args"]["youtube"]["player_client"][0]`.
Reading `dict(request.extra_option_values)["extractor_args"]` now gives:

```text
{'youtube': {'player_client': ['android']}}
```

### Design conclusions and remaining acceptance work

- **Destination discovery:** The generated table and regeneration test are useful drift
  detection. They are not proof that every option's conditional/coupled effect survives the
  adapter and worker. Explicit-default merging is implemented and covered; the findings above
  concern later losses and boundaries. No separate table-correctness finding is inferred merely
  from the generator's sampling method.
- **Probe options:** Applying extraction-affecting options to both probe and download is a
  sound reason to keep their format universes consistent. Moving the whole merge below the
  probe branch would lose that property. This does not approve unrestricted options on probes:
  application-owned probe and safety settings still need enforcement under R5, and each
  admitted output-affecting family needs coverage. No new product ruling is made here.
- **Migration:** Optional members in `jobs.request` JSON do not, by themselves, require a SQL
  schema migration. Existing blobs take the new defaults, as the codec and passing test show.
  The task owner should reconcile the migration-specific acceptance wording with backward
  compatibility and genuine queue/preset round trips; a ceremonial migration is not the fix
  for R7 or R9. T-048 still awaits a change that actually exercises a schema/value migration.
- **Unfinished acceptance:** The UI and complete worker/manager/row measurements for both
  `--break-on-existing` and `--break-per-input` remain in T-184. The earlier bounded cancellation
  approval does not supply those measurements. `--no-geo-bypass` currently returns the
  default-deny "no ruling" refusal; reconcile that with the older explicit safe-negation
  criterion when completing the suppressed-option dispositions. These open scope items are
  not silently waived by this review of the built stages.
- **Redaction scope:** Admission refuses `--verbose`; the observed privacy blocker does not
  depend on enabling it. After fixing R6, verification must still cover worker diagnostics,
  rather than treating a repr assertion as the whole logging contract.

### Checks and limits

| Check at `51f90ab` | Actual result |
|---|---|
| `ruff check .` | Passed. |
| `ruff format --check .` | Passed, 459 files. |
| `mypy src` | Passed, 69 files. |
| Bare `mypy`; `mypy --platform win32` | Both passed, 208 files each. |
| Activated-venv `python -m pytest -q tests/unit tests/integration`, with localhost sockets allowed | **3,425 passed, 19 skipped**, 488.76 seconds, exit 0. Includes the destination-table regeneration test. |
| Independent parser, persistence, logging, aliasing and output probes above | Reproduced the eight findings; these are additional checks outside the passing suite. |

An earlier reviewer invocation used the venv Python without activating its tool PATH and
ran inside a sandbox that disallowed local sockets. It ended **58 failed, 3,359 passed,
19 skipped, 8 errors** in 338.76 seconds. The failures included unavailable Ruff/mypy on the
subprocess PATH and local-socket `PermissionError`; that invocation is not counted as a
product gate. The corrected environment rerun above completed successfully. No shell pipeline
was used to substitute a trailing command's status for pytest's exit code.

The implementer's full-suite result at this head was still pending in the supplied handoff.
This reviewer did not repeat the full UI suite, perform Windows runtime acceptance, or run
real external downloads. The localhost worker and synthetic-file postprocessor checks have
only the scope described above. T-350's earlier full-suite failures are neither reproduced
nor waived by this unit/integration result.

### Readiness and coordination

Keep T-184 **In Progress** and return the built stages for correction. Address the eight
findings together, map each to its reproduction and correction test, and check sibling value
shapes and consumers. Demonstrate that removing the correction restores the failure; passing
checks that encode silent omission do not count as evidence of correctness.

Only this canonical record and its REVIEWS index entry are changed. Earlier dated review
content is preserved byte-for-byte. Source, tests, task/status records and ignored handoffs
are unchanged; synchronization remains with their owners. No push is performed or authorized
by this review. A green full suite would not resolve the independently reproduced blockers.
