# T-320 review

**Purpose:** Independent review of versioning and release/rollback instructions.
**Owner:** Reviewer
**Update when:** T-320 is reviewed or a finding receives a disposition.

## 2026-09-12 — Initial review (UTC)

**Reviewer:** Codex, independent of the implementer.
**Task:** T-320. **Round:** Initial.
**Base:** `59598de4fd7af2c9fe921bb99002761d7f01c766`.
**Head:** `2e2507b5cfe4e262fcfda9b954dbbf68c390636c`.
The version checker and most release instructions already exist at the boundary's
base; they are assessed because the task itself is explicitly submitted here.
**Platforms verified:** Linux; isolated release-version tree and temporary SQLite database.
**Verdict:** Changes requested.

### Findings

| ID | Severity | Blocks approval | Finding and evidence | Disposition/status |
|---|---|---|---|---|
| T320-R1 | High | Yes | The required release commit cannot pass the default suite. `tests/unit/test_version_tag_check.py:109` requires the checked-out version to be unpublishable; line 134 requires the real CLI to reject `v0.1.0`. In an archive of the assessed head, changing only `__version__` from `0.1.0.dev0` to REL-003's required `0.1.0` produces **2 failed / 17 passed** across the checker and skeleton suites. Both failures are those assertions; the CLI correctly accepts the release. | **Open.** Exercise development rejection with controlled input and make the live checkout/CLI contract valid for both permitted states. Verify the development tree and the actual release-version tree; do not bypass the release suite. |
| T320-R2 | High | Yes | `docs/RELEASE.md:113` promises that the application refuses a database migrated by a newer version, and tells users to reinstall the previous release. No such refusal exists. `persistence/db.py:migrate` skips migrations at or below the database version and returns; `connect` returns the writable connection. An isolated database at schema **11**, against this build's latest schema **10**, reopened successfully and accepted a committed write. The stated predictable loss of queue/history is also not what this path does. | **Open.** Correct the rollback procedure to the actual behavior and give a safe data recovery/backup route. Do not promise automatic refusal or automatic data reset without an implemented and verified guard. Any added product guard must be implemented within authorized scope and reviewed separately from the wording. |

### Checks and disposition

- The unmodified checker has **14 passing cases**; skeleton has **5**. They
  were included in the shared **133 passed in 11.94 s** run.
- The release-version probe used `git archive 2e2507b`, with only the version
  string changed and the archived `src` on `PYTHONPATH`. It ran the committed
  checker and skeleton tests, including the real CLI subprocess. The checkout's
  product source was not modified.
- The database probe created a temporary database through `db.connect`, set
  `PRAGMA user_version` to `db.latest_version() + 1`, reopened through
  `db.connect`, and committed one marker-table insert. Result: latest **10**,
  opened **11**, successful writes **1**. This establishes the missing refusal;
  it does not claim observed corruption of a real future schema.
- Ruff lint and whole-tree formatting passed; bare mypy and its Windows-platform
  run each reported no issues in **171 source files** before concurrent work
  appeared in the checkout.
- README contains no claim of yt-dlp parity. Keeping its truthful no-packages
  statement until publication is reasonable release sequencing; no finding is
  raised over that deferral. REL-003 records the scheme and first version, and
  CHANGELOG/SECURITY's publication-time steps are explicit.

T-324 still owns release workflow integration. Its absence is not treated as an
implemented workflow here. This review grants no release approval; task/status
coordination remains with the task owner.
