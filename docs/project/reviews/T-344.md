# T-344 review

**Purpose:** Independent review of progress during post-processing.
**Owner:** Reviewer
**Update when:** The progress implementation or installed-build observation changes.

## 2026-09-15 — Processing progress implementation review

**Reviewer:** Codex, independent of the implementer.
**Round:** Initial. **Base:** `682f71d`. **Implementation head:** `55dcf05`.
**Verification checkout:** `ea495166b59d3d76bd52524da7473df4af7bc513`, including T-345's chip wording.
**Platforms verified:** Linux with real ffmpeg and offscreen Qt. No new Windows execution.
**Verdict:** **Blocked on the required observation of a long conversion in the installed build.**
No blocking implementation defect established in this pass.

### Findings

| ID | Severity | Blocks approval | Finding and evidence | Disposition/status |
|---|---|---|---|---|
| T344-N1 | Note | No | The adapter depends on the pinned yt-dlp module's `Popen` lookup and `repeat+info` marker. Inspection of installed yt-dlp 2026.8.19 confirms that `real_run_ffmpeg` supplies one positional command, text mode and pipes, with neither input nor timeout. Its probe/version commands do not carry the marker. | Acceptable for the inspected dependency contract, with the real-conversion/canary coverage retained. This is not a general replacement for every `Popen.run` signature. |
| T344-N2 | Note | No | Post-processing uses the step fraction, preserves downloaded totals, and falls back to a moving bar with textual meaning. The timer updates processing rows at 10 Hz and stops after they leave that state or the model detaches. | No change requested. Unknown future hook names retain a stage label. |

### Checks and limits

Reviewed protocol validation, worker hooks and cancellation, both pipe readers and process
cleanup, queue-model state, delegate painting, and all new tests. The independent Linux
suite passed **4,721 tests, 22 skipped**, including the real composed MP3 regression;
Ruff and both mypy platforms passed. Shared commands and provenance are in [T-328](T-328.md).

An additional independent probe generated a 120-second local AAC file and ran the real
`FFmpegExtractAudioPP` through the wrapper. Raising the worker's `SessionCancelledError`
from the position callback propagated out, returned in **0.963 seconds**, left both
recorded processes exited (codes `0`, `-9`), and restored the previous `Popen` binding.
This checks real ffmpeg cancellation through the wrapper, not a person pressing Cancel
in the installed application. Local capture: `/tmp/tt-review-ea49516-ffmpeg-cancel.log`.

The handoff's Windows **166 passed, 3 skipped** is implementer-reported execution with
the change applied to `682f71d`; this review did not independently rerun it on Windows.
The long-conversion observation and UI cancellation remain on the draft acceptance walk.

The maintainer's recorded task rulings establish both the requested behavior and its
0.1.0 timing. No new dependency or architecture policy was introduced, so a separate
DECISIONS entry is not required merely to repeat that scheduling choice. T-345 separately
records the amendment to the chip vocabulary. The task/status owner should retain the
outstanding installed-build observation when updating coordination.
