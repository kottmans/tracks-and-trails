# T-328 release review

**Purpose:** Independent review of release preparation, followed by the candidate/publication gate.
**Owner:** Reviewer
**Update when:** Release preparation is reviewed, a finding is dispositioned, or the candidate
receives its separate release review.

## 2026-09-14 — Review of the 0.1.0 release-preparation commit

**Reviewer:** Codex, independent of the implementer.
**Round:** Initial review of the release-commit changes, not the full T-328 release review.
**Base:** `5d3f37df3b8edd7b80dd49a4967e3ff1b2c9370d`.
**Head:** `f22b2c7f743387b9d174fe806f48ce2b944046ef`.
**Validation checkout:** `07082ac086a37e8f2aa02463d0dd0f69d2dc9e0f`; source, packaging, tests,
project configuration and workflows are unchanged from `f22b2c7`.
**Verdict:** **Approved for release preparation.** Two Low documentation corrections remain in
T-328's existing completion work. **Publication approval is still pending** the separate candidate
review, its artifacts and all applicable release-gate evidence.

### Findings

| ID | Severity | Blocks this preparation approval | Finding and evidence | Disposition/status |
|---|---|---|---|---|
| T328-R1 | Low | No | CHANGELOG lines 26–27 say the SmartScreen warning cannot go away with more downloads; RELEASE repeats that reputation never accrues without a certificate. Microsoft's current developer guidance explicitly says unsigned files build reputation for each new version. REL-005 also contains the same incorrect factual premise and says EV/Artifact Signing removes the first-install warning, which current Microsoft guidance does not promise. | **Open — T-328 release-notes completion before publication.** Remove the permanent-warning sentence from CHANGELOG and RELEASE; qualify the warning as something Windows **may** show. If explaining reputation, say it can accumulate for this file, while each new unsigned build starts afresh. Append a factual correction to REL-005 without rewriting the dated decision or reopening the accepted unsigned-0.1.0/signing-before-1.0 choice. Make the eventual draft's actual body agree; no signing purchase or new signing decision is requested. |
| T328-R2 | Low | No | RELEASE's release-commit section places the README Install section in the release commit, while T-328 places it at publication. RELEASE's version sequence and REL-003 say the next commit bumps development, whereas T-328 and the current review request place `0.1.1.dev0` after publication. Leaving the README and bump out of `f22b2c7` follows the requested release sequence, but the instructions still disagree. | **Open — ordinary T-328 completion sync.** Make the maintained release instructions distinguish candidate preparation, publication with the README installation text, and the subsequent development-version bump. Append the corresponding timing clarification to REL-003. No early README claim, early development bump or new timing ruling is required by this review. |

Microsoft's [SmartScreen developer guidance](https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/smartscreen-reputation)
distinguishes file-hash reputation from publisher reputation, explicitly covers unsigned files,
and says EV no longer provides an automatic bypass. Its
[Artifact Signing FAQ](https://learn.microsoft.com/en-us/azure/artifact-signing/faq) likewise describes
reputation accumulating for a signed file, rather than a guarantee of no prompt. These are factual
corrections to the explanation, not a reconsideration of the maintainer's distribution choice.

Suitable replacement wording for the disputed explanation is: **This installer is unsigned, so
Windows may show a SmartScreen warning. If it appears, choose More info, then Run anyway. Each new
unsigned build must establish its own reputation.** Keep the existing observed prompt text and
the planned signing statement. Do not promise a particular download count, time or universal
Windows prompt behavior.

### Release claims and sequencing

- **The version has one source.** `__version__` is `0.1.0`, and Hatch reads that file. The tag
  checker accepts `v0.1.0`. The changelog extractor returns this release's body without leaking
  its heading; the updated subprocess test also retains the missing-version refusal. Replacing
  the old missing-file expectation is necessary now that CHANGELOG exists.
- **Both artifact names follow the actual build expressions.** Substituting `0.1.0` into
  `OutputBaseFilename` in the Inno script produces `Tracks-and-Trails-0.1.0-setup.exe`.
  `packaging/build_appimage.sh` produces `Tracks_and_Trails-0.1.0-x86_64.AppImage`. The workflow
  uploads these outputs and creates SHA256SUMS. This verifies names and wiring, not successful
  creation or digests of a yet-unbuilt 0.1.0 candidate.
- **The default Windows install description is supported.** The script uses lowest privileges
  and the per-user program directory; the prior Sandbox evidence and manual session support the
  ordinary double-click path. Explicit administrative `/ALLUSERS` remains an existing alternate
  invocation. Windows release packaging includes ffmpeg; Linux resolves the distribution's copy.
- **The Linux floor is consistent with the build.** The workflow uses
  `python:3.14-slim-bookworm`, matching the documented glibc 2.36 baseline.
  [Debian 12's libc6](https://packages.debian.org/bookworm/libc6) is 2.36;
  [Ubuntu 24.04's](https://packages.ubuntu.com/noble/libc6) is 2.39; and
  [Ubuntu 22.04's](https://packages.ubuntu.com/jammy/libc6) is 2.35. The examples therefore match
  the floor. This is a compatibility requirement, not a claim that every named distribution
  received a fresh candidate test. The system-CA and Linux-ffmpeg dependencies follow REL-007
  and OPS-001 and the existing packaging boundary.
- **The queue claims describe implemented behavior.** Reordering has named move actions and
  persisted ordering; crash tests kill a writer, reopen the database, and recover in-flight jobs.
  The claim concerns the queue surviving termination, not continued downloading while the app
  is stopped. The feature summary does not promise yt-dlp option parity. Narrator, macOS and
  single-instance/file-handler omissions remain disclosed.
- **Schema and support records are appropriate to the candidate.** Twelve migrations exist;
  `schema_versions/v12.sql` is present in `f22b2c7`, contains actual serialized job rows and is
  exercised by the historical-fixture test. No fixture regeneration is needed. SECURITY's latest
  0.1.x/minor support policy matches REL-003 and RELEASE; `main` remains the development line.
- **README and development-version changes belong to the stated later steps.** Their absence
  from this commit is accepted. The stale timing instructions are T328-R2, not evidence that the
  unpublished release should already be advertised as available.

### Independent verification

| Check | Actual result |
|---|---|
| Full Linux default suite at validation head, activated venv with localhost socket permissions | **4,610 passed, 22 skipped, 17 warnings; 175.15 s**, one complete run, exit 0. Log: `/tmp/tt-review-07082ac-pytest.log`. |
| Ruff lint and whole-tree formatting | Passed; 423 files already formatted. |
| Bare mypy; bare mypy with `--platform win32` | Both passed; 192 source files each. |
| Version checker | `v0.1.0 matches __version__ 0.1.0`, exit 0. |
| Build-name and fixture checks | Both evaluated artifact-name expressions occur in CHANGELOG; schema 12 is latest, and the frozen v12 fixture is byte-identical at the release commit and validation head. |
| GitHub check-runs API at exact `f22b2c7` | Linux, Windows desktop, both frozen jobs, STARBASE coverage, trailers and task placement completed successfully. Linux orphans is **skipped**, consistent with the existing recorded policy; it is not reported as passed. |
| Supplied CI run, logs inspected directly | [Run 34860342232](https://github.com/kottmans/tracks-and-trails/actions/runs/34860342232): Linux components **4,134 passed / 22 skipped** and **476 passed**; Windows components **4,113 passed / 41 skipped** and **471 passed / 5 skipped**. Windows platform types also passed on 192 files. These are CI executions, not a new Windows session by the reviewer. |
| Live repository state, read-only API | Releases and tags endpoints both returned **[]** during this review. The workflow source uses `gh release create --draft` and checks `isDraft`; it does not publish. |

The implementer's earlier **4,609 passed, 1 failed, 22 skipped** run was a real failure of the
stale changelog test, followed by a targeted 55-pass rerun after correction. It is not described
as an expected skip or one clean full run. The complete reviewer run and exact-commit CI above
now provide independent passing evidence. No mutation rerun was needed for this small test
adaptation; the existing extraction/refusal regressions remain in the full suite.

### Remaining release boundary

T-327 and T-333's manual-evidence dispositions are recorded separately. The actual workflow-built
candidate, T-326's remaining artifact checks, T-328's item-by-item publication review, README
publication update and first live published-update response remain required in their existing
steps. A successful draft workflow is not publication approval. A tag would be public even while
the GitHub Release stays a draft. This review created no tag, built no candidate, published
nothing and pushed nothing; it does not require replacing or moving a published tag.

The two Low findings stay within T-328's already-scheduled release work. They do not reopen the
session review, add standalone tasks, or require a second release-preparation review merely to
keep this commit approved. The eventual publication review must inspect the actual corrected
notes and the candidate evidence.

Review-document checks pass: `git diff --check`, 149 local file links and the September 14
cross-review anchors. The historical migration verifier preserves all 381 entries in 105 files,
totaling 2,403,546 original bytes. The review commit's message check also passes.

## 2026-09-14 — 0.1.0 candidate and release-gate review

**Reviewer:** Codex, independent of the implementer.
**Round:** Initial comprehensive candidate/publication review; focused verification of the two
previous release-preparation corrections.
**Base:** `d0cfcf2`. **Evidence head:** `00c422c`.
**Candidate:** `3c011b818fed387b06944b1ec7831a18d71ab699`, annotated tag `v0.1.0`.
**Scope:** `ff95306`, `412e93b`, `80c4612`, `095b788`, `3c011b8`, `00c422c`; complete candidate
evidence and TESTING §8. App source is unchanged from the prior review; the new DRM finding comes
from a deterministic counterexample to the submitted mandatory-test claim.
**Verdict:** **Changes requested — do not publish this candidate.** T328-R3 defeats the explicit
DRM retry safety constraint; T328-R4 leaves a required manual gate unsatisfied. T-322 and T-324
are approved within their installer/workflow scope. T-326 needs the safety correction and updated
candidate evidence. Existing startup and Narrator exceptions are retained, not reopened.

### Findings and earlier corrections

| ID | Severity | Blocks release approval | Finding and evidence | Disposition/status |
|---|---|---|---|---|
| T328-R1 | Low | No | `ff95306` corrects CHANGELOG/RELEASE to say Windows may warn and each unsigned build establishes reputation; the dated REL-005 amendment corrects both the permanent-warning and prompt-free-signing premises. The actual draft body matches the corrected section. | **Resolved.** Existing unsigned-release/signing-before-1.0 ruling stands. |
| T328-R2 | Low | No | RELEASE and the dated REL-003 clarification agree on candidate corrections at `0.1.0`, README installation text at publication, then `0.1.1.dev0`. | **Resolved.** No premature README publication or development bump is required. |
| T328-R3 | Critical | Yes | `ui/main_window.py:1393` captures an offered Retry in an enabled QAction. `ui/queue_view.py:1832` routes that old ordinary-row action without checking the current failure. `downloader/manager.py:2047` and its persistence callback allow every FAILED job. A menu opened at NETWORK remains usable through a subsequent attempt ending DRM_PROTECTED, and activation requeues the DRM job and requests a download. This is the same forbidden stale-action class already guarded for groups, not an unreachable direct-manager call. | **Open — correct before candidate approval in T-328's current correction work, with T-326 evidence refreshed.** Enforce current failed-job retryability at the authoritative manager transition/admission boundary, including a write queued behind other state changes. Preserve the accepted CANCELLED → Queue again behavior. Add a regression through the stale real menu action and audit sibling manual retry routes. Correct the evidence/STATUS claim and proposed 0.1.1 deferral. |
| T328-R4 | High | Yes | TESTING §8 item 6 and T-328 scope 2 require all nine REQUIREMENTS §11 criteria manually verified and recorded on both platforms. The submitted record explicitly lacks that candidate matrix; especially whole-app kill/restart and unsupported-URL/error/log isolation have no candidate human observation. Other partial/older checks do not constitute all nine. | **Open — complete T-328's existing acceptance sitting and record each criterion/platform against the candidate being approved.** The matrix below identifies reusable evidence and missing observations. This review does not waive the required manual scope. |

The SmartScreen correction agrees with Microsoft's
[developer guidance](https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/smartscreen-reputation)
and [Artifact Signing FAQ](https://learn.microsoft.com/en-us/azure/artifact-signing/faq), reread for
this review. No particular reputation threshold or first-install bypass is promised. The original
decision prose remains present with clearly dated additions; REL-003 also adds an inline pointer
to its clarification. T-327 and T-333's moved bodies are byte-identical after their changed status
line and cite the actual `468728a` approval.

### T328-R3: independent counterexample and correction boundary

The ordinary-row context menu uses nonmodal `popup()`, so the application's retry timer continues
while it is open. A NETWORK failure may automatically retry and then discover DRM. Updating the
row correctly removes Retry from a newly offered menu, but neither updating the row nor delivering
the failure closes or disables the existing QAction. Group retry separately rechecks each member;
ordinary retry does not.

The deterministic offscreen probe uses the existing `FakeQueue`/`make_job` test helpers, a store
implementing `requeue_at_end`, the real `DownloadManager`, and the real `MainWindow` with
`retry=manager.retry`. Only worker admission is replaced by a recorder to prevent spawning or
network activity. Its sequence is:

1. Store a FAILED/NETWORK job, ask the real queue view for its verbs, and open
   `window._show_row_menu(job.id, offered, True)`. Retain `rowVerb_retry` from that real QMenu.
2. Deliver QUEUED and RUNNING state updates, then FAILED/DRM_PROTECTED, representing the next
   automatic attempt. Emit `job_changed` for each state and `job_failed` for the final failure;
   process Qt events throughout. The old menu remains visible/enabled through every update.
3. Verify the fresh row offers no Retry, activate the retained QAction, and inspect the actual
   manager's persisted transition and admission request. Require the job to stay FAILED with no
   admission. That assertion **fails** at `00c422c`:

```text
Fresh row offers Retry: False
Old menu visible/enabled: True True
After old menu action: queued drm_protected admissions [('stale', <SessionKind.DOWNLOAD: 'download'>)]
AssertionError: stale ordinary-row menu retried a DRM failure through the real manager
```

The same probe with a temporary in-memory wrapper rejecting the current FAILED/DRM job before
calling the manager passes: status remains `failed drm_protected`, admissions `[]`. This control
is diagnostic, not a production correction or evidence that one entry check covers write races.
The first instrument attempt had the wrong signal arity and failed before exercising behavior;
after correcting that instrument, the failing baseline and passing guarded control were both
rerun, including the intervening QUEUED/RUNNING notifications. Logs and scripts are retained in
`/tmp/tt-review-stale-drm-menu{,-guard}.{py,log}` for this session.

No DRM service was contacted, and no media decryption is claimed. The demonstrated defect is
admission of a new attempt after the application already classified the job DRM_PROTECTED,
contrary to SEC-001 and TESTING §7. Workers still check DRM when an attempt executes. That later
check does not satisfy the explicit no-retry/no-bypass-path requirement. Deferring this to 0.1.1
is therefore not approved. The correction must test the actual manager, the old UI action,
non-retryable failure variants and pending writes, and retain ordinary retry and explicit
cancelled-job Queue again positive controls. UI-only refresh is insufficient as the final guard.

### TESTING §8, item by item

| Item | Linux | Windows | Assessment and evidence |
|---|---|---|---|
| 1 — Ruff and types | Pass | Pass in candidate CI | Exact candidate CI is green; independent head Ruff/format and both type-platform runs also pass. |
| 2 — Full default suite | Pass | Pass in candidate CI | Exact candidate CI logs and the independent full local run are quantified below. These results do not include the new failing stale-menu probe. |
| 3 — Network suite at the pin | Pass | Pass | Retained candidate outputs: Linux 2 passed/2 Windows-only skips; Windows 2 passed. Baseline is 2026.8.19. No fresh network run by reviewer. |
| 4 — Every mandatory area proven | **Fail** | **Fail: shared application path** | All named tests exist and pass, but the stale ordinary-row DRM retry is unguarded and untested (T328-R3). Settings freeze is adequately proven by the composed test; T326-R5 is nonblocking test accuracy cleanup. |
| 5 — Prior released database migration | N/A | N/A | First release. Frozen v12 data exists and migration tests pass; the next release must use it. This is not a waiver of in-tree migration coverage. |
| 6 — Nine manual MVP acceptance criteria | **Incomplete** | **Incomplete** | T328-R4 and the criterion matrix below. Automated/older evidence is not a complete candidate sitting. |
| 7 — Clean machine, no Python/toolchain | Pass, bounded to report | Pass, bounded to report | `linux-0.1.0.md`: Ubuntu 24.04 container prerequisite refusal check, extracted/run AppImage probes and offscreen launch. `windows-0.1.0.md`: clean Windows Sandbox, installed windowed application. Both reports name the draft hashes. Neither is evidence for every supported OS version. |
| 8 — Frozen launch/download/cancel/exit | Pass | Pass | Workflow probes plus real clean-machine downloads, and the recorded maintainer answers for cancellation with another transfer completing and File → Quit leaving nothing running on both draft artifacts. These specific human answers are attributed to the supplied record, not to reviewer observation. |
| 9 — yt-dlp purity | Pass | Pass | All four artifact gates pass on both release payloads in the actual workflow. Independently rerun on the extracted downloaded AppImage. |
| 10 — Frozen yt-dlp update path | Pass within established probe scope | Pass within established probe scope | Production install/extract/child resolution/revert exercised with a synthetic wheel/index/transport; Linux clean machine repeats it. This is the accepted offline artifact probe, not a live upstream-package or GUI update test. |
| 10a — Bumped-baseline canary | Pass | Shared canary; Windows covered at pin by default CI | Run 34885909896 at candidate: 2026.8.19, 4,595 passed/43 skipped/14 explicitly deselected. The canary workflow itself runs on Linux; no Windows canary is claimed. |
| 11 — Dynamic Qt | Pass | Pass within PE-gate scope | Actual release artifact gate, plus independent Linux rerun. Windows checks real bundled Qt DLLs and binding imports; it does not observe every runtime DLL resolution. |
| 12 — Licence texts | Pass | Pass | Release artifact gates; Windows includes ffmpeg licence, Linux uses the external distribution copy. |
| 13 — Artifact/repository secrets and paths | Pass within scan/inspection scope | Pass within scan/inspection scope | Actual payload gates, independent AppImage rerun, manual inspection of the changed source/records and bounded current-tree scan described below. Retained historical path spellings are not rewritten. |
| 14 — Cold start | Accepted 0.1.0 exception | Accepted 0.1.0 exception | REL-008's dated amendment accepts the previously measured numbers. No new cold-after-reboot measurements are claimed. |
| 15 — Windows manual session | Windows-specific item | Pass with accepted Narrator exception | T-327's approved record at 468728a, with the T-339 exception. It does not supply all of item 6. No Windows 11 or physical-machine verification is implied. |

The candidate records are [T-326's evidence](../evidence/2026-09-14-T326-release-candidate-0.1.0.md),
[Linux clean-machine report](../evidence/linux-0.1.0.md),
[Windows Sandbox report](../evidence/windows-0.1.0.md),
[Linux network output](../evidence/2026-09-14-T326-network-linux-0.1.0.txt),
[Windows network output](../evidence/2026-09-14-T326-network-windows-0.1.0.txt), and
[approved Windows session](windows-manual-session-2026-09.md#2026-09-14--focused-review-of-the-session-record-corrections).
The successful machine results are accepted for this candidate's bytes; they cannot automatically
be relabelled as results for a changed candidate.

### Item 6: record all nine, per platform

For each row, record date, OS, candidate hash, action and observed result. An existing observation
may be cited where it actually establishes the criterion; do not repeat work just to fill a second
form. The same remaining actions can be combined into one sitting per platform after the blocking
code correction. Use media the maintainer is entitled to download and a harmless unsupported URL.

| REQUIREMENTS §11 | Existing evidence and its limit | Needed for candidate closure |
|---|---|---|
| 1 — Default preset → Start → playable file in configured folder | Actual downloads and the maintainer's finished companion transfer on both platforms. The record does not explicitly establish the entire beginner path and playback for both candidate artifacts. | Record paste/default/Start, output location and successful playback on each platform. |
| 2 — Format table agrees with `yt-dlp -F` | T-107's August 7 comparison is Linux at an older baseline. | Compare the same URL and baseline on both candidate platforms; record the displayed formats and agreement. |
| 3 — Three concurrent downloads, accurate independent progress, responsive UI | Candidate sitting names two jobs; automated tests supply stronger concurrency coverage but are not this manual observation. | Run three together and record independent progress plus usable UI on each platform. |
| 4 — Cancel within 2 s, no orphan, no partial shown complete | Candidate human cancel/other-job/quit observations and automated timing/tree tests are useful. The human record does not explicitly establish all clauses. | Record cancellation timing and resulting process/file state; carry forward the existing unaffected-companion observation where applicable. |
| 5 — Whole app killed mid-download, queue intact/recoverable after restart | Strong automated recovery tests; **no candidate human observation**. | Terminate the application itself during a transfer, restart and record intact jobs and recoverable state on both platforms. |
| 6 — Unsupported URL, extractor's message, copyable log, other job unaffected | Automated error/isolation coverage; **no candidate human observation**. | While another download runs, submit an unsupported URL, compare the displayed extractor message, copy the log and record the other job continuing. |
| 7 — Worker forcibly killed, app survives | Real process-kill tests pass. No candidate human observation is recorded for this separate operation. | Terminate one worker and record the application remaining responsive on each platform. |
| 8 — Every interactive control keyboard reachable/operable | Windows desktop accessibility tests, tab-order evidence and earlier manual observations are partial evidence. They are not a complete two-platform candidate control walk. | Record the candidate keyboard walk, including row context actions and dialogs; the separate accepted Narrator exception stays in force. |
| 9 — Full suite/Ruff/mypy | Independently inspected successful exact-candidate CI on both platforms and complete local verification. | **Verified by this review of machine results.** No redundant human rerun is required merely to tick this row. |

This is the requested ruling on satisfying item 6: retain valid evidence, complete the missing
interactive observations, and record all nine against the candidate being approved. No desktop
sitting was performed by Codex in this review. Changes to the documented manual scope require
an explicit maintainer decision; cancellation of T-212 did not remove it, because T-328 inherited it.

### Independent verification and provenance

| Check | Actual result |
|---|---|
| Local full default suite at `00c422c` | **4,628 passed, 22 skipped, 17 warnings; 201.98 s**, one complete run, exit 0. `/tmp/tt-review-00c422c-pytest.log`. Candidate source, packaging and tests are identical to head. |
| Ruff lint / whole-tree formatting | Pass; **427 files** already formatted before this documentation-only review. |
| Bare mypy / bare mypy `--platform win32` | Both pass; **192 source files** each. |
| Version and shell checks | `v0.1.0` matches; mismatching `v0.1.1` exits 1 with the disagreement; both changed shell scripts pass `bash -n`. |
| Exact-candidate CI logs | [34880640426, attempt 2](https://github.com/kottmans/tracks-and-trails/actions/runs/34880640426): Linux 4,152 passed/22 skipped plus 476 passed; Windows 4,131 passed/41 skipped plus 471 passed/5 skipped. Relevant jobs succeed; Linux orphans is skipped under existing policy. |
| Release run and live draft | [34881606168](https://github.com/kottmans/tracks-and-trails/actions/runs/34881606168): all four jobs succeed. Authenticated API enumeration: release `388629995`, draft true, prerelease false, three expected assets. Draft body equals the corrected changelog section after normalizing line endings/edge whitespace. |
| Canary logs | [34885909896](https://github.com/kottmans/tracks-and-trails/actions/runs/34885909896): baseline 2026.8.19; **4,595 passed, 43 skipped, 14 deselected**, 23 warnings, 1,394.67 s. |
| Asset identity | Independently hashed each existing downloaded asset and compared size/digest with authenticated GitHub metadata. Installer: `aafc574cacdab3493b1b67ffe271efed5065d8d399b9020e65e5b834b86082f7`; AppImage: `566c24a19f43a3b7d99b1624a8c6856a9f2e7c9cae9de8973f883b785b7a4965`; SHA256SUMS: `355103902b81fc7caeee862934f42ab0943f1a31bd5b5209a8b1ae2d309f33bd`. This is a hash check of downloaded copies, not a new reviewer download. |
| Actual AppImage payload | Extracted a separate copy under `/tmp/tt-release-review-00c422c`; reran `packaging/artifact_gates.py` against it. All four gates pass. No new clean-machine or Windows execution is attributed to this check. |
| Workflow negative controls | Seven regression functions pass current source and reject their corresponding pre-fix source. Pipeline checks reject both older scripts. Details in the T-324 review. |
| Mandatory enumeration | All 38 named references resolve; tests pass in default suites. The new ordinary-row stale-menu probe fails; its guarded diagnostic control passes. These results are separate from the committed suite. |
| Repository inspection | Inspected the bounded diff and retained records; scanned all **577 tracked files** for private-key headers, common credential-token signatures and credential-bearing filenames. No flags. This is a bounded current-tree inspection, not a proof over all Git history or arbitrary secret formats. Existing dated provenance paths remain intact under AGENTS §6. |

### README draft, correction scope and readiness

A separate complete publication README and applicable patch are prepared at
`/tmp/tt-release-review-00c422c/README.md` and `README.patch`. They provide the actual artifact
names, SmartScreen steps, Linux/system-CA/ffmpeg requirements, the approved AppImage menu choices
and two-command launcher, with source-install instructions retained. The two launcher commands
were executed against a disposable directory containing spaces: the copy is executable, the
Desktop Entry validates, and its Exec points to that copy. The check used a task-specific variable
rather than changing HOME. No repository README publication claim was made. The draft should be
reviewed now and applied with publication only after release approval; any replacement asset
names/version must be reflected in it.

T-324's workflow fixes and T-322's workflow-built artifact are approved in their own records.
T328-R1/R2 close; T326-R5 stays in ordinary completion work. T328-R3 and R4 remain in this release
review until corrected and independently verified. Critical/High correction passes are allowed
by TESTING §14 without another pass-budget authorization. No new task is created merely for these
findings. The task/status owner must reconcile the current safety/readiness wording with this
verdict; this reviewer changes only the assigned review records and their index.

After a correction, preserve these original artifacts/runs as history and review the actual
replacement candidate and applicable repeated gates. Choosing or moving a tag remains the
maintainer's release operation. Publication, README application, the subsequent development bump,
Phase 5 exit bookkeeping and the first live published-update check remain T-328's later steps.
This review does not publish, retag, or push anything.

Review-document validation: Ruff lint and formatting pass (427 files); `git diff --check`,
175 local file links, the new cross-review anchors and all prior review prefixes pass. The
historical migration verifier preserves 381 entries in 105 files, totaling 2,403,546 bytes.
The publication README patch applies cleanly in check mode; its final Desktop Entry validates
without warnings. No application suite was repeated for the documentation-only review additions.

## 2026-09-14 — Focused release review of candidate 93c7357

**Reviewer:** Codex, independent of the implementer.
**Round:** Focused correction pass for T328-R3/R4 and T326-R5, plus initial reviews of the
new T-340 Diagnostics and T-341 Fedora corrections.
**Base:** `2fb9c16`. **Head:** `4f7c7a5`.
**Candidate:** `93c7357d22fcdf9066e28161c3d6380896caa9c3`, tag `v0.1.0`.
**Range:** `246dcdf`, `3f41813`, `93c7357`, `4f7c7a5`.
**Verdict:** **Changes requested — publication remains blocked.** The original stale queue-menu
case is fixed, but T328-R3's no-DRM-retry constraint still fails through an obsolete automatic
retry deadline and through both Add-dialog retry actions. T328-R4's human acceptance walk and
current-candidate item 8 sitting are unfinished. Machine checks alone do not close these gates.

### Finding dispositions

| ID | Severity | Blocks approval | Current evidence and disposition |
|---|---|---|---|
| T328-R3 | Critical | Yes | **Open — partially corrected.** Call-time and write-time manager checks and ordinary-row verb revalidation pass the original reproduction. Two independently reproduced sibling paths still retry DRM: a NETWORK deadline surviving a manual retry that ends DRM, and Add URLs' row/bulk retry replacing the failed staged job with a fresh probe. Complete the safety correction in this existing review; details below. The submitted assertion that staged reads are exempt because no download is admitted does not satisfy SEC-001's explicit never-retried rule. |
| T328-R4 | High | Yes | **Open.** No new two-platform human acceptance results were supplied. The ignored checklist identifies the candidate and hashes but is a procedure, not an execution record. Retain the prior nine-criterion matrix and complete the missing observations against the candidate being approved. Include item 8's companion completion and normal exit. |

T326-R5 is **Resolved**; T326-R6 is Low completion wording/attribution. T-341's reported Fedora
failure is **Approved** within the conservative location-discovery boundary, with Low T341-R1
accuracy cleanup. T-340's Diagnostics implementation is **Approved**, while the task itself is
**Blocked** on its explicit manual-observation criterion. T-322/T-324's approved implementation
boundaries remain; the unchanged installer/workflow and fresh candidate reports add execution
evidence, not publication approval.

### T328-R3: what is fixed

`DownloadManager.retry` now evaluates `_may_run_again` on entry and again when its queued write
runs. FAILED jobs with a non-retryable kind are refused, while missing-kind compatibility and
CANCELLED → Queue again are preserved. The queue view rechecks its current offered verbs before
routing Retry, Start again or Queue again. The original offscreen real-menu/manager instrument
now leaves the DRM job FAILED with no admission. The new integration checks cover direct refusal,
a changed failure between initial read and write, and a retryable positive control.

Independent mutations confirm the effective guards: removing the write-time check fails the
changed-before-write regression; removing the view's recheck fails the view-route regression.
The reported surviving call-time-only mutation is correctly explained by the write-time refusal;
it does not invalidate that final guard. The helper's CANCELLED status exception remains distinct
from a FAILED job carrying the non-retryable CANCELLED error kind. Existing Queue again behavior
is covered by the complete suite.

### T328-R3: the old network deadline still retries a DRM failure

`retry()` does not retire an existing `_retry_at` entry. After a manual retry produces DRM,
`_schedule_automatic_retry()` refuses to add a new deadline but leaves the old one intact.
`_perform_due_retries()` checks only `status is FAILED`, including inside its queued write, and
therefore consumes that old deadline against the new DRM failure.

Independent real-process reproduction at `4f7c7a5`, using the actual manager and an in-memory
repository, with a derived worker that emits NETWORK once and DRM thereafter:

1. Start the job and wait for its NETWORK failure and worker release. Capture its real two-second
   automatic retry deadline.
2. Call the legitimate manual retry while the job is still NETWORK. Its second worker fails DRM
   and is released **before** that deadline.
3. Let the application's timer run past the original deadline. A third actual worker starts,
   with no further user action, and the stored automatic-attempt count increments:

```text
NETWORK failure scheduled an automatic retry
Manual retry finished DRM before old deadline: True
Old deadline still scheduled: True
Worker results: ['network', 'drm_protected', 'drm_protected'] automatic attempts: 1
AssertionError: an obsolete network timer retried the DRM failure
```

The diagnostic control removes only the superseded deadline before the manual retry: both the
initial NETWORK attempt and legitimate manual attempt still run, but the third does not;
automatic attempts remains 0. No DRM service or real network was contacted. This is a direct
counterexample to the mandatory **never auto-retried** property, not only a hidden UI affordance.
The instrument and both logs are under
`/tmp/tt-release-review-93c7357/stale-automatic-retry{,-guard}.log`, with
`stale-automatic-retry.py` as the executable instrument.

**Correction required:** retire obsolete retry plans when an intervening attempt supersedes them,
and enforce the automatic policy against the current failure at execution/write time. A manual
retryability predicate is broader than the automatic NETWORK/transient-read policy and is not a
substitute for it. Test the NETWORK → manual retry → DRM sequence with real worker admissions,
including a deferred write, and audit the other permanent failures and transient-read variant.

### T328-R3: Add URLs retries its permanent staged failures

`AddUrlDialog.row_menu()` offers **Read this URL again** for every FAILED line;
`_retry_row()` discards its old job ID and stages a new probe. The bulk `retry_failed()` action
does the same for all failed lines. Neither retains/checks the typed failure before discarding
it. This is a real production outcome for a probe: `downloader/worker.py:572–583` detects DRM
in its PROBE branch and returns `_drm_failure`, whose contract also says the UI offers no retry. The new manager retry guard is bypassed because these routes call `stage()` instead of
`retry()`.

Independent offscreen reproduction uses the real AddUrlDialog, real DownloadManager, existing
FakeStore/FakeSink test helpers, and a real spawned worker that reports a derived DRM failure.
After the first worker exits, its staged job is DRM_PROTECTED and the row displays that class.
Activate the actual row QAction, and independently repeat with the actual bulk button:

```text
row-menu first failure: drm_protected: Derived DRM refusal
menu action enabled: True
row-menu worker runs: 2 new job: True
bulk-button first failure: drm_protected: Derived DRM refusal
bulk action enabled: True
bulk-button worker runs: 2 new job: True
AssertionError: DRM staging failures started another real probe through ['row-menu', 'bulk-button']
```

Each worker records its own invocation, so this proves another probe ran, not merely that a
callback was invoked. A temporary control suppressing the retry when the existing staged job's
kind is DRM keeps both routes at one worker and the same job ID. Instrument/logs:
`/tmp/tt-review-staged-drm.py`, `/tmp/tt-review-staged-drm.log`,
`/tmp/tt-review-staged-drm-guard.log`. No DRM site was contacted or decrypted.

The supplied sibling audit explicitly excludes this route because staged items are not durable
queue jobs and the retry admits only a probe. That observation about persistence is correct;
the safety conclusion is not. SEC-001 calls DRM_PROTECTED permanent and never retried, and the
current automatic-probe path already respects that prohibition. Giving the same failed item a
new ID does not satisfy it.

**Correction required:** preserve the typed failure and enforce the policy for both existing
staging retry actions, including stale menus and mixed bulk selections. Retain positive controls
for legitimate probe retries. Update the sibling audit and T-326 evidence to distinguish the
fixed queue path from these remaining paths. The correction belongs to the current Critical
finding; no extra task or pass-budget authorization is required under TESTING §14.

### New implementation reviews and their limits

[T-340](T-340.md#2026-09-14--diagnostics-implementation-review) accepts Diagnostics in both
queue menus and on a failed Add line. The placement supplies the missing log access under UX-003,
including keyboard access. The log is a snapshot at open and Copy rereads the current file;
separate log/result queues mean the last diagnostic may arrive after the failure signal. The
existing eventual drain remains, but the tests do not prove an immediately complete display.
No human observation is invented from those tests.

[T-341](T-341.md#2026-09-14--fedora-certificate-discovery-review) accepts locating the
platform's existing CA bundle when the bundled OpenSSL's defaults are absent. Either user-set
certificate variable prevents the write. Existing defaults are deliberately preserved; a
README-only capath or empty cafile suppresses fallback too. This is a conservative location
heuristic, not a validity check, which Low T341-R1 asks the names/documentation to say accurately.
The real-certificate context test and new Fedora artifact execution establish the reported fix.
REL-007's platform-failure reopening condition applies without requiring an external user report.

### Candidate evidence and release-gate update

Authenticated GitHub inspection identifies draft release **388772254**, `draft: true`,
`prerelease: false`, with the expected two artifacts and SHA256SUMS. The annotated tag resolves
to the exact candidate. All three existing downloaded files independently match the API's size
and digest metadata:

| Asset | Bytes | SHA-256 |
|---|---:|---|
| Windows setup | 93,475,363 | `14ed5542a7013f5135f0b569d1449aad8f26afea21ff4e443d7f1e33f82c3fb2` |
| AppImage | 68,286,968 | `ad859def982df6075433ffc2b56c683f9905f3cc53a399cb0516224a0034330a` |
| SHA256SUMS | 206 | `07247eecf08cdcb4971715544255d1bd738f26c13fea200b27e70eca7373b48d` |

The draft body equals the unchanged corrected 0.1.0 changelog section after line-ending/edge
normalization. The earlier tag/draft history is disclosed in the supplied evidence; this review
created, deleted, moved or published none of them. Asset hashing was against already-downloaded
copies, not a fresh download by the reviewer.

The earlier item-by-item gate record remains the baseline, with these candidate-specific updates:

| TESTING §8 | Disposition at `93c7357` |
|---|---|
| 1–2, static/full suite | Exact-candidate Linux and Windows CI pass; independent local checks are recorded below. These committed tests miss the two new DRM counterexamples. |
| 3, network | Retained candidate outputs: Linux 2 passed/2 Windows-only skips; Windows 2 passed. Neither was newly run by Codex. |
| 4, mandatory coverage | **Fail on DRM, both platforms' shared path.** Original 38 references remain present and the queue-menu regressions pass. Settings-freeze proof remains valid. |
| 5, migration | N/A for first release; v12 fixture and migration coverage remain. |
| 6, manual MVP acceptance | **Incomplete — T328-R4.** No new two-platform human results supplied. |
| 7, clean machines | Pass within retained evidence: Ubuntu 24.04, added Fedora 44, and Windows Sandbox, all on the new hashes. Linux launch is offscreen, not a desktop acceptance walk. |
| 8, frozen smoke | New launch/download probes and real transfers pass. **Current-candidate cancellation/companion completion/normal quit remain incomplete.** Earlier sitting is separately attributed below. |
| 9, 11–13, artifact gates | All four pass on both release payloads; independently rerun on the extracted candidate AppImage. Changed code/evidence inspection found no newly introduced credentials or private data. Prior gate scope limits remain. |
| 10, frozen update | Both release jobs and both Linux clean-machine probes pass the established synthetic-wheel install/resolve/revert check. No live upstream update or GUI click is claimed. |
| 10a, canary | Pass at 2026.8.19 in Linux canary 34908424424; Windows default CI uses the same pin. |
| 14–15, startup/Windows manual | Existing 0.1.0 startup and Narrator exceptions and approved T-327 evidence stand. New Diagnostics human observation is still required by T-340/T328-R4. |

The reports are [T-326 candidate evidence](../evidence/2026-09-14-T326-release-candidate-0.1.0.md),
[Ubuntu](../evidence/linux-0.1.0.md), [Fedora](../evidence/linux-fedora-0.1.0.md), and
[Windows Sandbox](../evidence/windows-0.1.0.md). Older captures remain retrievable at their exact
commits; current report paths now contain the newer candidate, so earlier review results must
not be read as executions of these replacement bytes.

### Human evidence: spock and the remaining sitting

During this review, asked which Linux machine supplied the earlier cancellation/quit answer,
the maintainer replied **“spock”**. This identifies the host of the **`3c011b8`** sitting. It does
not establish a new run, the host's exact OS/TLS configuration, or a Codex observation; the
original answer was the maintainer's. The earlier Windows answer remains on the earlier installer.

The nine-criterion matrix in the preceding review remains the checklist for what must actually
be observed. The current ignored sheet has the correct `93c7357` hashes but no results. Its
criterion 4 cancels a transfer; finish the sitting with the distinct §8 item 8 operations too:
let a companion transfer complete, then File → Quit, and check nothing remains running.
Criterion 9 is satisfied by independently checked machine results and needs no redundant manual
rerun. T-340's copyable-log observation belongs with criterion 6. No two-platform human sitting
was performed by Codex in this review.

### Verification record

- **Independent full Linux suite:** `pytest -n auto` at `4f7c7a5` completes with **4,650 passed,
  22 skipped, 17 warnings**, 166.42 seconds, exit 0. Log:
  `/tmp/tt-review-4f7c7a5-pytest-parallel.log`. The initial serial run was deliberately interrupted
  to use the established parallel runner after **920 passed, 2 skipped, 2 deselected**, 765.83
  seconds; its exit 2/KeyboardInterrupt is retained in `/tmp/tt-review-4f7c7a5-pytest.log` and is
  not described as a completed suite or a product failure.
- **Static checks:** Ruff lint/format pass (428 files); bare mypy and `--platform win32` both pass
  on 192 files. Version checker accepts `v0.1.0`; both changed shell scripts pass `bash -n`.
- **Candidate CI:** [34905128347](https://github.com/kottmans/tracks-and-trails/actions/runs/34905128347),
  head `93c7357`: Linux **4,170 passed/22 skipped + 480 passed**; Windows
  **4,148 passed/42 skipped + 475 passed/5 skipped**. Relevant jobs succeed; Linux orphans is
  conditionally skipped. These are inspected CI logs, not a new Windows run by Codex.
- **Release:** [34908379562](https://github.com/kottmans/tracks-and-trails/actions/runs/34908379562),
  exact candidate, all four jobs succeed, including artifact gates and frozen probes.
- **Canary:** [34908424424](https://github.com/kottmans/tracks-and-trails/actions/runs/34908424424),
  exact candidate, installed yt-dlp 2026.8.19; **4,617 passed, 43 skipped, 14 deselected**, 23 warnings,
  1,351.16 seconds. The existing expected-stale exclusions remain explicit.
- **Independent artifact gates:** all four pass on the extracted downloaded AppImage under
  `/tmp/tt-release-review-93c7357/artifact`; no fresh clean-machine/host transfer is claimed.
- **Independent mutations:** manager write guard removed → 1 failure; view recheck removed →
  1 failure; Linux fallback disabled → 3 failures/10 passes; queue Diagnostics entry suppressed
  at its actual caller → 2 failures. The first Diagnostics mutation targeted an already-imported
  definition rather than the caller and is discarded, as explained in T-340's record.
- **Independent adversarial probes:** original stale queue menu passes; obsolete automatic timer
  and both staged retry actions fail the no-retry expectation with real workers. Their limited
  guarded controls pass. All derive errors locally and contact no DRM services.

### Readiness and next correction

Keep T328-R3 and R4 open. Complete the timer and staged-action guards together, with the requested
sibling/negative controls, before treating a replacement candidate as ready for the remaining
acceptance sitting. T-326's current safety wording must reflect this review; its raw passing
machine results remain valid at their stated revision. Low T341-R1 and T326-R6 stay in ordinary
completion work, with no synthetic follow-up tasks.

Only assigned review records and their index are changed here. The task/status owner should
refresh the stale candidate/next-action wording as part of coordination. The earlier publication
README draft remains separate and unapplied. No release, tag movement, publication, source fix,
or push is performed by this review. Critical/High corrections continue under the existing
TESTING §14 rule; publication approval has not been granted.

Review-document verification: Ruff lint and formatting pass (430 files after adding the two
records), `git diff --check` passes, and all 176 local file links and new cross-review anchors
resolve. Existing review bytes remain exact prefixes; the migration verifier preserves all
381 entries in 105 files, totaling 2,403,546 historical bytes. No application suite was repeated
for these documentation-only additions.

## 2026-09-15 — Four handoffs reviewed through ea49516

**Reviewer:** Codex, independent of the implementer.
**Round:** Focused correction review of T328-R3; initial reviews of T-342, T-344,
T-345 and T-346 in their separate records.
**Base:** `9065d788bdcd114a090ef4330b1c9ee8776d1516`.
**Implementation and verification head:** `ea495166b59d3d76bd52524da7473df4af7bc513`.
**Scope:** The correction at `8d70e01`, dialog fitting at `a988847`, the T-343 filing
at `682f71d`, processing progress at `55dcf05`, startup/chip corrections at `81d7bdd`,
and the missing-file notice/T-347 filing at `ea49516`.
**Platforms verified:** Linux source and offscreen Qt, with real local workers and ffmpeg.
**Verdict:** **Changes requested.** The DRM blocker is resolved, but T346-R1 needs a
plain-text notice correction. T328-R4 and the replacement candidate's release evidence
remain outstanding. This is not publication approval.

### Findings and dispositions

| ID | Severity | Blocks release approval | Finding and evidence | Disposition/status |
|---|---|---|---|---|
| T328-R3 | Critical | Yes while open | The obsolete retry deadline is retired on manual retry and session registration. The automatic write additionally requires the failure kind for which it was planned. Add URLs retains the error kind and checks retryability for menu construction, activation and bulk retry. | **Resolved at `ea49516`**, correction introduced in `8d70e01`. Real-worker regressions, the original independent timer probe and independent guard mutations are recorded below. |
| T328-R4 | High | Yes | No new two-platform human acceptance record on the draft files was supplied. The maintainer's source trial at `ea49516` and the procedure sheet do not supply that record. | **Open — T-328's existing acceptance sitting.** Include checks A–E and the frozen-smoke cancellation, companion completion, normal quit and process check. |
| T346-R1 | Medium | Yes | The new message box interprets literal paths as rich text and can omit part of the refusal. | **Open — owned in [T-346](T-346.md).** Correct both notice constructors and verify literal rendered/accessibility text before approving the replacement candidate. |

The two Low completion corrections, T341-R1 and T326-R6, are **Resolved** in their
existing [T-341](T-341.md) and [T-326](T-326.md) records. They introduce no further task.

### Implementation reviews

| Scope | Independent outcome | Remaining boundary |
|---|---|---|
| [T-342](T-342.md), dialogs | No blocking code defect established; task **Blocked** on external observation | Real Windows display at 125%, including maximized parent |
| [T-344](T-344.md), processing progress | No blocking code defect established; task **Blocked** on installed-build observation | Long conversion, visible progress and Cancel in the draft |
| [T-345](T-345.md), startup offer/chips | **Approved for implementation** | Source trial is separate from draft checks C/D |
| [T-346](T-346.md), missing-file box | **Changes requested**, T346-R1 | Plain-text correction, then draft check E |

The two unbuilt task filings, T-343 and T-347, retain the maintainer's explicit
postponements. This review neither promotes them nor claims their behavior is fixed.

### Independent verification

- **Full Linux suite:** after activating `.venv`, `pytest -n auto` with local sockets
  permitted: **4,721 passed, 22 skipped, 17 warnings**, **178.70 seconds**, exit 0.
  Capture: `/tmp/tt-review-ea49516-pytest-unrestricted.log`.
- **Initial failed run:** invoking `.venv/bin/pytest -n auto` inside the socket-restricted
  sandbox without activating its PATH gave **58 failed, 4,663 passed, 22 skipped** in
  109.43 seconds. Of these, 56 failed on denied socket creation and two on missing Ruff/mypy
  commands in PATH. The run is retained at `/tmp/tt-review-ea49516-pytest.log`; it is not
  described as green. A focused end-to-end run reproduced the local HTTP-server
  `PermissionError`. The correctly configured full rerun above passed.
- **Static checks:** `.venv/bin/ruff check .` and `.venv/bin/ruff format --check .`
  passed, 435 files formatted; bare mypy and `mypy --platform win32` each passed on
  197 files. Windows type checking is not Windows runtime execution.
- **Original timer counterexample:** reran
  `/tmp/tt-release-review-93c7357/stale-automatic-retry.py` with this checkout on
  PYTHONPATH and offscreen Qt. Exit 0: old deadline absent; real worker results exactly
  `['network', 'drm_protected']`; automatic attempts `0`. Capture:
  `/tmp/tt-review-ea49516-stale-automatic.log`.
- **Regression controls:** the five NETWORK-to-manual-failure variants also passed
  separately. The full suite includes both automatic-write policies, their positive
  retry controls, staged DRM denial, stale staged action and the mixed-batch control.
- **Independent mutations:** a temporary pytest plugin replaced the automatic write
  predicate with the old status-only guard: both changed-failure tests failed by finding
  the DRM job queued (**2 failed**). Making all failed staged rows readable again caused
  all three new dialog regressions to fail (**3 failed**), including an extra real DRM
  probe in the mixed batch. Captures: `/tmp/tt-review-ea49516-mutation-automatic.log`
  and `/tmp/tt-review-ea49516-mutation-staging.log`. Source files were not edited for
  these mutations; the plugin was enabled only for those separate commands.
- **Additional probes:** real ffmpeg cancellation, chip widths and message-box
  accessibility/rendering are recorded with their limits in the task reviews above.

### Candidate and coordination limits

The user reports that replacement release checks and evidence for `ea49516` are being
prepared. At review time the committed T-326 evidence still identifies `93c7357`, and
the ignored acceptance sheet still has that earlier draft's run and checksums. This
review did not independently inspect a new draft, new CI results, clean-machine runs,
network suites, Sandbox or canary. The full source suite does not certify those artifacts.

After the notice correction, record the exact candidate and refreshed artifact hashes,
then take the acceptance evidence from those draft files on both platforms. The sheet
has eight human steps; criterion 9 is the machine gate. Keep the separate TESTING §8
item 8 operations explicit: cancel with a companion running, let the companion finish,
quit normally and check that no processes remain. No such human results are invented here.

The task/status owner should link these reviews and refresh the stale candidate and
next-action wording in ordinary coordination. Only reviewer-owned records and the index
were edited; task/status ownership and the implementer's evidence work remain intact.
Handoffs stay ignored. Review commits do not change the implementation head above.

Review-document checks passed: Ruff lint and formatting (439 files), `git diff --check`,
193 local file links and all new September 15 anchors. All three prior review records
remain exact byte prefixes; the migration verifier also preserves all 381 historical
entries and 2,403,546 bytes. No application suite was repeated for these prose edits.

## 2026-09-15 — Notice correction verified at 48cf076

**Reviewer:** Codex, independent of the implementer. **Round:** Disposition update
following the focused T346-R1 re-review.
**Base:** `b900047`. **Implementation and verification head:**
`48cf076aa84a7714a66e149a397b639d230de5a0`.
**Verdict:** **Blocked on replacement-candidate evidence and two-platform draft acceptance.**
This update resolves the remaining notice correction; it does not approve publication.

T346-R1 is **Resolved**, and T-346's implementation is **Approved**, as recorded in its
[focused correction review](T-346.md#2026-09-15--focused-plain-text-correction-review).
The original literal-path reproduction, all four independent guard mutations and both
startup presentation routes were verified. The independent full Linux suite passed
**4,726 tests, 22 skipped**, and Ruff plus both mypy platforms passed. No new code
blocker was established within this correction scope. T328-R3 remains resolved.

T328-R4 remains **Open, High, blocking**. The maintainer reports the tag now names
`48cf076`, the release and main CI are queued, and canary `35017328610` was dispatched.
This pass did not inspect those remote runs or the replacement draft's hashes,
network/clean-machine/Sandbox evidence. Their pending work remains T-326's evidence
task, followed by acceptance from the identified draft files on both platforms,
including checks A–E and the separate frozen-smoke cancellation/completion/quit steps.

Only the reviewer-owned records and index are updated. The task/status owner should
carry this resolution and the pending human criteria into ordinary coordination.
The source trial and local suite remain distinct from artifact acceptance; no new
Windows runtime or human desktop observation is claimed.

## 2026-09-16 — Published README and release notes

**Reviewer:** Codex. **Round:** Initial review of the newly published pages.
**Base/head:** `b112fa2` → `1aa85e95725ef014246fcd2659081b36468e48b6`; public README at `1aa85e9`, release body
generated from that head's `CHANGELOG.md`, shipped application at
`5b1bf97e63cb6f6b3ede2f8e75d0feccda3404a3` (`v0.1.0`).
**Provenance:** Claude applied the page changes. The installation paragraphs in
`7e22223` came from an earlier Codex draft; checking those paragraphs here is a
factual recheck, not a second author's independent prose approval.
**Verdict:** **Changes requested for the README**, T328-R5. Three Low improvements
belong in this page correction; they create no separate tasks or release gate.

### Findings

| ID | Severity | Blocks approval | Finding and evidence | Disposition/status |
|---|---|---|---|---|
| T328-R5 | Medium | Yes — documented command does not produce its promised output | `README.md:186–188` tells installed Windows users to run `tracks-and-trails.exe --help`; line 219 promises the log path. The shipped Windows executable is windowed, with no standard output. `app.run()` only prints for `--help` and `--version`, then exits successfully. These users see neither help nor the log path. | **Open — correct the current README.** Scope printable help/version output to the AppImage and console source install. Keep the applicable flags, but explain the installed Windows output limitation and use the profile table for its log location. Do not prescribe shell redirection as a fix; this executable has no output stream. |
| T328-R6 | Low | No | Both pages omit the two confirmed desktop limitations and their simple remedies. T-343 records taskbar Close window being ignored while Add URLs or Preferences is open on Windows. T-347 records Show in folder selecting the file without restoring an already-open Dolphin window on the download folder under KDE Wayland. Deferring the fixes does not make these behaviors discoverable to a new user. | **Open — current page completion.** Add short known-limitations text: close the dialog before closing the application from the Windows taskbar; restore Dolphin from the taskbar if Show in folder leaves it minimized. Put it in the release notes and expose it from the README. Keep implementation work in the existing T-343/T-347 tasks. |
| T328-R7 | Low | No | `README.md:142–146`, `165–169` and `179` clone/pull the default branch. After `f0d9601`, that installs `0.1.1.dev0`, although the page leads with the available `0.1.0` release and never identifies the source instructions as the development channel. The commands work; the version/channel distinction is missing. | **Open — current README completion.** State explicitly that these commands install and follow development `main`. Keep the packaged-release path as the route to `0.1.0`; a separate tagged-source recipe is optional. |
| T328-R8 | Low | No | The README supplies installation commands and a feature list, but no first-download procedure. In particular, its introductory “one-click downloads” does not explain the initial stopped queue: adding URLs to the queue does not start downloads until Start is pressed (`ui/main_window.py:1844–1869`, UX-006). | **Open — current README completion.** Add a short usage sequence using the shipped labels: + Add URLs, paste and wait for the URLs to be read, choose the preset/output location, Add to queue, then Start in the queue window. One shared sequence serves both platforms. |

### Windows help evidence

The release workflow sets `TT_RELEASE_BUILD=1` for the Windows PyInstaller build
(`.github/workflows/release.yml:221–225`); the spec sets `console=not RELEASE_BUILD`
(`packaging/tracks-and-trails.spec:178`). The entry point neither attaches a console
nor supplies a help-output file. `src/tracks_and_trails/app.py:163–168` uses only
`print()` for both help and version. The probe-specific `TT_PROBE_REPORT` mechanism
does not apply to those branches.

[PyInstaller's Windows windowed-build documentation](https://pyinstaller.org/en/stable/common-issues-and-pitfalls.html#sys-stdin-sys-stdout-and-sys-stderr-in-noconsole-windowed-applications-windows-only)
confirms that `sys.stdout` is `None` in this build mode. A local deterministic
source probe called `app.run(["tracks-and-trails", flag])` with ordinary captured
stdout and then with `sys.stdout = None`, restoring the stream afterwards:

| Flag | Console control at `1aa85e9` | Simulated missing stdout |
|---|---|---|
| `--help` | exit 0, 1,253 characters | exit 0, no printable output |
| `--version` | exit 0, 11 characters | exit 0, no printable output |

This is a source-level reproduction of the documented bootloader condition on
Linux, not a newly executed Windows installer test. The released source has the
same help/version branches; the only source change after `5b1bf97` is the version
bump. Windows source installs use `[project.scripts]`, so their console help
example does not have the packaged windowed-build limitation.

### Public content and behavior checked

- Read the complete README and release section against the CLI, installer and
  build definitions, platformdirs calls, update service, TLS selection, Diagnostics
  and processing-progress paths. The added features and three documented probe
  options exist in `0.1.0`; the `packaging/` description now fits its contents.
- Read back GitHub's current README through the contents API: **byte-for-byte
  equal** to the local file; blob `757a7cec9e2fe572155c454120da999e8698c492`.
  A cached web rendering still showed pre-release text and was not used as current
  evidence. The API was retried with permitted network access after the sandboxed
  request could not connect.
- Read back the actual [published release](https://github.com/kottmans/tracks-and-trails/releases/tag/v0.1.0),
  ID `389487493`: **not draft**, **not prerelease**. Its body equals
  `tools.changelog_section.section_for("0.1.0", CHANGELOG)` after normalizing line
  endings and trimming surrounding whitespace. Body SHA-256:
  `7c32aebc4732bd66c39ec8bb551a0dc2d20c91289ea05918cd5a947983efa64d`.
  Published `2026-09-17T02:25:34Z`, September 16 in America/Chicago; body updated
  `2026-09-17T02:46:16Z`. A repeat comparison initially unpacked the helper's tuple
  incorrectly and raised `AttributeError`; unpacking `(body, error)` and requiring
  no error produced the successful comparison above.
- GitHub's asset names, sizes and SHA-256 metadata match the retained T-326
  installer/AppImage evidence. This check did not download and hash those large
  artifacts again. See the [publication attribution](T-326.md#2026-09-16--published-artifact-attribution).
- **59 passed** in 0.27 seconds: `tests/unit/test_changelog_section.py`,
  `tests/unit/test_build_scripts.py`, and `tests/unit/test_app_release.py`.
  No application source or test files changed in this review.

### Deliberately unchanged text and release disposition

The glibc minimum and Debian/Ubuntu examples remain accurate. Fedora 44's retained
clean-machine success makes it a reasonable additional example, not a required
correction. T-341 locates the distribution's certificate bundle when the compiled
default is absent; the statement that the app uses the system trust store remains
correct without explaining that implementation detail. The Windows profile paths
agree with the platformdirs calls and the attributed STARBASE measurement; this
pass did not independently repeat that Windows measurement. The update/removal
instructions agree with the installer policy and separate profile storage.

The maintainer's September 16 ruling in [T-328](../TASKS.md#t-328--the-first-release)
authorized publication without the acceptance walk. **T328-R4 remains Open, High;
the publication gate was waived, not passed.** This supersedes the previous
next-action instruction to wait for a draft walk before publishing. No retroactive
reviewer acceptance, Windows 125% observation, checks A–E or current-artifact
cancellation/companion-completion/normal-quit sitting is asserted here. The
unperformed walk belongs in the project record; these pages need no account of
the review process and make no claim that the walk passed.

The earlier source approvals remain at their identified revisions. This page
review does not independently approve the `5b1bf97` screen-fit correction or rerun
its complete candidate gates. Its retained machine results have their own
attribution in T-326. T-328's published-update criterion remains outstanding:
**a `0.1.0.dev0` build** must discover `0.1.0` and open its published download page;
running that check on `0.1.0` would test a different result.

Only the reviewer-owned T-328/T-326 records and review index are updated. The
task/status owner should carry the page findings and this publication disposition
into ordinary completion sync, preserving the missed acceptance as waived rather
than completed. No additional task or publication approval request is introduced.

**Review-document validation:** Ruff lint and formatting passed (440 files), as
did `git diff --check`, 224 local file links, README image paths and both new
navigation anchors. Both prior review records remain exact byte prefixes; the
migration verifier preserved all 381 original entries and 2,403,546 historical
bytes. No full application suite was repeated for these review-only edits.
