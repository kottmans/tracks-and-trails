# T-346 review

**Purpose:** Independent review of the persistent missing-file notice and removal action.
**Owner:** Reviewer
**Update when:** The notice correction or its verification changes.

## 2026-09-15 — Missing-file message box review

**Reviewer:** Codex, independent of the implementer.
**Round:** Initial. **Base:** `81d7bdd`.
**Implementation and verification head:** `ea495166b59d3d76bd52524da7473df4af7bc513`.
**Scope:** T-346 and the accompanying documentation-only T-347 filing.
**Platforms verified:** Linux, offscreen Qt; no new Windows or audible screen-reader check.
**Verdict:** **Changes requested.**

### Findings

| ID | Severity | Blocks approval | Finding and evidence | Disposition/status |
|---|---|---|---|---|
| T346-R1 | Medium | Yes | `MainWindow._show_file_refusal` (`src/tracks_and_trails/ui/main_window.py:1481`) and `FileActions._show_plain_box` (`src/tracks_and_trails/ui/file_actions.py:230`) pass literal filesystem text to `QMessageBox.setText` in its default AutoText mode. A valid Linux output directory containing `<style>` causes the displayed and accessible explanation to lose the directory name and everything after it. The new tests assert `box.text()`, which still contains the original string and misses the rendering defect. | **Open — correct within T-346.** Set explicit plain-text mode in both notice constructors and verify the rendered/accessibility text against a path containing markup-like characters. Keep the ordinary-name control, OK-only refusal and removal behavior. |

### Reproduction and correction evidence

Create a Linux download directory named `<style>downloads`, save an ordinary `clip.mp3`
there, delete it, and activate Show in folder. The independent offscreen probe built
the completed row using the real `safe_output_path(directory, "clip.mp3")`, then invoked
the real queue verb and window notice. The selected root is preserved by path sanitation;
this case does not depend on bypassing the application's filename sanitizer.

For a root `/tmp/example/<style>downloads`, the label says only
`clip.mp3 is no longer in /tmp/example/`, losing the actual directory and the explanation
about moving, renaming or deletion. `QAccessible` reports that same truncated label.
Calling `box.setTextFormat(Qt.TextFormat.PlainText)` in the probe restores the entire
literal message, including `<style>downloads`, without changing `Refusal.reason`.
Ordinary paths work in both modes, and Remove still targets `job-1` in both cases.
Local probe and capture: `/tmp/tt-review-ea49516-ui-probe.py` and its `.log`.

The initial exploratory probe also used a raw filename `<style>clip.mp3`, which hides
the whole explanation. That is not the supported-path reproduction: application filename
sanitation removes those characters. The selected-directory case above establishes the
shippable defect and its narrower Medium severity. It blocks under TESTING §14 because
the notice fails to convey the literal refusal it exists to show.

### Accessibility, other behavior and checks

Using a standard message box instead of the tooltip's separate announcement is acceptable
for the inspected NFR-005 mechanism. The box exposes `AlertMessage`, the title `File not
found`, its explanatory label and standard buttons. OK is the default; removal captures
the acted-on job and uses the existing queue-removal route. An extra announcement is not
needed to replace a missing semantic role here. This is Qt accessibility-tree evidence,
not a claim that Codex heard Narrator or Orca speak the box.

The independent full Linux suite passed **4,721 tests, 22 skipped**; Ruff and both mypy
platforms passed, including the new refusal/removal tests and widget-lifetime checks.
Those passing tests do not cover T346-R1. Shared execution details are in [T-328](T-328.md).

T-347 records the maintainer's explicit deferral of Dolphin activation behavior; it
introduces no implementation for review. The source trial is not the draft's check E.
The task/status owner should keep T-346 awaiting the correction and route the focused
re-review here. Product source and task/status records were not changed by this review.

## 2026-09-15 — Focused plain-text correction review

**Reviewer:** Codex, independent of the implementer.
**Round:** Focused correction re-review of T346-R1 and its sibling-path correction.
**Base:** `b900047`. **Implementation and verification head:**
`48cf076aa84a7714a66e149a397b639d230de5a0`.
**Platforms verified:** Linux source, offscreen Qt and its accessibility tree.
**Verdict:** **Approved for the implementation. T346-R1 Resolved at `48cf076`.**
No new blocking finding was established. The draft acceptance obligation remains separate.

| ID | Severity | Blocks approval | Disposition and evidence |
|---|---|---|---|
| T346-R1 | Medium | No — resolved | **Resolved.** Both notices preserve literal path text; the original reproduction and independent guard mutations verify the correction. The sibling settings/startup changes are checked below. |

### Correction and independent evidence

Both refusal constructors set `PlainText` before assigning their sentence. The
settings warning and the shared helper for both startup refusals do the same.
Inspection of all message-box construction sites confirms the update notice was
already plain text, About deliberately formats application-owned text, and the
remaining boxes contain fixed wording and counts. No further affected path was found.

The independent original reproduction, rerun against this checkout, shows the
complete literal `<style>downloads` directory and following explanation in the
accessible label before the probe applies any control setting. The ordinary-path
control also stays whole, and Remove targets `job-1` in both cases. Capture:
`/tmp/tt-review-48cf076-original-probe.log`.

Each of the four new settings was independently removed from its exact function
with an isolated, temporary pytest plugin. The corresponding regression failed in
each case: **window 1 failed, fallback 1 failed, settings 1 failed, startup 1 failed**.
The first two fail on the truncated accessible label; the other two fail on the
explicit text-format assertion. These are mutation results, not product failures.
Source files were unchanged by the probes. Captures:
`/tmp/tt-review-48cf076-mutation-{window,fallback,settings,startup}.log`.

The settings test's explicit format assertion is appropriate for the stated contract:
today's fixed first line does not trigger Qt's markup heuristic, so rendering alone
would not test whether this sibling guard was installed. The default-mode control
in the new test file demonstrates that the accessible-label reading detects truncation.

The startup helper changes how the modal is constructed. Two additional independent
processes drove `app.run()` with an injected composition refusal, allowed the real
`QMessageBox.exec()` to run and clicked its real OK button using a Qt timer. Both
retained the complete literal message, plain-text mode, the appropriate icon and a
visible OK button. Already-running returned **3**; newer-schema returned **4**.
This verifies presentation and exception routing, not a new test of lock acquisition
or database refusal. Captures: `/tmp/tt-review-48cf076-startup-instance.log` and
`/tmp/tt-review-48cf076-startup-schema.log`.

### Checks and acceptance boundary

- Ruff lint and formatting passed (**440 files**).
- Bare mypy and `mypy --platform win32` passed (**198 files** each).
- After activating `.venv`, independent `pytest -n auto` with local test-server sockets
  permitted passed: **4,726 passed, 22 skipped, 17 warnings**, **175.94 seconds**, exit 0.
  Capture: `/tmp/tt-review-48cf076-pytest.log`.

No new Windows runtime, audible screen-reader observation or draft acceptance is
claimed. The user's reported release/CI queue and dispatched canary `35017328610`
are pending execution evidence, not checks independently inspected in this pass.
Draft check E and the two-platform acceptance record remain with T328-R4. The task/status
owner retains coordination and closure; this review changes only its canonical records
and index, and leaves the handoff ignored.

Review-document checks passed: Ruff, `git diff --check`, 179 local file links and the
September 15 anchors. Both earlier records remain exact byte prefixes; the migration
verifier preserves all 381 historical entries and 2,403,546 bytes. Source and tests
remain identical to `48cf076`. The prepared review commit message also passed its check.
