# T-347 review

**Purpose:** Independent review of KDE reveal-window measurements and the proposed correction.
**Owner:** Reviewer
**Update when:** The proposal, implementation or desktop evidence changes.

## 2026-09-18 — Initial desktop evidence and proposal review

**Reviewer:** Codex, independent of the implementer.
**Round:** Initial measurement review; no reveal implementation is submitted.
**Base:** `a5f881c233561ff5ecef3d16e2a1d7c1c6b1ec89`.
**Submitted head:** `c57b457aad721e0e16cc3457c60b9ea6c6b4ff83`.
**Checked through:** `531f4b8a1a099afda0f3285d6fbe1458230c7868`.
**Verdict:** **Changes requested** before relying on the proposed identity safeguard or
the completed three-case measurement criterion. The choice of shape remains the maintainer's.

### Findings

| ID | Severity | Blocks approval | Finding and evidence | Disposition/status |
|---|---|---|---|---|
| T347-R1 | Medium | Yes | `TASKS.md:535–540` and the evidence file's lines 70–72 call the third WindowsRunner match string an application ID and propose filtering it to establish Dolphin identity. It is **`iconName`**. KRunner serializes `(id, text, iconName, categoryRelevance, relevance, properties)`; KWin populates that member from `window->icon().name()`. An icon can be shared or changed and does not identify the application. Nor would an application-level filter distinguish two Dolphin windows showing different directories with the same basename. The proposed `isUrlOpen` precondition establishes that a matching folder exists somewhere, not that a title-matched runner UUID identifies that window. | **Open — T-347 proposal correction.** Correct the evidence's field interpretation and cost an actual identity/correlation method, or explicitly retain a heuristic with refusal to act on ambiguous matches. Measure duplicate folder names and misleading/custom icons before claiming the narrow route selects the correct existing window. |
| T347-R2 | Medium | Yes | `TASKS.md:550–551` marks all three cases remeasured, but the retained evidence describes only an already-open target folder put behind the probe window. It supplies neither a three-case before/after record nor a retained executable probe/command sequence. `isActiveWindow == false` also does not distinguish an obscured window from a minimized one, and `isUrlOpen` does not assert file selection. The record is insufficient to independently verify the full checked criterion or “reproduces exactly.” | **Open — complete the evidence or narrow the completion claim.** Retain the instrument invocation and versioned results for closed, other-folder, and target-folder/minimized cases, with all relevant window identities and an observation of selection. Preserve the original withdrawn measurements as history. |

### Evidence inspected

The desktop report credibly distinguishes raising an existing window from opening a new one,
and preserves the implementer's two invalid probes and their corrections. The reported
false-to-true active-state transition supports trying WindowsRunner; it does not establish
the identity filter proposed around it.

The third match field is established by the upstream
[KRunner serialization definition](https://raw.githubusercontent.com/KDE/krunner/master/src/dbusutils_p.h)
and [KWin match construction](https://raw.githubusercontent.com/KDE/kwin/Plasma/6.6/src/plugins/krunner-integration/windowsrunnerinterface.cpp).
KWin's inspected current source agrees. A read-only query of the local running bus returned
`Match(s) -> a(sssuda{sv})`. Installed packages were `kwin-6.7.5-1.fc44.x86_64` and
`dolphin-26.08.1-1.fc44.x86_64`; the structural source comparison is not claimed as an exact
build-source audit of those packages.

[Qt's public Wayland interface](https://doc.qt.io/qt-6/qnativeinterface-qwaylandapplication.html)
does not offer a token-request method. However,
[Qt's desktop-services implementation](https://raw.githubusercontent.com/qt/qtbase/6.11/src/gui/platform/unix/qdesktopunixservices.cpp)
does request and forward tokens internally. Thus “no directly exposed token-request API” is
the defensible bound; failure of tokenless shell/D-Bus calls does not exhaust every route
through Qt. No alternative is claimed here to preserve selection or fix this desktop's case
without measurement.

### Checks and readiness

Read the complete measurement commit, task, shipped `ui/reveal.py`, and the relevant desktop
interfaces. The shared audit/placement/toolchain/reservation/taskbar/composition checks passed
188 tests at the checked head. No focus-changing desktop probe or new reveal implementation
was executed by this reviewer; the read-only bus inspection does not reproduce activation.

Option B needs the above corrections before its stated safeguards can be relied on. A and C
remain choices rather than reviewer rulings; C's measured success is limited to the recorded
KDE desktop, not proof of activation on every desktop running Dolphin. T-347 and T-349 remain
unfinished. Only this record and its index entry are changed.

## 2026-09-18 — Ruled implementation and correction review

**Reviewer:** Codex, independent of the implementer.
**Round:** Initial implementation review, with focused verification of the proposal findings.
**Base:** `2b4b6c4352a6eb6df8db1081b78361c3bec618f2` (the preceding review records).
**Implementation head:** `da874bbee5322cef3f6f94a123b412eae27c30f9`.
**Checked through:** `24a5450de92b4e980dfd4ce58c65020b66ba1467`.
**Scope:** `e10ef01`'s corrections, `93651db`'s recorded ruling, and the new reveal source,
retained probe, tests and desktop evidence. Later T-184 parser work is outside this snapshot.
**Verdict:** **Changes requested.** The recorded choice of KWin is accepted; its implementation
has two new High blockers, and the original correlation/evidence findings remain partly open.

### Findings and disposition

| ID | Severity | Blocks approval | Finding and evidence | Disposition/status |
|---|---|---|---|---|
| T347-R3 | High | Yes | `ui/reveal.py:322` scans the entire printable Match reply for action IDs, including window captions, and returns the matched action prefix unchanged at line 330. A caption containing `1_{target-uuid}` ahead of the target's genuine `0_{target-uuid}` match passes the real target's pid/class check and is sent to Run. In KWin, action 1 closes the window; it does not activate it. The new tests also treat a `1_{uuid}` match as an activation candidate. | **Open — current T-347 implementation.** Read actual match members and accept only activation actions. Cover action-shaped text in captions/properties, non-activation IDs and the resulting Run arguments. |
| T347-R4 | High | Yes | `ui/reveal.py:348–377` performs synchronous subprocess round trips from `FileActions.reveal_selected`/`_act` (`ui/file_actions.py:207–215`). Every ListNames, per-instance isUrlOpen, Match, per-window getWindowInfo and Run can wait up to 10 seconds. A stalled Dolphin can therefore freeze the application's event loop, with additional instances extending the delay. This newly added discovery sequence violates NFR-001. | **Open — current T-347 implementation.** Keep discovery/activation waits off the GUI thread, bound the operation and verify responsiveness with delayed/error replies and shutdown while a request is outstanding. |
| T347-R1 | Medium | Yes | The iconName error is corrected and pid/class now establish which process owns a candidate window. However, `instance_showing` (`ui/reveal.py:305–310`) returns the first sorted instance answering true, even when several instances have the folder open. That does not identify which instance ShowItems selected. An instance can answer true for an inactive tab while a different instance receives the selection; raising the first can display an unrelated current tab. The original request to establish correlation or refuse ambiguity is not satisfied. | **Open — partially corrected.** Keep the verified pid/class join, but correlate with the recipient of the reveal or decline when that cannot be established. Retain evidence for multiple matching instances and inactive tabs, not only different pids in a fake match list. |
| T347-R2 | Medium | Yes | The retained probe's preconditions still accept invalid setups, and `minimize_dolphins` unloads the KWin script at lines 244–249 before sleeping at line 250. That is the exact ordering the evidence says was corrected. The table retains summary results, but not the actual versioned before/after output for each case or the direct-production invocation behind the two new rows. | **Open — partially corrected.** Make preconditions identify the target and distinguish the cases, settle/verify minimization before unloading, and retain actual commands and per-window results through the production route. The explicit deferral of visual selection to the human walk is accepted; it is not the remaining blocker. |

### Independent reproductions

All adversarial probes used injected replies or offscreen Qt; none sent a close action to KWin.
Repository source and tests were not edited.

**Action confusion.** A fake Match reply contained a genuine `0_{other-uuid}` record whose title
included `1_{bbbbbbbb-0000-0000-0000-000000000002}`, followed by the genuine activation record for
that target. The info reply assigned the target pid 777 and `resourceClass=org.kde.dolphin`.
`window_of(777, ask)` returned `1_{bbbbbbbb-0000-0000-0000-000000000002}`;
`activate_command` retained that exact ID as the Run argument. The inspected
[KWin action enum](https://github.com/KDE/kwin/blob/Plasma/6.6/src/plugins/krunner-integration/windowsrunnerinterface.h)
defines ActivateAction first and CloseAction second. Its
[Run implementation](https://raw.githubusercontent.com/KDE/kwin/Plasma/6.6/src/plugins/krunner-integration/windowsrunnerinterface.cpp)
uses the integer embedded in the ID and calls `closeWindow` for CloseAction. This establishes a
wrong window action; no data loss or real desktop close is claimed.

**Event-loop stall.** With a temporary real file, a fast fake ShowItems/ListNames reply and a
250 ms delay in the fake isUrlOpen response, a 20 ms QTimer callback did not run before
`reveal_file` returned after **271 ms**. It ran only after event processing resumed. Production
uses `subprocess.run` at the same call site with a **10-second timeout per query**. This isolates
the new discovery wait from the pre-existing launch call.

**Ambiguity.** Supplying bus names `org.kde.dolphin-777` and `org.kde.dolphin-888`, with both
answering true, returns 777 without testing uniqueness. The inspected upstream
[ShowItems handler](https://raw.githubusercontent.com/KDE/dolphin/master/src/dbusinterface.cpp)
uses `attachToExistingInstance`; its
[recipient selection](https://raw.githubusercontent.com/KDE/dolphin/master/src/global.cpp)
starts at the active-window index and tests item visibility, not the lexically first folder-open
instance. With no active Dolphin it starts at the last candidate. The risk of raising a different
recipient follows from these distinct algorithms; it is a source-based inference plus a local
lookup reproduction, not a newly performed two-window desktop measurement. The upstream source
comparison is not an exact-source audit of the installed package.

**Probe preconditions.** Calling `precondition` directly produced:

| Supplied before-state | Case | Actual answer |
|---|---|---|
| No windows (`{}`) | 2, open elsewhere | `established: a window is open and none of them shows the folder` |
| One minimized unrelated window; target folder false | 4, target minimized | `established: a window shows the folder and is minimized` |
| Target folder true, active false, minimized true | 3, behind ours | `established: a window shows the folder and is behind ours` |

The third and fourth cases must be distinguishable on the same target. The retained tool also
minimizes every Dolphin window, including pre-existing operator windows, despite its message that
those are left alone. Correct that as part of making the instrument safe to rerun. The four-case
loop calls its own `show_items`/`raise_through_windowsrunner` implementation; the separately claimed
production-function measurements have no retained executable invocation/output. Repeating that
loop alone would therefore not verify a correction to the product function.

### Checks and limits

At `24a5450`, Ruff lint and format checks passed (452 files), `mypy src` passed (67 files), and
bare mypy plus `mypy --platform win32` passed (202 files each). The combined focused selection of
reveal, new raise, taskbar, queue, option-audit and task-placement tests passed **279 tests** in
**11.11 seconds**. The green tests do not exercise the counterexamples above. The separate broader
run is recorded with the T-184/T-343 correction checks; this review does not treat its outcome as
proof of desktop identity or responsiveness.

No fresh live desktop raise/selection observation or Windows runtime execution was performed by
this reviewer. The supplied behind/minimized measurements remain implementer evidence, subject
to the instrument limitations above. The human selection walk remains explicitly due.

This is the first review of the built implementation. Correct the in-scope findings together;
TESTING §14 permits further focused verification of unresolved High defects. No approval of
T-347, the patch release or the end-of-phase walk follows. Only this record and its index entry
are updated; source corrections and task/status synchronization stay with their owners.

### CI completion during this review — additional gate finding

The submitted-head [CI run 35386695039](https://github.com/kottmans/tracks-and-trails/actions/runs/35386695039)
completed after the initial checks above. Linux and both frozen jobs passed; the Windows job
failed its unit/UI partition: **1 failed, 4,276 passed, 42 skipped** in 160.08 seconds.

| ID | Severity | Blocks approval | Finding and evidence | Disposition/status |
|---|---|---|---|---|
| T347-R5 | Medium | Yes | `tests/ui/test_reveal_raises.py:178–192` injects FakeDesktop and requests `platform="linux"` but leaves `dbus_available()` dependent on the real host. Without dbus-send, the production guard returns False before the fake replies are used. Current-head Windows CI fails this exact assertion at line 189. | **Open — current T-347 test correction.** Pin availability in the success-path test and audit the sibling fake-desktop tests for accidental early exits. Keep real absence as its own tested refusal and verify the Windows unit/UI gate. |

A temporary pytest plugin set only `reveal.dbus_available` to False for that existing test.
The local reproduction produced **1 assertion failure**, no setup/collection errors, in
**0.13 seconds**, at the same line as Windows. The unmodified control is included in the green
279-test focused selection. This is a host-dependent test defect, not a demonstrated Windows
Explorer regression. It joins the current correction batch; no separate task is warranted.

The same run passed **46 real Windows desktop tests**, Linux unit/UI **4,299 passed / 22 skipped**,
and Linux integration **500 passed**. These are inspected remote logs, not new local Windows
execution. The failing Windows full-suite gate remains a blocker despite the native desktop pass.

## 2026-09-18 — Sealed worker correction and daemon discovery regression

**Reviewer:** Codex, independent of the implementer.
**Round:** Focused correction verification and correction-regression review; unresolved High
findings permit this pass under TESTING §14.
**Base:** `72a0979bc56a4f3cd91104080d07757f28f97807`.
**Correction commits:** `6e9ffb901ce10a9ee486cfada9dfcb82018f8eb1` and
`9347755d155225da436bd41dabd97ec3f3f1341d`.
**Checked source head:** `0cec26579532ec6dfa040a3ce91a9d4795cdb5be`, initially clean.
The later T-350 filing is documentation only. T-184's cancellation change has its own review;
the unbuilt parser and migration remain outside this scope.
**Verdict:** **Changes requested.** Action safety and worker/shutdown corrections are verified.
Stricter discovery introduces a High regression on the desktop this task is meant to fix;
the measurement precondition and current-head desktop evidence also need completion.

### Findings and disposition

| ID | Severity | Blocks approval | Finding and evidence | Disposition/status |
|---|---|---|---|---|
| T347-R1 | Medium | No | Discovery now asks `isItemVisibleInAnyView(file)`, checks every candidate, and declines multiple positives or an unresolved answer. Pid/class confirmation remains. Restoring permissive discovery fails both empty/error-reply tests. | **Resolved for correlation/refusal.** This verifies conservative refusal, not identification of the actual ShowItems recipient in an ambiguous setup. Non-window daemon handling is T347-R6. |
| T347-R2 | Medium | Yes | The probe names its target, rejects the three original invalid setups, scopes minimization to owned pids and waits before unloading. Retained production-route output exists, but was committed in `6e9ffb9`, before `9347755` changed discovery. Also, `tools/dolphin_raise_probe.py:174–177` accepts `minimized='unknown'` as established non-minimized state in case 3. | **Open — partly corrected.** Require measured false for case 3; retain fresh, revision-identified production-route output after R6 is corrected, including the daemon alongside window services. Visual selection remains deferred to the accepted human walk. |
| T347-R3 | High | No | `activate_command` constructs action 0 itself. Candidate parsing is described as heuristic, including forged `struct {` boundaries; KWin must confirm Dolphin class and pid. Tests cover caption actions, non-activation records and forged candidates. Removing pid confirmation fails the forged-record test. | **Resolved for the production reveal path.** Captions can supply candidates, but cannot supply the action or bypass KWin ownership confirmation. No real close action was sent during review. |
| T347-R4 | High | No | FileActions schedules discovery on its `SealedPool`; composition registers it with shutdown and fixture isolation includes `_SHARED_POOL`. Each question has a one-second timeout. A delayed production action returns while a GUI timer runs, and shutdown waits for its worker. | **Resolved.** Scheduling, composition and pool-barrier tests pass. Restoring the global pool fails the scheduling assertion. The existing synchronous ShowItems launch is outside this added-discovery correction. |
| T347-R5 | Medium | No | Fake-desktop tests supply availability. Forcing host `dbus_available()` to False still gives **1 passed** for the formerly failing test; actual absence remains a refusal case. | **Resolved for the demonstrated host-dependent test defect.** Windows-platform typing passes; no new Windows runtime/CI pass at this unpushed head is claimed. |
| T347-R6 | High | Yes | `ui/reveal.py:357–367` queries every Dolphin bus name and returns None for any non-boolean reply. This desktop has a `dolphin --daemon` service without `/dolphin/Dolphin_1`. It returns `UnknownObject`; `ask` discards stderr, leaving an empty reply. That service prevents every raise, including when the window process answers true. | **Open — regression introduced by `9347755`.** Distinguish confirmed non-window services from unresolved candidate-window replies. Preserve refusal on timeouts and ambiguity. Add daemon-plus-visible-window coverage and remeasure the corrected production route. |

### Independent daemon reproduction

Read-only bus and process checks used `/tmp/tt-review-nonexistent/clip.mp4`. No operator folder
contents were enumerated and no window was activated, minimized or closed.

| Service | `--daemon` present | Visibility stdout | Exit / stderr |
|---|---|---|---|
| `org.kde.dolphin-1447820` | No | `boolean false` | 0 / empty |
| `org.kde.dolphin-758844` | Yes | empty | 1 / `Error org.freedesktop.DBus.Error.UnknownObject: No such object path '/dolphin/Dolphin_1'` |

A controlled call to current `raise_the_file_manager` supplied both names, `boolean true` for
the window, and the daemon's observed stdout/stderr/exit code. It returned **False**, with
**zero KWin Run calls**. The positive window answer is injected, not a live selection observation.
The observed absence of the daemon's main-window object does not depend on the queried file.

The probe already understands this distinction: `instances()` explicitly skips services without
a main window and names `--daemon` in its docstring. Production does not. Earlier successful
measurements cannot verify the later discovery change. Ignoring every failed visibility query
would reintroduce R1; a skipped service must be established not to represent a candidate window.

### Worker and mutation evidence

An independent offscreen probe blocked the FileActions worker inside injected ListNames.
`reveal_selected` returned in **0.96 ms**, a GUI timer ran, and shutdown remained
`finished=False`, `quit calls=0`, with one reveal worker active. Releasing the worker produced
`finished=True` and exactly one quit. The composition test checks registration of this same pool.

In-memory mutations left repository source unchanged:

| Weakened behavior | Independent result |
|---|---|
| Restore the global pool | Scheduling assertion fails: outstanding stays 1 instead of 2. The aborted test also reports a parentless-view teardown error; this is not a clean test run. |
| Remove pid confirmation | Forged-record test: **1 failed**. |
| Restore permissive unknown-reply handling | Empty/error cases: **2 failed**. |

The first pid-mutation attempt left the test's imported function reference unchanged and was
inert. Rebinding that reference produced the stated failure; the inert pass is not evidence.

### Checks, suite failure and readiness

At `0cec265`, reveal, raise, file-action, shutdown-pool, queue-view and task-placement tests
passed **264 tests in 20.12 seconds**. Ruff lint, formatting (452 files), `mypy src` (67 files),
bare mypy and `mypy --platform win32` (202 files each) passed. No fresh full-suite or Windows
runtime pass was performed by this reviewer.

The implementer reports **4,807 passed, 22 skipped, 6 failed** for the full suite. The narrower
saved baseline log has **5 failed, 698 passed**, but identifies no revision. That exact baseline
was requested and remains unverified; `HEAD` alone is not an immutable control. A successful
trailing `tail` did not make pytest green.

Independent investigation reproduced a fixture race: `_staged` supplies formats manually while
a real probe of `example.invalid` is still running. Its later network failure changes READY to
FAILED and removes the format control. Injecting the existing controlled child kept READY through
the same delay and passed. The helper, dialog and manager files are unchanged between this base
and head. This establishes the fixture race, not every reported failure's cause or the desktop
slowdown. It is filed as the separate Medium test-gate task
[T-350](../TASKS.md#t-350--a-staged-format-fixture-races-a-real-probe-and-loses-its-format-control).

Keep T-347 In Review. R6 needs correction and R2 needs its current-head evidence. No release or
human-selection acceptance follows. Task/status synchronization remains with its owner. The
review changes this record and its index, alongside the separately committed T-350 filing;
application source is unchanged and nothing was pushed.

## 2026-09-19 — Daemon exception correction and unsupported-query ambiguity

**Reviewer:** Codex, independent of the implementer.
**Round:** Focused verification of the High R6 correction and its regression boundary.
**Base/head:** `4222a7e1c53faa14142de94034702aaea459a876` plus the submitted uncommitted
changes in `ui/reveal.py`, `test_reveal_raises.py`, `tools/dolphin_raise_probe.py` and
`evidence/2026-09-18-T347-raising-dolphin.md`.
**Bounded patch:** SHA-256
`1d6601827b34aa334d9e48850b96d97ea37b677fc7ddedaaab43453219fb1a5a`, from
`git diff --binary` over those four paths; unchanged throughout the checks. There is no new
implementation commit or replacement desktop capture in this submission.
**Verdict:** **Blocked.** The High daemon failure is resolved for this snapshot. The exception
list reopens the Medium ambiguity finding for unsupported queries, and R2 still lacks its fresh
desktop capture. With only Medium findings remaining, TESTING §14's ordinary pass budget now
requires a maintainer decision before another focused correction review.

| ID | Severity | Blocks approval | Evidence | Disposition/status |
|---|---|---|---|---|
| T347-R6 | High | No | Production now carries stderr into discovery. A positive window plus the observed daemon-shaped `UnknownObject` on stderr reaches exactly one activation request. Removing either the exception branch or stderr handling fails the new production-route test. | **Resolved for the daemon/stderr defect in the identified snapshot.** No live desktop activation was performed by the reviewer. |
| T347-R1 | Medium | Yes | New `NOT_A_WINDOW` entries at `ui/reveal.py:357–358` classify `UnknownInterface` and `UnknownMethod` as proof that a service has no window. They establish only an unsupported interface/method. With one positive and either error from a second candidate, the current production route sends one Run request instead of leaving uniqueness unresolved. | **Open — reopened by this correction.** Remove those two exemptions or independently establish that their service has no candidate window. Keep such replies unresolved when absence has not been proved, and test each through the stderr/production route. This is the narrow unsupported-query case, not the former every-desktop daemon failure. |
| T347-R2 | Medium | Yes | Case 3 now requires measured `false`; direct checks reject `true` and `unknown`. The probe prints the revision with a dirty marker and lists daemon replies. The previous table is explicitly superseded. | **Open — instrument correction verified; fresh capture pending.** After the remaining correction, capture the production route at an identified source revision, retaining service identities and before/after states. No human file-selection observation is claimed. |

### Why unsupported is not absent

[Qt's D-Bus error definitions](https://doc.qt.io/qt-6/qdbuserror.html#ErrorType-enum)
distinguish a missing object path from a missing method or interface on an object. An existing
main window can lack the requested API; that reply supplies no item-visibility answer. This is
why the allowlist's four entries cannot all establish the absence claimed by its comment.

An injected second candidate returned each named error on stderr with exit 1. The same positive
candidate and KWin ownership replies were used in every run:

| Second candidate's error | `raise_the_file_manager` | Run calls |
|---|---|---|
| `UnknownObject` | True | 1 |
| `ServiceUnknown` | True | 1 |
| `UnknownMethod` | True | 1 |
| `UnknownInterface` | True | 1 |
| `NoReply` | False | 0 |
| `AccessDenied` | False | 0 |

This establishes the selection decision under supported D-Bus error meanings. It does not claim
that an installed Dolphin instance on this host currently lacks that method, or that a real
wrong window was raised. No desktop interaction was needed for the reproduction.

### Checks and handoff boundary

The reveal, raise, file-action and shutdown-pool selection passed **101 tests in 6.70 seconds**.
Ruff lint and formatting (452 files), `mypy src` (67 files), bare mypy and
`mypy --platform win32` (202 files each) passed. Four independently applied in-memory mutations
were caught: removing the branch (**1 failed**), dropping stderr (**1 failed**), replacing error
name parsing with substring matching (**1 failed**), and skipping every unanswered candidate
(**4 failed**). These were assertion failures, not collection/setup failures.

The producer reports a full suite running; no completed result was supplied or independently
verified in this pass. The previous full-suite failures remain documented under T-350. No new
Windows execution, desktop capture or release gate is claimed. The current revision banner
correctly reports `4222a7e plus uncommitted changes`; a fresh named-commit capture should follow
the implementation commit, rather than treating that banner as an exact source identifier.

The maintainer is asked to authorize one further focused correction/evidence pass, accept the
remaining documented risk, change scope, or defer it as TESTING §14 requires. The proposed live
capture opens/closes probe windows, minimizes one and takes focus for approximately three minutes;
this review does not grant permission on the user's behalf. Only the review record and its index
are written by the reviewer; the implementer's four uncommitted files are preserved and no push
is performed.

## 2026-09-19 — Authorized correction and current-source desktop capture

**Reviewer:** Codex, independent of the implementer.
**Round:** The further focused correction/evidence pass authorized by the maintainer and recorded
in T-347. The current handoff confirms that authorization and separate desktop consent; neither
is attributed to the reviewer.
**Base:** `5040927023c1b277833bc9bea4483ad276c82454`.
**Implementation commits:** `aecda780650018d69ce0e7a8ba5cdfdfb26abe7a` and
`bfc11c13ce46ef1c82fb1d3efb4a4af55196d699`.
**Checked head:** `d4519be85fe1b18c1b3b80ac15e592959f1580d7`, initially clean.
**Scope:** The narrowed daemon exception, production-route refusal tests, corrected probe and
retained four-case desktop capture. No parser, migration, T-350 repair or release is submitted.
**Verdict:** **Approved with follow-ups** for the T-347 implementation and machine-observable
raising behavior. All T-347 review findings are resolved. T-350 and the already deferred human
selection walk retain their existing ownership; this is not a clean full-suite or release verdict.

### Findings and disposition

| ID | Severity | Blocks this approval | Evidence | Disposition/status |
|---|---|---|---|---|
| T347-R1 | Medium | No | `NOT_A_WINDOW` now contains only `UnknownObject` and `ServiceUnknown`. `UnknownMethod` and `UnknownInterface` on stderr remain unresolved through `raise_the_file_manager`, with no Run request. Restoring both exemptions produces **2 assertion failures** in the new parametrized test. | **Resolved**, including the compatibility regression that reopened it. The existing multiple-positive, timeout and unrelated-error refusals also pass. |
| T347-R2 | Medium | No | The retained capture identifies clean source `bfc11c1`, names the daemon and its error, establishes each case's precondition and reports per-window after-states. Case 3 has measured `minimized: false`; case 4 has measured `true` before and `false` after. | **Resolved for the required production-route evidence.** File highlighting remains the explicitly deferred human observation. |
| T347-R6 | High | No | The daemon/stderr correction previously verified as a bounded patch is now committed. The daemon-shaped production test passes, and the new desktop capture names service `org.kde.dolphin-758844` returning `UnknownObject` while every case completes an activation request. | **Resolved at the submitted committed source**, continuing the previous snapshot resolution. |

R3, R4 and R5 retain their previously recorded resolutions. Current reveal and shutdown tests
cover those guards; this pass does not reopen their settled scope.

### Capture provenance and interpretation

The reviewer inspected the complete
[2026-09-19 production capture](../evidence/2026-09-19-T347-production-route.txt) and the probe's
production branch, which calls `reveal_file` followed by `raise_the_file_manager`. Its banner is
`probe revision: bfc11c1`, with no dirty marker. Comparing `bfc11c1` with `d4519be` confirms that
the latter changes only TASKS, the explanatory evidence document and the new capture; source,
tests and probe are identical. The earlier raw capture remains preserved and is labeled superseded.

| Case | Retained observation |
|---|---|
| No Dolphin window | No window before; a new active window shows the item afterward. |
| Window on another folder | The original window's reported state is unchanged; a new active window shows the item. |
| Target folder behind | Pid 1496307 is inactive and not minimized before; the same window is active afterward. |
| Target folder minimized | Pid 1496388 is inactive and minimized before; the same window is active and restored afterward. |

Every case records `reveal_file -> None` and `raise_the_file_manager -> True`. True records an
activation request, not necessarily a change of focus: in the first two cases it can target the
window ShowItems just opened. This is consistent with the retained observations; the capture does
not expose a separate state sample between ShowItems and activation. The media-thumbnail warnings
at its start do not change the recorded D-Bus/window results.

These are implementer-executed desktop measurements inspected by the independent reviewer, not
a fresh reviewer desktop run or a human confirmation of highlighted selection. No additional
window manipulation was performed in this pass.

### Checks, follow-ups and readiness

At `d4519be`, reveal, raise, file-action, shutdown-pool and placement tests passed **133 tests in
7.15 seconds**. Ruff lint and formatting passed (452 files), as did `mypy src` (67 files), bare
mypy and `mypy --platform win32` (202 files each). The in-memory allowlist mutation produced
**2 failed** in 0.13 seconds, both at the intended refusal assertion, with no setup/collection
errors. Repository source and tests were unchanged by verification. No Windows runtime test was
executed by this reviewer.

The preceding full-run log was inspected: **6 failed, 4,810 passed, 22 skipped, 2 deselected,
17 warnings**, with **pytest exit 1**. All six failures are mounted-format-panel cases already
tracked by [T-350](../TASKS.md#t-350--a-staged-format-fixture-races-a-real-probe-and-loses-its-format-control).
Its attribution to `aecda78` comes from the implementer's handoff; the log itself has no revision
header. The current `d4519be` run remains unfinished at review time, and its partial log already
contains failure markers. No final count or attribution of those current failures is asserted.

Approval closes this T-347 correction loop. It does not waive the full-suite/release gates,
complete T-350, certify other desktops, or complete the accepted end-of-phase selection walk.
The task/status owner can synchronize this approval while preserving those boundaries. Only this
review and its index are changed by the reviewer; no push is performed or authorized here.
