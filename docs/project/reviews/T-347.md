# T-347 review

**Purpose:** Independent review of KDE reveal-window measurements and the proposed correction.
**Owner:** Reviewer
**Update when:** The proposal, implementation or desktop evidence changes.

## 2026-09-18 — Initial desktop evidence and proposal review

**Reviewer:** Codex, independent of the implementer.
**Round:** Initial measurement review; no reveal implementation is submitted.
**Base:** `a5f881c233561ff5ecef3d16e2a1d7c1c6b1ec89`.
**Submitted head:** `c57b457aad721e0e16cc3457c60b9ea6c6b4ff83`.
**Checked through:** `531f4b8a1a099afda0f3285d6fbe1458230c7868`.
**Verdict:** **Changes requested** before relying on the proposed identity safeguard or
the completed three-case measurement criterion. The choice of shape remains the maintainer's.

### Findings

| ID | Severity | Blocks approval | Finding and evidence | Disposition/status |
|---|---|---|---|---|
| T347-R1 | Medium | Yes | `TASKS.md:535–540` and the evidence file's lines 70–72 call the third WindowsRunner match string an application ID and propose filtering it to establish Dolphin identity. It is **`iconName`**. KRunner serializes `(id, text, iconName, categoryRelevance, relevance, properties)`; KWin populates that member from `window->icon().name()`. An icon can be shared or changed and does not identify the application. Nor would an application-level filter distinguish two Dolphin windows showing different directories with the same basename. The proposed `isUrlOpen` precondition establishes that a matching folder exists somewhere, not that a title-matched runner UUID identifies that window. | **Open — T-347 proposal correction.** Correct the evidence's field interpretation and cost an actual identity/correlation method, or explicitly retain a heuristic with refusal to act on ambiguous matches. Measure duplicate folder names and misleading/custom icons before claiming the narrow route selects the correct existing window. |
| T347-R2 | Medium | Yes | `TASKS.md:550–551` marks all three cases remeasured, but the retained evidence describes only an already-open target folder put behind the probe window. It supplies neither a three-case before/after record nor a retained executable probe/command sequence. `isActiveWindow == false` also does not distinguish an obscured window from a minimized one, and `isUrlOpen` does not assert file selection. The record is insufficient to independently verify the full checked criterion or “reproduces exactly.” | **Open — complete the evidence or narrow the completion claim.** Retain the instrument invocation and versioned results for closed, other-folder, and target-folder/minimized cases, with all relevant window identities and an observation of selection. Preserve the original withdrawn measurements as history. |

### Evidence inspected

The desktop report credibly distinguishes raising an existing window from opening a new one,
and preserves the implementer's two invalid probes and their corrections. The reported
false-to-true active-state transition supports trying WindowsRunner; it does not establish
the identity filter proposed around it.

The third match field is established by the upstream
[KRunner serialization definition](https://raw.githubusercontent.com/KDE/krunner/master/src/dbusutils_p.h)
and [KWin match construction](https://raw.githubusercontent.com/KDE/kwin/Plasma/6.6/src/plugins/krunner-integration/windowsrunnerinterface.cpp).
KWin's inspected current source agrees. A read-only query of the local running bus returned
`Match(s) -> a(sssuda{sv})`. Installed packages were `kwin-6.7.5-1.fc44.x86_64` and
`dolphin-26.08.1-1.fc44.x86_64`; the structural source comparison is not claimed as an exact
build-source audit of those packages.

[Qt's public Wayland interface](https://doc.qt.io/qt-6/qnativeinterface-qwaylandapplication.html)
does not offer a token-request method. However,
[Qt's desktop-services implementation](https://raw.githubusercontent.com/qt/qtbase/6.11/src/gui/platform/unix/qdesktopunixservices.cpp)
does request and forward tokens internally. Thus “no directly exposed token-request API” is
the defensible bound; failure of tokenless shell/D-Bus calls does not exhaust every route
through Qt. No alternative is claimed here to preserve selection or fix this desktop's case
without measurement.

### Checks and readiness

Read the complete measurement commit, task, shipped `ui/reveal.py`, and the relevant desktop
interfaces. The shared audit/placement/toolchain/reservation/taskbar/composition checks passed
188 tests at the checked head. No focus-changing desktop probe or new reveal implementation
was executed by this reviewer; the read-only bus inspection does not reproduce activation.

Option B needs the above corrections before its stated safeguards can be relied on. A and C
remain choices rather than reviewer rulings; C's measured success is limited to the recorded
KDE desktop, not proof of activation on every desktop running Dolphin. T-347 and T-349 remain
unfinished. Only this record and its index entry are changed.
