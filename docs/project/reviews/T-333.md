# T-333 review

**Purpose:** Independent review of the yt-dlp version table and OPS-002 amendment.
**Owner:** Reviewer
**Update when:** T-333 is reviewed or installed-build evidence is supplied.

## 2026-09-13 — Initial review

**Reviewer:** Codex, independent of the implementer.
**Base:** `2e2507b5cfe4e262fcfda9b954dbbf68c390636c`.
**Head:** `8e67f6ff5b5804a8da907c56aeecac8e9fa5a0a7` (implementation `0b3703a`).
**Platforms verified:** Linux source/UI tests and retained Windows unit/UI evidence.
**Verdict:** Blocked on the task's installed-Windows observation; no new code
defect was established in this change.

| ID | Severity | Blocks approval | Finding and evidence | Disposition/status |
|---|---|---|---|---|
| T333-R1 | High | Yes — explicit acceptance criterion | The task's final criterion, seeing the table in the Windows installed build, is still unchecked. Source-based UI tests and the frozen smoke job do not supply it. | **Open — T-333's existing installed-build check.** Retain the observed installer/build identity and result; no separate task is needed. |

The table reports the worker's resolved version, the bundled pin, and a separately
checked latest release. Numeric normalization handles yt-dlp's padded date
spelling. Update remains disabled until a newer release is known, and every row
equal to the greatest known version is tagged only after Latest is known.

Composition connects Check explicitly and opening Settings only resolves the
local version. The new service operation neither installs files nor takes the
worker exclusion. The tests verify the real signal wiring and no check on open.

The September amendment expressly retains the August recovery direction. The
single recovery line, tested-baseline detail, quiet Update button, explicit Check
and one-action revert honor that direction. A subjective preference against a
visible version table is not a contradiction of the approved amendment.

The screen does not constitute a guarantee that PyPI remains unchanged between
Check and Update: installation still fetches latest at the time of the action.
No version-pinning behavior beyond the accepted latest-update mechanism is
inferred. [Shared checks](phase-5-second-batch.md).
