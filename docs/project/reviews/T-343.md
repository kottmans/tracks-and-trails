# T-343 review

**Purpose:** Independent review of Windows taskbar close through modal dialogs.
**Owner:** Reviewer
**Update when:** The implementation, native evidence or review disposition changes.

## 2026-09-17 — Initial Windows taskbar-close review

**Reviewer:** Codex, independent of the implementer.
**Base:** `507e6758c8b7bce279a10f809ed4fc1cb6e110c1`.
**Implementation head:** `2c3cd770d9a504181f83879625dbd5f3be4cb1ee`.
**Submitted evidence head:** `6ff46c9cc3b19aef78235be5ab09c800625d5ec4`.
The difference after the implementation head is task/status/evidence prose only.
**Scope:** `ui/taskbar_close.py`, composition installation, the three changed test
surfaces, the recorded Windows-only ruling and Windows CI evidence.
**Verdict:** **Approved** for the current single-main-window Windows application.
One Low robustness finding belongs in ordinary completion; it does not hold this
task in review or create a separate task.

### Finding

| ID | Severity | Blocks approval | Finding and evidence | Disposition/status |
|---|---|---|---|---|
| T343-R1 | Low | No | `ui/taskbar_close.py:180–188` asks for the global active modal widget on each iteration, but ownership is checked only before entering the loop (`:150–156`). In an independent two-owner probe, an unrelated window's modal dialog was opened first, then the target window's modal dialog. Closing the target through `consider()` closed both dialogs: after the owned one disappeared, the loop continued into the unrelated one. The existing unrelated-dialog test covers only the initial selection. No such second independent owner was found in the shipped application's normal modal routes, so this is bounded robustness work rather than a demonstrated user-path regression. | **Open — T-343 ordinary completion.** Recheck ownership for each dialog selected by the loop and stop closing dialogs when ownership changes. Add the two-owner regression while retaining the stacked-owned-dialog case. No new task or automatic extra review round is required for this Low item. |

### Implementation assessment

- `present()` installs the rule before showing the main window. Off Windows the
  helper installs nothing. Repeated installation for the same owner returns the
  existing filter.
- The native entry point checks the event type before interpreting its pointer,
  converts the real PySide `VoidPtr` to an address, and reads only the needed MSG
  fields. The native Windows test checks their offsets and sizes against
  `ctypes.wintypes.MSG`. Both WM_CLOSE and masked SC_CLOSE are covered.
- Target-handle filtering and the initial ownership check preserve other message
  routes. An unblocked close passes to Qt. An owned dialog closes through its own
  normal close/done path; the main window's close event still saves geometry and
  starts orderly shutdown. The independent composed checks below exercised this
  with the actual Add URLs and Preferences classes.
- Refusing a close while Add URLs is saving is consistent with its existing
  persistence rule. The filter asks once and consumes the gesture; it does not
  close over an unsettled commit. Requiring another gesture afterwards is an
  acceptable implementation choice within the ruling, without adding a deferred
  shutdown policy. Discarding unadded text uses the existing normal-close policy.
- The filter reaches its owner through Qt parenting and retains no QWidget
  attribute. The destruction connection retains the application and filter,
  and explicitly unregisters the filter when its owner is destroyed. The current
  tests reject restoring eager or lazy owner references and removing cleanup.

### The Qt documentation judgment

[Qt documents automatic removal in the native-filter destructor](https://doc.qt.io/qt-6/qabstractnativeeventfilter.html#dtor.QAbstractNativeEventFilter).
That describes a destructor contract. It does not establish that destroying the
QObject side of this PySide multiple-inheritance object invokes that destructor
at the required time. The retained Windows failure demonstrates that dispatch
still reached an unusable Python filter after owner destruction; it does not
trace the C++ destructor itself. The precise underlying binding/lifetime cause
remains unproved.

Explicit removal tied to owner destruction, backed by native Windows execution
after that destruction, is the appropriate boundary here. The current correction
does not depend on deciding whether Qt's C++ contract or the binding's object
lifetime caused the earlier behavior. Likewise, the old owner reference was a
forbidden shape; the earlier collection crash alone did not prove its causation.
No source correction is requested on that attribution question.

### Independent checks

**Linux source and types, submitted implementation:**

- Full offline suite, activated environment with its required local sockets:
  **4,769 passed, 22 skipped, 17 warnings**, 176.19 seconds, exit 0.
- Focused taskbar-close, composition-installation and task-placement checks:
  **68 passed**, 2.22 seconds.
- Ruff lint and formatting: passed, **443 files**. Bare mypy and
  `mypy --platform win32`: each passed on **200 files**, including tests.

**Windows execution inspected, not newly run by this reviewer:**

Read the metadata and full log of
[CI run 35276130787](https://github.com/kottmans/tracks-and-trails/actions/runs/35276130787).
It completed successfully at the exact implementation head `2c3cd77`. The native
desktop suite reports **46 passed**. Both unmodified-behavior controls pass,
followed by both corrected WM_CLOSE/SC_CLOSE cases; the MSG-layout and actual
application-launch checks also pass. Subsequent tests continue successfully
after the close test destroys its window. The full Windows suite partitions
report **4,255 passed/42 skipped** and **487 passed/5 skipped**. The Linux
partitions report **4,277 passed/22 skipped** and **492 passed**.

The native close measurement uses a window-modal QDialog with a real MainWindow,
not the actual Add URLs/Preferences dialogs. It proves the Windows message and
disabled-owner mechanism in that configuration. The actual screen classes are
covered by the local composed probe below and the existing suite. No new human
Windows interaction or packaged `0.1.1` acceptance is claimed.

**Additional composed and adversarial probes, offscreen Qt:**

- Built the real application with temporary database, settings, cache and geometry
  paths, plus a quiet yt-dlp service. Closed its actual Add URLs dialog containing
  unadded text, then separately its actual Preferences dialog, through the filter's
  `consider()` entry point. Both dialogs and their main window closed; geometry
  was saved and the real orderly-shutdown lifecycle finished.
- Set the actual Add URLs dialog's saving flag to exercise its close-refusal
  branch. The filter consumed the request while both windows stayed visible.
  Clearing the simulated saving state then allowed normal close and shutdown.
  This checks the real dialog's refusal behavior; it is not a new asynchronous
  database-write test.
- Opened one modal dialog under each of two independent owners, with the target
  owner's dialog last. The active widget was initially the owned dialog; after
  handling its close, the other owner's dialog was also hidden. This is R1's
  counterexample and is outside the app's current normal single-owner routes.

**Independent guard mutations:** a separate temporary pytest plugin restored
four specific defects: missing `int()` conversion, an eager QWidget reference,
a lazy QWidget reference, and missing explicit filter removal. The unmodified
control ran **3 tests, all passed**. Each mutation ran its selected existing test
and produced **1 test failure**, exit 1, with **zero collection/setup errors and
zero skips**. The runner checked JUnit counts, not merely a nonzero exit code.
No repository source was edited for these runs. This independently verifies those
four guards; it does not claim to repeat the supplied 22-mutation campaign.

### Completion boundary

The decision, native Windows behavior and unadded-input policy are sufficiently
established for the ruled implementation scope. T-343 may complete with R1 routed
through its ordinary completion work. T-347 and T-349 retain their recorded order.
The planned `0.1.1` artifact and end-of-phase observations remain with their owning
release work; neither source tests nor the generic-dialog native measurement are
recorded as that future human walk.

Only this reviewer-owned record and the review index are changed. The task/status
owner should record this approval, exact implementation head and Low disposition
when moving the task to the completed record.

**Record validation:** all 160 local file links in this record and the index
resolve, and the new index anchor matches this heading. Existing T-326/T-328
review records are unchanged. The migration verifier preserves all 381 original
entries in 105 files (2,403,546 historical bytes). `git diff --check` passes.
